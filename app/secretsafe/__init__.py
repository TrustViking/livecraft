"""Сейф livecraft: секрет в памяти, шифрование файлов сейфа, привязка к Windows-аккаунту (CLAUDE.md §7).

Имя пакета — `secretsafe`, а не `secrets`: иначе оно совпало бы и с именем папки данных `secrets\\`,
и с модулем stdlib `secrets` (§5).
"""
