"""Нейросеть контура A (CLAUDE.md §2 строки про llm\\, §5): разъём, выбор модели, учёт расхода, реализации.

`backend.py` — разъём `LlmBackend` и общие запрос и ответ; `errors.py`, `usage.py`, `selection.py`, `json_text.py`
— общие объекты, о конкретной нейросети они не знают. Реализации — в `backends\\`. Контур B этот пакет не
импортирует (§4).
"""
