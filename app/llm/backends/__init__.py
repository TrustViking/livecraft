"""Реализации разъёма нейросети `app\\llm\\backend.py::LlmBackend`: у каждой нейросети — свои модули с приставкой имени.

Сейчас реализация одна — OpenAI Responses API (`openai.py` и модули `openai_*`). Вторая нейросеть добавляется
новыми модулями рядом, без правки merge и прогона.
"""
