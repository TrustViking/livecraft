# CLAUDE.md — livecraft

Инструкции для агента (Claude Code / Cowork / Codex), работающего в `D:\_projects\livecraft`.
Этот файл — единственный документ требований и правил проекта. Расхождение кода с этим файлом — доложить, не решать молча.

Составлен 20-09-2026 по разбору трёх рабочих репозиториев: `D:\_projects\restreamer` (коммит `3f10426`, 17-09-2026), `D:\_projects\broadcaster` (коммит `efcbb3e`, 09-09-2026), `D:\_projects\planers` (коммит `9af58a6`, 19-09-2026). Всё, что ниже названо именем файла или символа, проверено по исходникам этих репо на дату разбора.

---

## 0. Главное правило: код пишется в стиле ООП

**Весь код livecraft пишется объектно-ориентированно. Это не рекомендация, а условие приёмки любой задачи.**

- Данные живут в объектах предметной области: Таблица, Ряд, Видео, Слот, Канал, Форма, Сейф, Эфир. Функции заполняют, читают и сравнивают поля объекта; собственных данных у функций нет.
- Правило, которое переводит значение одного объекта в значение другого (ряд таблицы → слот, слот → ответ формы, ответ YouTube → канал, поле настройки → запись сейфа), — **метод объекта-владельца правила**, а не функция по месту использования.
- Новая проверка = новое поле объекта и правило внутри самого объекта. Не новая функция рядом с вызовом.
- Изменилась форма, таблица, площадка или набор настроек — правится структура **одного** объекта, а не цепочка функций.
- Объект с незаполненными полями не выбрасывается и не «чинится» молча: причина — подробно в лог, кратко в терминал; дальше по конвейеру объект не допускается (см. §6 «Допуск»).
- Классы вместо функций с длинными списками аргументов: запрос/конфиг-объект вместо семи именованных параметров.
- Модуль-функция вне объекта допустима **только** как чистое преобразование без знания о предметных объектах (`normalize_handle`, `safe_trim`, `parse_sheet_datetime`). Любое другое исключение — строкой-обоснованием в отчёте задачи.
- В отчёте каждой задачи: какие объекты и какие их поля добавлены или изменены.

Образец, на который надо равняться, — `D:\_projects\planers\app\pipeline\plan.py::PlannedBroadcast`: один объект несёт слой из пакета, слой с площадки и слой результата, сам решает допуск (`admit`), сам решает отправку ключа (`decide_key_delivery`), сам строит свою запись памяти (`to_record`). Именно так, а не «функция, принимающая девять аргументов».

Антиобразец, который переносить в livecraft **нельзя**, — процедурный слой restreamer: `app\planning\batch_planner.py` (`build_prepared_videos`, `materialize_prepared_previews`, `derive_planned_videos` — три функции на 474 строки, состояние в кортежах), `app\llm\merges\merge_service.py` (свободные функции `attempt_openai_merge_with_audit`, `enforce_openai_merged_paragraphs`). Переносим оттуда **поведение и правила**, а не форму кода: каждая такая функция в livecraft становится методом объекта (`SheetPlan`, `VideoPreparation`, `MergeJob`).

---

## 1. Что это

**Livecraft** — от *live* (прямой эфир) и *craft* (создавать, мастерить): подготовка эфиров «под ключ» на одной машине, одной программой.

Сейчас работа разорвана на две программы и ручную пересылку:

| Сегодня | Программа | Делает |
|---|---|---|
| машина 1 | `restreamer` / `broadcaster` | Google Sheets → yt-dlp → LLM-merge → анонсы в Google Docs и Telegram → пакет `plan_*.bcast` документом в Telegram |
| руками | оператор | пересылает пакет владельцу канала, тот кладёт его в `bcast\` |
| машина 2 | `planers` | пакет → сверка с YouTube → эфиры → ключи → `keys.txt` → Google-форма → отчёт |

Livecraft склеивает это в один процесс и заменяет публикацию анонсов планированием эфиров:

```
Google Sheets (план стримов)
   → ряды: ссылка, дата, время
   → разбор по дате, времени и языку
   → метаданные и превью источников (yt-dlp)
   → LLM-merge: одно название + одно описание на слот (функция merge)
   → объект-слот StreamSlot
   → YouTube: сверка, создание/исправление эфира, поток, ключ, обложка
   → Google-форма ключей → стример
   → keys.txt, консоль, отчёт, лог
```

Livecraft **не**: стримит; не ведёт расписание (источник — Google Sheets, её ведёт оператор); не обходит Google-форму; не трогает эфиры, не соответствующие ни одному слоту; не публикует анонсы в Docs и Telegram (v1, §12 решение 3); не хранит логины и пароли площадок.

Площадки v1 — только YouTube. Facebook и Rumble — за пределами v1.

---

## 2. Что откуда берётся

Два контура, шов между ними — объект-слот. Ничего не переписывается «с нуля», где есть проверенный боем код, но всё переносимое приводится к ООП (§0).

### Контур A — «Замысел»: из `restreamer` (`D:\_projects\restreamer\app\`)

| Откуда | Что даёт | Как переносить |
|---|---|---|
| `google\sheets_client.py` (`read_rows`), `planning\sheet_loader.py::load_sheet_state`, `BatchSheetState` | чтение диапазона таблицы в `SheetRow` | в объект `SheetPlan`: читает, отдаёт ряды, знает свой диапазон и колонку ссылок |
| `planning\sheet_parser.py::parse_sheet_datetime`, `column_letters_to_index`, `extract_end_column_letters_from_sheet_range` | разбор даты и времени (7 форматов даты, 6 форматов времени), буквы колонок | чистые преобразования — переносятся как есть, в `core\sheet_text.py` |
| `core\models.py::SheetRow`, `RowVideoCharacteristics`, `VideoMetadata`, `NormalizedImage`, `PreparedVideo`, `PlannedVideo` | модель ряда и подготовленного видео | сливаются в объекты `SheetRow` → `SourceVideo` → `SlotVideo` |
| `planning\link_normalization.py`, `dedup.py`, `duplicate_rows.py` | нормализация ссылок, дедупликация, подсветка дублей рядов | методы `SheetPlan` / `SourceVideo` |
| `core\language.py`, `language_profile.py`, `language_resolver.py`, `language_display.py`, `planning\merge_rules.py::planned_video_block_language` | определение языка ряда и языка блока (`langdetect`, `pycountry`) | объект `LanguageResolver`; правило языка блока — метод `SlotVideo` |
| `ingest\youtube_metadata.py` (yt-dlp через `subprocess` + JSON, `extract_youtube_video_id`) | название, описание, превью, языки аудио и субтитров источника | объект `MetadataFetcher` (Protocol) + `YtDlpFetcher` |
| `media\image_normalizer.py` | нормализация превью (Pillow) | метод объекта `Preview` |
| `planning\slots.py::planned_video_slot_key`, `planned_video_time_key`, `language_sort_key` | ключ слота `{ddmmyy}_{HHMM}_{lang}`, порядок языков | методы `StreamSlot` |
| `llm\` целиком: `llm_client.py`, `llm_factory.py`, `providers\provider_openai.py`, `llm_rate_limits.py`, `llm_usage_tracker.py`, `model_selection.py`, `models\model_identity.py`, `model_pricing.py` | транспорт OpenAI Responses API, выбор модели, тарифы, учёт расхода | переносится, ключ API — только через сейф (§7) |
| `llm\merges\` целиком (20 модулей: `merge_orchestrator.py::MergeOrchestrator.run`, `merge_prompt.py`, `merge_parser.py`, `merge_validation*.py`, `merge_quality.py`, `merge_retry.py`, `merge_formatting.py`, `script_mix_repair.py`, `quality_*.py`) | **функция merge**: контракты compact (2 источника) / expanded (3+) / narrative (одно событие), валидация, нормализация, повторы | ядро переносится как есть; свободные функции `merge_service.py` становятся методами `MergeJob` |
| `resources\text\prompt_merge_*.txt`, `prompt_retry_*.txt`, `llm\prompts\templates.yaml`, `resources\text_catalog.py`, `resource_loader.py` | промпты и лексиконы | переносятся файлами, без правок текстов |
| `publish\post_llm_sanitation.py`, `sanitizers\description_composer.py`, `quality_gate.py`, `tail_parser.py`, `url_selector.py`, `core\safe_trim.py`, `core\video_title_cleanup.py`, `core\description_cleaner.py` | санация текста после LLM, безопасная обрезка ≤100 / ≤5000 | переносятся; **обрезка обязательна** — лимиты YouTube жёстче лимитов анонса |
| `publish\slot_publish_texts.py::resolve_slot_publish_texts`, `SlotPublishTexts` | «какие именно тексты ушли в анонс» | становится «какие тексты уходят в эфир» — метод `StreamSlot.texts` |
| `publish\planer_package_exporter.py::PlanerPackageFormat` | **схема слота** (`slot_id`, `date`, `time`, `start`, `language`, `title`, `description`, `previews`, `sources`) | схема остаётся как контракт объекта `StreamSlot`; ZIP-упаковка — нет (§4) |
| `runtime\ytdlp_updater.py`, `cookies_updater.py`, `deno_updater.py`, `single_instance.py`, `startup_banner.py` | автообновление `tools\yt-dlp.exe` и `tools\deno.exe`, срок cookies, файл-замок одного экземпляра | переносятся; замок — `state\livecraft.lock`, захват **до** настройки логов |
| `paths\_root.py`, `project_paths.py`, `core\safe_trim.py`, `bootstrap\logging_config.py` | корень проекта, пути, логирование | заменяются образцом из `planers` (§5) |

**Не переносится из restreamer:** `telegram\`, `telegram_bot\`, `bot_main.py`, `promo.py` (aiogram и весь Telegram-контур); `publish\google_docs_writer.py`, `docs_*`, `doc_*`, `google\docs_client.py`, `drive_client.py` (Google Docs и Drive); `pipeline\daily_doc_publish.py`, `daily_telegram_publish.py`, `operator_notifier.py`; `core\cta_detection.py`, `official_links.py` и лексиконы CTA (нужны анонсу, не эфиру — если окажутся нужны в описании эфира, вернуть отдельной задачей); `config\app_config_loader.py` вместе с YAML (§5: конфиги — JSON, как в planers).

### Контур B — «Эфир»: из `planers` (`D:\_projects\planers\app\`)

Переносится **целиком и почти без правок** — это самая отлаженная часть пайплайна (задачи 5a–5n-D, живые прогоны с 13-09-2026 по 19-09-2026).

| Пакет | Что даёт |
|---|---|
| `pipeline\plan.py` | `PlannedBroadcast` (объект запланированного эфира), `BroadcastSpec` + `diff` (единственное определение «одинаковый»), `ChangedField`, `FixCall`, `FIX_CALLS`, `FIXABLE_FIELDS`, `REPORTED_FIELDS`, `Decision`, `AdmissionKind`, `AdmissionReason`, `admit`, `decide_key_delivery`, `confirm_key`, `to_record` |
| `pipeline\selection.py::build_planned`, `SkipReason`, `Selection` | размножение слота по каналам своего языка, признак `too_late` по `min_lead_minutes` |
| `pipeline\reconciler.py::Reconciler` | сверка с площадкой: опознание по минуте старта UTC + маркер `slot_id` в названии потока, заглушки обложек, решения MATCH / UPDATE / CREATE / AMBIGUOUS |
| `pipeline\runner.py` | оркестрация, `_admit_all`, `_check_form_dates`, `_Executor` (создать → привязать → исправить → настройки видео → факты → ключ в форму), `decide_exit` → `RunExit` |
| `platforms\youtube.py::YouTubePlatform` | все вызовы YouTube Data API v3 через единственную точку `_execute`: пауза `youtube_pause_seconds`, повторы, память отказов; `REASON_BEHAVIORS`, `OPERATION_REASON_BEHAVIORS`, `CREATING_OPERATIONS` (создающие вызовы не повторяются вслепую), `YOUTUBE_STREAM_KEY_PATTERN` |
| `platforms\channel.py`, `channel_sync.py`, `passport.py`, `verified.py`, `base.py`, `fake.py` | объект-канал `Channel` со статусом, `ChannelBook` (проверка без браузера при старте + фаза входов), паспорт «ник ↔ id ↔ файл токена», шлюз `VerifiedPlatform` (к площадке — только каналы `READY`), `FakePlatform` для тестов |
| `form\` (`base.py`, `discovery.py`, `key_form.py`, `submitter.py`) | объект-форма `KeyForm`, разбор `FB_PUBLIC_LOAD_DATA_`, `FormAnswers` / `MissingAnswer`, `DateCoverage` (предстартовая проверка дат), POST `formResponse`, структурное подтверждение `read_confirmation` |
| `records\slot_record.py`, `record_store.py` | память: `SlotRecord`, `SlotStage`, `RecordResults`, `ConfirmedAnswer`, `RecordStore` на stdlib `sqlite3` |
| `google\auth.py` | OAuth с одним токеном на канал, скоуп только `…/auth/youtube`, `save_token` / `drop_token` только после подтверждения канала, `LOGIN_TIMEOUT_SEC` = 600 |
| `output\` (`console.py`, `report.py`, `keys_file.py`, `progress.py`), `ui\messages_ru.py` | шесть блоков консоли, отчёт `.md`, `keystreams\keys.txt`, строки прогресса, все русские тексты в одном месте |
| `core\dates.py`, `retry.py::RetryPolicy`, `retention.py`, `text.py` | форматы дат, единственная политика повторов, чистка старья, `normalize_handle` / `token_file_stem` |
| `paths.py`, `observability\logging_setup.py`, `config\loader.py`, `version.py` | корень рядом с exe (frozen) или репо (dev), атомарная запись, логи, JSON-конфиги без умолчаний, единственный номер версии |
| `planer.spec`, `planer.iss`, `build_local.bat`, `build_release.bat`, `planer.bat` | PyInstaller (одна папка), Inno Setup, две сборки (release без секретов / local с секретами), пусковой bat |

**Не переносится из planers:** `package\` (`bcast.py`, `model.py`, `reader.py`) — пакета-файла больше нет (§4); `tools\` (пробники) переносятся, но в поставку не идут.

---

## 3. Сквозной сценарий

1. Оператор один раз настраивает программу через графический настройщик (`livecraft.bat --setup`, §8): ключ OpenAI, ссылка на таблицу плана, ссылка на форму ключей, каналы YouTube, технические настройки. Всё секретное ложится в сейф зашифрованным (§7).
2. Оператор запускает `livecraft.bat`. Программа:
   1. берёт файл-замок `state\livecraft.lock` (второй экземпляр — сообщение и код 1); настраивает логи; печатает шапку `Livecraft <версия> — <дата время>`;
   2. открывает сейф, читает настройки и `secrets\channels.json`; сверяет каналы по токенам **без браузера** (ник → id по паспорту → название; сменённые ник и название выравнивает сама);
   3. читает Google Sheets (диапазон из настроек), разбирает ряды: ссылка, дата, время; нормализует ссылки, отбрасывает дубли и прошедшее; определяет язык каждого ряда; группирует ряды в **слоты** по `{дата}_{HHMM}_{язык}`;
   4. по каждому источнику слота берёт метаданные и превью (`tools\yt-dlp.exe`), нормализует картинку;
   5. **merge**: на каждый слот — один запрос к LLM, контракт по числу источников (compact / expanded / narrative), разбор, валидация, нормализация, повторы; на выходе — название ≤100 и описание ≤5000 после санации и безопасной обрезки;
   6. собирает объекты `StreamSlot` — это шов между контурами; дальше про таблицу, yt-dlp и LLM никто не знает;
   7. читает форму ключей (один GET на уникальный URL), делает предстартовую проверку покрытия дат (`KeyForm.date_coverage`);
   8. отбирает слоты под языки каналов → объекты `PlannedBroadcast`; фаза входов в браузере для каналов, которым нужен вход; после неё браузер не открывается;
   9. допуск: канал подтверждён + форма прочиталась + для даты, языка и прочих полей в форме есть варианты. Не допущенный объект на YouTube ничего не создаёт и не исправляет, в форму ничего не шлёт, но остаётся в отчёте, консоли и `keys.txt`; код выхода 1;
   10. сверка с YouTube и действия по каждому объекту: нет эфира → создать (эфир → поток с маркером `slot_id` → bind → обложка → настройки видео); есть и что-то разошлось → исправить **только нужными вызовами** (`FIX_CALLS`); совпало → копия;
   11. ключ и адрес потока читаются **только с площадки**; объект сам решает, должен ли ключ уйти (`decide_key_delivery`); ключ → форма → подтверждение → запись в память; затем следующий объект;
   12. перегенерирует `keystreams\keys.txt`, печатает итог и блоки в консоль, пишет `logs\{DD-MM-YYYY}_{HHMMSS}_report.md` и `…_livecraft.log`, чистит старьё по `keep_days`.
3. Оператор смотрит отчёт. Что-то не так с эфиром — правит или удаляет его на YouTube руками; следующий запуск это увидит.

---

## 4. Шов между контурами: `StreamSlot`

Пакет `*.bcast` был форматом **пересылки** между двумя машинами. Машина теперь одна — файла-пакета в v1 нет. Но **схема** пакета остаётся: она описывает ровно то, что контур «Эфир» должен знать о слоте, и она уже проверена в бою.

`StreamSlot` (пакет `app\slots\`) — единственный объект, который пересекает границу:

```
slot_id           "16-09-2026_1900_uk"     — {DD-MM-YYYY}_{HHMM}_{lang}
date              "16-09-2026"             — для людей и для формы
time              "19:00"                  — для людей, в форму не уходит
start             ISO-8601 со смещением    — момент для сравнения с YouTube
language          "uk"
title             ≤100 после safe_trim
description       ≤5000 после safe_trim
previews          кортеж Preview           — 1..N, обложкой ставится одно случайное
sources           кортеж ссылок источников
```

Правила шва:

- Контур A ничего не знает про YouTube, форму, каналы и ключи. Контур B ничего не знает про таблицу, yt-dlp, LLM и Telegram. Единственный общий импорт — `app\slots\`.
- Тексты слота — **окончательные**: контур B их не редактирует, только обрезает по лимитам площадки при сравнении спек (обрезка с обеих сторон, иначе длинное название правилось бы бесконечно).
- Прошлое фильтрует контур B сам, даже если контур A уже отфильтровал.
- `--export-slots <путь>` — отладочный ключ: выгрузить слоты запуска JSON-ом (та же схема, без ZIP), чтобы прогонять контур B на сохранённом входе без обращений к таблице и LLM. Обратного чтения из файла в боевом запуске нет.
- Границу держат тесты: контур A тестируется по `StreamSlot` на выходе, контур B — по `StreamSlot` на входе и `FakePlatform` / поддельному `requests`.

---

## 5. Структура, корень и файлы

```
app\
  main.py                 argparse → режимы; коды выхода 0/1/2/3
  version.py              APP_VERSION — единственный номер версии
  paths.py                корень рядом с exe (frozen) или репо (dev); write_text_atomically
  core\                   dates.py, text.py, retry.py (RetryPolicy), retention.py, safe_trim.py, sheet_text.py
  secretsafe\             сейф: value.py (SecretValue), vault.py (Vault), store.py (VaultStore), crypto.py (AES-GCM + HKDF), dpapi.py (ctypes), program_key.py (генерируется сборкой, в git нет)
  config\                 loader.py → LivecraftConfig (настройки + каналы), объекты ChannelConfig / LivecraftSettings
  observability\          logging_setup.py — маскирование секретов и ключей потока
  runtime\                single_instance.py (InstanceLock — замок одного экземпляра),
                          ytdlp_updater.py, deno_updater.py, cookies_updater.py — автообновление внешних бинарников
  sheets\                 client.py (Google Sheets API), plan.py (SheetPlan, SheetRow), rows.py (нормализация, дедуп, язык)
  sources\                fetcher.py (Protocol), ytdlp.py (YtDlpFetcher), preview.py (Preview, нормализация Pillow), metadata.py
  llm\                    client.py, factory.py, providers\, rate_limits.py, usage_tracker.py, model_*.py, merges\ (контракты merge), prompts\
  texts\                  санация после LLM, safe-обрезка, композиция описания, quality gate
  slots\                  slot.py (StreamSlot), builder.py (SlotBuilder: ряды + метаданные + merge → слоты), preview.py
  platforms\              base.py, youtube.py, channel.py, channel_sync.py, passport.py, verified.py, fake.py
  google\                 auth.py — OAuth: скоупы и токены (§9)
  form\                   base.py, discovery.py, key_form.py, submitter.py
  records\                slot_record.py, record_store.py (SQLite secrets\livecraft.sqlite3)
  pipeline\               plan.py (PlannedBroadcast, BroadcastSpec), selection.py, reconciler.py, runner.py
  output\                 console.py, report.py, keys_file.py, progress.py
  setup\                  графический настройщик (§8): app.py, panels\, fields\, validators.py; readiness.py — готовность к запуску (сейф + конфиги), её же показывает настройщик
  ui\messages_ru.py       ВСЕ тексты консоли, отчёта, keys.txt и настройщика
  examples\               channels.example.json — шаблон для репо и README; в сборку не идёт
  tools\                  пробники разработки (форма, обложки, эфиры); в поставку не идут
  tests\                  pytest; data\ — сохранённые страницы формы и ответы yt-dlp
secrets\                  vault.dat, vault.local.dat, livecraft.json, channels.json, channels.previous.json,
                          channels_passport.json, livecraft.sqlite3, client_secret.json, <handle>.token.json,
                          sheets.token.json, cookies.txt
tools\                    yt-dlp.exe, deno.exe (внешние бинарники, обновляются сами)
image\                    локальные превью по шаблону {date}\{language}
keystreams\keys.txt       ключи потоков для ручной передачи
state\                    livecraft.lock, отметки автообновлений
logs\                     {DD-MM-YYYY}_{HHMMSS}_report.md, …_livecraft.log
livecraft.bat             рядом livecraft.exe — запускает его, иначе python -m app.main из .venv_livecraft
livecraft.spec            PyInstaller, одна папка; livecraft.iss — Inno Setup; livecraft.ico
build_release.bat         exe + инсталлятор без данных оператора; build_local.bat — то же с ними
```

Папка `platforms\` — во множественном числе (`platform` занято stdlib). Папка `secretsafe\` — не `secrets\`, чтобы имя пакета не совпадало с именем папки данных.

Корень — `app\paths.py::resolve_root`: `LIVECRAFT_ROOT` из окружения (только тесты и отладка), иначе папка exe в frozen-режиме, иначе корень репо. Ставить в папку с правом записи (`D:\_exe\Livecraft`), не в Program Files.

**Конфиги — JSON, все поля обязательные, умолчаний в коде нет** (решение planers, задача 5b; YAML и PyYAML не тянем). Два файла: `secrets\livecraft.json` (технические настройки, поставляется со сборкой заполненным) и `secrets\channels.json` (каналы, заполняет оператор или настройщик). Нет файла или обязательного поля — код 2, ошибка и точный шаблон файла в консоли.

**Ссылок, ключей и entry-ID в конфигах нет вообще** — они только в сейфе (§7).

---

## 6. Инварианты — не нарушать без явного согласования с Артуром

0. **Рабочая единица контура A — слот** (`app\slots\slot.py::StreamSlot`), контура B — **объект запланированного эфира** (`app\pipeline\plan.py::PlannedBroadcast`): один объект = один эфир одного слота на одном канале. Правило допуска — только `PlannedBroadcast.admit`. Память — `secrets\livecraft.sqlite3`, одна запись на (`slot_id`, id канала на YouTube); обратно читаются только результаты (эфир, поток, ключ, подтверждение формы). Задания из памяти не берутся: что делать, каждый запуск решают свежая таблица, форма и `channels.json`.
1. **Истина об эфирах — на YouTube; ключ потока и ссылка на эфир читаются только с площадки.** Память — истина только о том, что форма подтвердила. Перед любым созданием — `liveBroadcasts.list(broadcastStatus=upcoming)` (без `mine` — параметры взаимоисключающие); совпадение по минуте старта UTC + маркер `slot_id` в `liveStreams.snippet.title`. Опознанный эфир сверяется по всем диктуемым полям (`BroadcastSpec.diff`): исправимое расхождение — правка на месте только нужными вызовами (`FIX_CALLS`), план в приоритете; неисправимое (`REPORTED_FIELDS`) — строка в лог, «внимание:» в консоли и строка в отчёте. Решения «создать заново» нет.
1a. **В форму уходит ключ, который в этом запуске должен дойти до стримера** — одно правило, `PlannedBroadcast.decide_key_delivery`: объект допущен, не `too_late`, ключ и адрес потока взяты с площадки в этом запуске, и в памяти нет подтверждения ровно этой тройки «тот же ключ, тот же адрес формы, те же ответы». Момент отправки — сразу после действий по своему объекту; финального прохода нет; подтверждение сразу пишется в память. Задвоение строки у стримера допустимо: стример берёт последнюю строку по дате, каналу и языку — README обязан это объяснить.
2. **Google-форма — единственный канал передачи ключей стримеру.** Никаких обходных путей. `entry.*`-ID не хардкодятся и не конфигурируются — читаются из HTML формы. Программа никогда не отправляет значение, которого нет среди вариантов вопроса: нет даты в «Время стрима» → объект не допущен. Названия вопросов, тексты вариантов и формат даты — настройки (сегодня `app.planer.form_fields` / `form_values` / `form_date_format` в `restreamer\app\config\runtime\app_config.example.yaml`); **сама ссылка на форму — в сейфе**.
3. **Google Sheets — только чтение плана.** Обратной записи в таблицу в v1 нет (в restreamer она была под флагами `STG_SHEETS_LINK_WRITEBACK` / подсветки дублей — в livecraft не переносится). Диапазон — настройка (`sheets_range`, сегодня `"A:F"`); **id таблицы — в сейфе**.
4. **Даты `DD-MM-YYYY`, время `HH:MM` по Киеву** — во всех файлах, именах и отчётах. Исключения: `start` слота и `slot_start_utc` в памяти — ISO-8601, только для сравнения моментов; значения, уходящие в форму, — в формате даты формы.
5. **Никаких паролей.** Авторизация — OAuth. Ключ канала — ник `handle` (без «@», NFC, без регистра): все словари, кеши, группы вывода и память отказов — по нему. Паспорт `secrets\channels_passport.json` связывает ник ↔ id YouTube ↔ файл токена; id канала не меняется никогда. Ник или название сменились, а id подтверждён — программа выравнивает `channels.json`, файл токена и паспорт сама.
6. **Секреты не попадают в вывод.** Ключи потока маскируются в логах (последние 4 символа), полностью — только в `keys.txt`. Ключ OpenAI, id таблицы и URL формы **не печатаются нигде**: ни в консоль, ни в отчёт, ни в лог, ни в тексты исключений. Подробно — §7.
7. **Настройки эфира задаёт программа, а не канал.** Аудитория — всегда «не для детей»; категория, автостарт и «ставить обложку» — из `livecraft.json`; язык — из слота; видимость — из `channels.json`. Всё правится одним проходом по ресурсу `videos` (`part=snippet,status`, read-modify-write целиком). Возрастное ограничение 18+ через API только читается.
8. **Эфиры не удаляются автоматически.** Эфиры без соответствия слоту не трогаются и в отчёт не попадают. Эфир с маркером программы, чьего слота больше нет, — в отчёт «Перенесён или отменён?», не удаляется.
9. **Один сбой не валит запуск.** Ошибка изолируется в объект и уходит в отчёт; остальные обрабатываются. Отказ YouTube обрабатывается по причине (пять поведений: повторить / не повторять этот вызов / не вызывать эту операцию на канале / не обращаться к каналу / не обращаться к YouTube). Создающие вызовы (`liveBroadcasts.insert`, `liveStreams.insert`) с неизвестным исходом **не повторяются вслепую**. Между любыми двумя обращениями к YouTube — не меньше `youtube_pause_seconds`.
10. **Одна папка данных на роль.** `secrets\` — всё секретное и все конфиги; `image\` — превью; `keystreams\` — ключи; `state\` — замок и служебные отметки; `logs\` — лог и отчёт. Всё остальное — в `app\`. Повреждённая память переименовывается в `livecraft.sqlite3.broken-<DD-MM-YYYY_HHMMSS>` и создаётся новая.
11. **Новые зависимости — только с явного согласования.** Разрешённый набор v1: `openai`, `google-api-python-client`, `google-auth`, `google-auth-oauthlib`, `requests`, `cryptography`, `pillow`, `langdetect`, `pycountry`, `tzdata` (база часовых поясов для `zoneinfo`; добавлен решением 10 §14, 21-09-2026) + stdlib (в т.ч. `sqlite3`, `tkinter`, `ctypes`). Сборка — `pyinstaller`, тесты — `pytest` (в приложение не импортируются). `cryptography` **уже стоит** в обоих окружениях (`planers` 50.0.1, `restreamer` 46.0.5) как транзитивная зависимость Google-стека — это не новая зависимость. Внешние бинарники: `tools\yt-dlp.exe`, `tools\deno.exe`. Убрано против restreamer: `aiogram`, `aiohttp` и весь Telegram-стек, `PyYAML`, `python-dotenv`.
12. **Один экземпляр.** Файл-замок `state\livecraft.lock` берётся в `main` **до** настройки логов; успешный захват и освобождение пишутся в `logs\startup.log`; конфликт — русское сообщение в stderr, строка `lock_rejected` и код 1. Застарелый замок (PID мёртв) перезаписывается атомарно: `unlink` → ровно одна повторная попытка `O_EXCL`; проигрыш повтора — **fail-closed**, а не перезапись. В существующий файл замка не писать никогда.

---

## 7. Секреты: сейф и шифрование

### 7.1 Задача

Программа раздаётся вместе с данными Артура: ключом OpenAI, ссылкой на таблицу плана и ссылкой на форму ключей. Получатель должен **уметь запустить** программу (она расшифрует и использует эти значения), но **не должен уметь прочитать** их глазами — открыть файл в блокноте и скопировать ключ или перейти по ссылке руками. При этом любой получатель может заменить их на свои.

### 7.2 Честная граница возможного — читать до проектирования

Если программа расшифровывает секреты **без пароля от пользователя**, значит ключ расшифровки лежит внутри программы, и достаточно упорный пользователь достанет его оттуда: распакует `livecraft.exe` (PyInstaller-архив разбирается открытыми утилитами), прочитает байт-код, поставит перехватчик на `requests` или посмотрит трафик.

**Поэтому шифрование поставочных секретов — это защита от копирования, а не от специалиста.** Она честно решает ровно ту задачу, которую поставил Артур («тупо перейти по ним руками не сможет»), и не решает никакой другой. Не описывать её в README и в интерфейсе как «защищено шифрованием» без оговорки.

Второе, что нужно понимать: **ссылка на таблицу — не система доступа**. Если таблица открыта «всем, у кого есть ссылка», то скрытая ссылка — единственное, что её защищает, и тогда прятать её осмысленно. Если таблица выдана поимённо, ссылка не защищает ничего: получатель без доступа её всё равно не прочитает. Артур выбирает режим доступа к таблице и форме сам; livecraft подстраивается.

Третье: **секреты самого пользователя защищаются по-настоящему** — они не обязаны никуда уезжать, поэтому шифруются ключом, привязанным к его Windows-аккаунту, и копирование файла на другую машину не даёт ничего. Это и есть настоящая, а не декоративная часть схемы.

### 7.3 Устройство

Пакет `app\secretsafe\`. Четыре объекта, ни одной свободной функции с доступом к секрету.

**`SecretValue`** (`value.py`) — обёртка над секретной строкой:
- `reveal() -> str` — единственный способ получить значение; вызывается только в точке применения (заголовок HTTP, параметр API), никогда — ради вывода;
- `masked -> str` — то, что видят люди: `sk-…dc7f`, `docs.google.com/…/edit` → `таблица плана (…f3a1)`;
- `__str__`, `__repr__`, `__format__` возвращают `masked`. Секрет, случайно попавший в `f"{...}"`, в `logging` или в текст исключения, выходит маской. Тест на это — обязательный.

**`Vault`** (`vault.py`) — объект-сейф в памяти: поля по именам (`openai_api_key`, `sheets_id`, `sheets_range`, `key_form_url`), каждое — `SecretValue` либо `None`, плюс происхождение поля (`поставка` / `своё`). Знает, каких полей не хватает для запуска, и сам формирует причину недопуска — так же, как `PlannedBroadcast.admit` формирует свои `AdmissionReason`.

**`VaultStore`** (`store.py`) — чтение и запись файлов сейфа. Два файла, всегда в этом порядке приоритета:

| Файл | Чем зашифрован | Кто пишет | Смысл |
|---|---|---|---|
| `secrets\vault.local.dat` | ключ, привязанный к Windows-аккаунту через DPAPI | настройщик | свои значения пользователя; копирование файла на другую машину бесполезно |
| `secrets\vault.dat` | программный ключ (внутри exe) | сборка Артура | поставочные значения; защита от копирования, не от специалиста |

Поле берётся из локального сейфа, если оно там есть; иначе — из поставочного. Поставочный файл программа **никогда не переписывает**: настройщик пишет только `vault.local.dat`. Сброс к поставке — удаление поля из локального сейфа, а не правка поставочного.

**`VaultCrypto`** (`crypto.py`) — сама криптография, `cryptography` (уже в окружении, инвариант 11):
- шифр — **AES-256-GCM** (`cryptography.hazmat.primitives.ciphers.aead.AESGCM`): аутентифицированный, подмена байта ломает расшифровку, а не даёт мусор;
- ключ поля — `HKDF-SHA256(ikm = ключ сейфа, salt = 16 случайных байт файла, info = b"livecraft.vault.v1")`;
- на каждое поле — свой `nonce` (12 случайных байт) и своя AAD = имя поля + версия формата: перекинуть блоб из поля `sheets_id` в поле `key_form_url` не выйдет;
- формат файла — JSON: `{"version": 1, "salt": base64, "fields": {"<имя>": {"nonce": base64, "ct": base64}}}`; запись — только `paths.write_text_atomically`;
- **ключ сейфа берётся по-разному у двух файлов (решение 9, 20-09-2026).** У `vault.dat` это `ProgramKey` из exe. У `vault.local.dat` это 32 случайных байта, созданные при первой записи и завёрнутые DPAPI; завёрнутый ключ лежит в самом файле пятой записью `"key": base64`. В `vault.dat` записи `"key"` нет и быть не должно; в `vault.local.dat` она обязательна — нет её или не разворачивается DPAPI, файл считается нечитаемым и программа работает на поставочном сейфе, сказав об этом. Ключ разворачивает только та учётная запись Windows, под которой он был создан: копия файла на другой машине или под другим пользователем бесполезна — это и есть настоящая, а не декоративная часть схемы (§7.2, третий абзац). Потеря профиля Windows делает локальный сейф нечитаемым; лечится повторным `--setup`, поставочный сейф при этом не страдает;
- неизвестная версия формата — ошибка с именем файла и код 2, а не тихое игнорирование.

**`ProgramKey`** (`program_key.py`) — ключ поставочного сейфа. В git его нет. `build_release.bat` генерирует модуль с 32 байтами перед вызовом PyInstaller (тем же приёмом, каким `planers` поднимает patch-версию: `python -m app.version --bump`). В dev-режиме модуля нет — ключ берётся из `secrets\program.key` (в `.gitignore`), а если и его нет, поставочный сейф просто не читается и программа работает на локальном. Ключ не «зашит строкой»: собирается из нескольких констант в момент использования и не хранится в переменной дольше вызова — это не криптография, а повышение цены разбора; писать в коде, что это криптография, нельзя.

**`Dpapi`** (`dpapi.py`) — `CryptProtectData` / `CryptUnprotectData` из `crypt32.dll` через `ctypes` (stdlib, ноль зависимостей), флаг `CRYPTPROTECT_LOCAL_MACHINE` **не** ставится — привязка к пользователю, а не к машине. Описание (`szDataDescr`) — `"Livecraft local vault"`. Не Windows или DPAPI недоступен — локальный сейф не создаётся, настройщик говорит об этом прямо и предлагает работать на поставочных значениях.

### 7.4 Дисциплина обращения — обязательна к соблюдению

- Расшифрованное значение живёт **только в `SecretValue`** и только в памяти. Ни временных файлов, ни переменных окружения, ни `state\`, ни кеша.
- `reveal()` вызывается в трёх местах и нигде больше: заголовок `Authorization` клиента OpenAI, параметр `spreadsheetId` клиента Sheets, базовый URL клиента формы. Каждый новый вызов `reveal()` — строкой-обоснованием в отчёте задачи.
- **Логи, консоль, отчёт и `keys.txt` не содержат ни ключа, ни ссылок.** Там, где `planers` пишет `form_url=…` (`form_ready`, `slot_admitted`, `form_dates_checked`), livecraft пишет ярлык и короткий отпечаток: `form=key-form(9c2b)`. Отпечаток — первые 4 hex sha256 от значения, достаточно, чтобы различить две формы в одном запуске, и бесполезно для восстановления.
- В `observability\logging_setup.py` ставится `logging.Filter`, который вычёркивает известные секреты из готовой строки лога — последний рубеж на случай чужой библиотеки (`googleapiclient`, `openai`, `urllib3` пишут URL в DEBUG). Фильтр — страховка, а не разрешение писать секреты.
- Тексты исключений секретов не содержат. Ошибка «таблица не открылась» называет ярлык и код ответа, а не URL.
- Тесты: секрет в `f"{secret}"`, в `logging.info("%s", secret)`, в `repr(obj)` и в трассировке даёт маску; сейф с подменённым байтом не расшифровывается; поле из локального сейфа перекрывает поставочное; поставочный файл после работы настройщика не изменился (сверка по байтам).

### 7.5 Что шифруется

| Значение | Где сейчас | В сейф |
|---|---|---|
| ключ OpenAI | `GPT_API_KEY` в `secrets\.env` | **да** |
| id Google-таблицы | `GOOGLE_SHEETS_ID` в `secrets\.env` | **да** |
| URL формы ключей | `app.google.form_url` в `app_config.yaml` | **да** |
| диапазон таблицы | `app.google.sheets_range` | да (дёшево и заодно не подсказывает структуру) |
| `client_secret.json` | файл рядом с exe | нет: формат задан Google, читается его библиотекой. Ограничение — по §7.2 честно называется в README |
| токены каналов, `sheets.token.json` | `secrets\<handle>.token.json` | нет: они и так выдаются на конкретный аккаунт и без него бесполезны |
| названия вопросов и варианты формы | `app_config.yaml` | нет: это не секрет, а контракт формы |
| каналы (`channels.json`) | конфиг | нет: ник канала публичен |

---

## 8. Графический настройщик

### 8.1 Решение: Tkinter, окно в самой программе. Браузерного интерфейса не делаем.

Артур просил варианты — вот они, с разбором, и вывод, а не меню.

**Вариант A — Tkinter (stdlib).** Выбран.
- Ноль новых зависимостей (инвариант 11). Tcl/Tk входит в официальный установщик python.org, и у Артура стоит `Python 3.13.9` из `C:\Users\vikon\AppData\Local\Programs\Python\Python313` (`pyvenv.cfg` всех трёх окружений). Проверить за секунду: `.\.venv_livecraft\Scripts\python.exe -c "import tkinter; print(tkinter.TkVersion)"`. Если компонент «tcl/tk and IDLE» при установке был снят — переустановить Python с галочкой, других последствий нет.
- Один процесс, никакого сетевого порта, никакого браузера. Секреты не покидают память программы.
- PyInstaller: в `planers\planer.spec` сейчас `excludes=[… "tkinter"]` — в `livecraft.spec` эту строку **не** переносить. Цена — Tcl/Tk в сборке (единицы–десятки МБ в one-folder); на фоне `googleapiclient` и `pillow` это не проблема.
- Минусы честно: вид по умолчанию скучный (лечится `ttk` и аккуратной сеткой), таблица каналов на `ttk.Treeview` делается дольше, чем HTML-таблица, и на HiDPI-экране MSI Pulse нужен `ctypes.windll.shcore.SetProcessDpiAwareness(1)` в самом начале, иначе шрифт будет мыльным.

**Вариант B — локальный веб-интерфейс (`http.server` + `webbrowser`).** Отклонён, и причина не во вкусе.
Форма настройки показывает и принимает **ровно те значения, которые мы шифруем**. В браузере они попадают в адресную строку и историю, в DevTools, в кеш форм и под любое установленное расширение. То есть вариант B открывает наружу именно то, что §7 закрывает. Плюс появляется localhost-порт, который надо связывать только с `127.0.0.1`, закрывать одноразовым токеном в URL, проверять `Origin` и гасить по таймауту, — работа, которая нужна только затем, чтобы вернуться к уровню безопасности, который у варианта A есть даром. Веб-интерфейс был бы оправдан, если бы настройка шла с другой машины; она идёт с той же.

**Вариант C — PySide6 / Qt.** Отклонён: 100+ МБ в сборке, новая тяжёлая зависимость против инварианта 11, ради формы на три вкладки.

### 8.2 Что настройщик делает

Запуск — `livecraft.bat --setup` (тот же exe, отдельный режим). Окно на три вкладки; каждая вкладка — панель-объект, каждое поле — объект со своим правилом проверки.

1. **Ключи и ссылки** — ключ OpenAI, ссылка (или id) таблицы плана, ссылка на форму ключей, диапазон таблицы.
   - Поле, пришедшее из поставочного сейфа, показывается **маской** и кнопкой «заменить своим»: тот, кому программу передали, не увидит чужой ключ и чужую ссылку в интерфейсе (иначе весь §7 обнуляется).
   - Поле, которое пользователь ввёл сам, показывается ему открыто с переключателем «показать/скрыть»: это его значение.
   - «Сбросить к поставке» удаляет поле из локального сейфа.
   - Проверка на месте: ключ OpenAI — форма и пробный запрос по кнопке «проверить»; ссылка таблицы — вытащить id (`/spreadsheets/d/<id>/`) и попробовать чтение первой строки диапазона; ссылка формы — прочитать структуру (`form\discovery.py`) и показать, какие вопросы и варианты нашлись и покрывает ли «Время стрима» ближайшие даты (`KeyForm.date_coverage`). Всё это — вызовы **того же боевого кода**, а не отдельная реализация для настройщика.
2. **Каналы YouTube** — таблица каналов, шесть полей на канал, как в `planers\app\config\loader.py::ChannelConfig`: `platform` (только `youtube`), `account_name` (название как на YouTube), `handle` (ник, ключ канала), `google_account` (почта — подсказка браузеру при входе), `languages` (непустой список кодов, канал может обслуживать несколько), `privacy` (`public` | `unlisted`). Кнопки: «добавить», «удалить», «войти в канал» (вызывает ту же фазу входа `ChannelBook.log_in`, после подтверждения пишет токен и запись паспорта), «проверить все» (эквивалент `--check`).
   Запись — только через `config\loader.py::render_channels_file` и `write_text_atomically`, прежний файл — в `channels.previous.json`. Настройщик **не** пишет свой формат JSON руками.
3. **Настройки запуска** — `min_lead_minutes`, `keep_days`, `auto_start`, `set_thumbnail`, `category_id`, `youtube_pause_seconds`, модель LLM и её параметры (`model`, `fallback_model`, `reasoning_effort`, `service_tier`, `timeout_sec`, `max_output_tokens`), пути шаблонов превью, часовые пояса. Пишется в `secrets\livecraft.json` тем же загрузчиком, который его читает.

Правила настройщика:

- Настройщик **не знает** ни про файлы, ни про шифрование. Он работает с `Vault`, `LivecraftConfig` и `ChannelBook` — теми же объектами, что и пайплайн. Всё, что он умеет делать с диском, делают `VaultStore` и `config\loader.py`.
- Все тексты настройщика — в `app\ui\messages_ru.py`, рядом с текстами консоли и отчёта. В коде — английские идентификаторы.
- Настройщик не запускает пайплайн и ничего не создаёт на YouTube, кроме входа в канал по явной кнопке.
- Нет сейфа и нет обязательных полей — обычный запуск не начинается: код 2 и строка «запустите `livecraft.bat --setup`». Это продолжение правила `planers` «нет конфига → код 2 и точный шаблон в консоли», а не новое поведение.
- Тесты настройщика — на объекты панелей и правила полей (валидация, приоритет сейфов, маскирование, запись через загрузчик), без поднятия окна Tk.

---

## 9. Google: две личности, минимальные скоупы

В livecraft уживаются два разных субъекта авторизации — это надо развести сразу, иначе один токен расползётся на все скоупы.

| Личность | Что делает | Скоуп | Токен |
|---|---|---|---|
| **оператор** | читает план из Google Sheets | `https://www.googleapis.com/auth/spreadsheets.readonly` | `secrets\sheets.token.json` — один на установку |
| **владелец канала** | планирует эфиры на своём канале | `https://www.googleapis.com/auth/youtube` | `secrets\<handle>.token.json` — один на канал |

- Скоупы задаются в `app\google\auth.py` и **больше нигде** — по образцу `planers\app\google\auth.py::YOUTUBE_SCOPE`.
- Из скоупов restreamer (`documents`, `drive`, `spreadsheets.readonly`, `cloud-platform.read-only`) остаётся **только** `spreadsheets.readonly`: Docs и Drive в livecraft нет. Ветка service account не переносится — только OAuth.
- `client_secret.json` — «паспорт программы», OAuth-клиент типа Desktop, один на всех, лежит рядом с exe. Нет файла — код 2, к Google не обращаемся.
- Механика входа — `InstalledAppFlow.run_local_server(port=0)`, `PROMPT = "select_account consent"`, `login_hint` = `google_account`, ожидание не дольше `LOGIN_TIMEOUT_SEC` (600 с), тексты в браузере — русские из `messages_ru`. Токен пишется **только после** подтверждения канала (`keep_login`), иначе чужой токен запоминается навсегда (боевой случай `planers` 17-09-2026).
- Квота YouTube Data API v3 — 10 000 единиц в сутки на проект GCP. Цены — по факту Google Cloud Console за 20-09-2026 и 21-09-2026 (planers, задача 5p; единственный источник — `planers\app\platforms\youtube.py::QUOTA_UNITS`): все чтения (`channels.list`, `liveBroadcasts.list`, `liveStreams.list`, `videos.list`) — 1; `liveBroadcasts.insert`, `.update`, `.bind`, `liveStreams.insert`, `.update` — 1; `videos.update` и `thumbnails.set` — 50. Документация Google даёт 50 за все записи — считаем по факту. Новый эфир с обложкой и настройками видео обходится примерно в 103 единицы.
- Открыто (наследуется от `planers`): свой проект Google Cloud и свой OAuth-клиент; проходить ли верификацию под sensitive-скоуп `auth/youtube` (без неё «приложение не проверено» и потолок 100 пользователей за жизнь проекта).

---

## 10. Режимы запуска и коды выхода

`livecraft.exe` через `livecraft.bat` (окно не закрывается до Enter; bat один на оба случая — рядом exe запускает его, иначе `python -m app.main` из `.venv_livecraft`).

- без аргументов — полный цикл;
- `--setup` — графический настройщик (§8);
- `--dry-run` — читает таблицу, делает merge и сверяется с YouTube (только чтение); ничего не создаёт, в форму не шлёт, `keys.txt` не меняет;
- `--no-llm` — пропустить merge, взять тексты источника как есть (быстрый прогон контура B без расхода токенов);
- `--export-slots <путь>` — выгрузить слоты запуска JSON-ом (§4);
- `--check` — по каждому каналу: вход при необходимости, проверка ник → id → название, языки, число запланированных эфиров;
- `--auth <handle>|all` — принудительный вход (ник в PowerShell **всегда в кавычках**: `--auth "@Osvald.X"`);
- `--status` — сверка эфиров с маркером программы + `keys.txt` и отчёт, без таблицы и LLM;
- `--debug` — подробный лог в терминал; `--version` — `Livecraft <APP_VERSION>`, код 0, ничего не читает.

Коды выхода (решение и причины — одно место, `runner.decide_exit` → `RunExit`): `0` — сделано всё, что можно; `1` — есть ошибки (в том числе неподтверждённая отправка ключа, который должен был уйти, и недопущенные объекты); `2` — ошибка конфигурации, сейфа или авторизации, ничего не делалось; `3` — в таблице нет будущих слотов (к каналам не обращались).

---

## 11. Стандарты кода

- **ООП — §0. Это первый пункт стандартов, а не последний.**
- `from __future__ import annotations` в каждом файле; аннотации `X | None`, lowercase generics; явные типы возвращаемых значений и переменных.
- Имена английские: функции `verb_noun`, булевы `is_` / `has_` / `should_`. Папки во множественном числе, файлы в единственном, первое слово имени файла = домен папки.
- Методы ~30 строк, одна ответственность, guard clauses. Явная обработка ошибок, без «голых» `except`. Единственный перехват `Exception` — в `main.run_cli` (обрыв и падение: трассировка в лог, русская строка в консоль, код 1).
- Никакого хардкода: константы — в конфиг, сейф или enum; единый источник истины для каждой константы. Числа тестов в отчётах не хардкодить — формулировка «новых падений относительно baseline нет».
- Повторы сетевых обращений — только через `app\core\retry.py::RetryPolicy` (первое обращение и до 4 повторов, пауза `2 * 2^(n-1)` с плюс случайная добавка до 1 с, потолок 32 с; `rng` параметром, в тестах фиксированный). Что повторять, решает вызывающий; своих констант повторов в модулях нет.
- Иерархические имена логгеров под `"livecraft"` (`"livecraft.youtube"`, `"livecraft.form"`, `"livecraft.llm"`, `"livecraft.sheets"`, `"livecraft.vault"`); структурированные сообщения `key=value`, имя канала в кавычках (`channel="Osvald.X"`). Имя корневого логгера — константа, не строка по месту.
- Все тексты для людей (консоль, отчёт, `keys.txt`, настройщик) — только в `app\ui\messages_ru.py`. В коде — английские идентификаторы сообщений.
- Перенос модуля: старый модуль остаётся re-export фасадом. Дублированного кода нет.
- Тесты `pytest` в `app\tests\`, файл = `test_<модуль>.py`. Тесты гоняют реальный production-путь (JSON-конфиги и сейф во временном корне), а не переписывают логику inline. Подменяются: платформа (`FakePlatform`), `requests`, yt-dlp (сохранённый JSON), LLM (сохранённые ответы), часы (`run(..., clock=)`).
  Обязательное покрытие: разбор таблицы и группировка в слоты с подменённым «сейчас»; маскирование секретов (§7.4); приоритет локального сейфа над поставочным и неизменность поставочного файла; сверка на фейковой платформе (нет / совпадает / отличается / удалён / два на минуту); `keys.txt` и отчёт по фиксированному состоянию; разбор формы на сохранённом HTML; сопоставление значений (дата есть / нет, `stream_url` с другим регистром и слэшем); регулярка ключа потока (5 и 4 группы — ок, 3 группы и заглавные — ошибка); submitter с подменённым `requests`; граница контуров (§4).
- Переводы строк: репо хранит LF (`.gitattributes: * text=auto eol=lf`), `.bat` / `.ps1` — CRLF. В workspace `"files.eol": "\n"`.
- Окружение одно — `.venv_livecraft` (Python 3.13): приложение, тесты и сборка exe. Других окружений не заводить. `requirements.txt` руками не править — это снимок `pip freeze`, который делает `D:\_projects\manage_project.py`; по нему и контролируется инвариант 11.
- Номер версии — только `app\version.py::APP_VERSION`. `.spec`, `.iss` и скрипты своих констант версии не держат; сборка поднимает patch сама.

---

## 12. Как работать с задачами

- Ветка — `feature/livecraft`. Коммиты без trailer `Co-Authored-By`, если задача его не просит. Push и теги — только по задаче.
- Каждая задача приходит со SCOPE LOCK и списком DO NOT TOUCH. Границы — буквально. Файлы вне области не трогать, даже если «заодно». Замеченное вне области — строкой в отчёт, а не правкой.
- **Все технические вопросы агент решает сам, до конца.** Символ не найден — ищет фактический эквивалент `grep`'ом; тест падает — разбирается и чинит в своей области; неясен вызов — читает код и выбирает решение, совместимое с текущим. Наверх выносятся только три вещи: (а) нужно изменить контракт из этого файла; (б) нужно править файл из DO NOT TOUCH; (в) не решается ни одним из испробованных способов — тогда в отчёте перечень попыток. Каждое самостоятельное решение — строкой в отчёте.
- Перед правкой — `grep` по текущему коду на точные имена символов; не писать по памяти.
- Перед правкой назвать объект и поля, которые меняются. Решение «добавить функцию рядом» вместо поля или метода объекта — только с обоснованием в отчёте (§0).
- Порядок: исправления ошибок → переименования → структурные рефакторинги. Не смешивать виды изменений в одной ветке.
- Проверка каждой задачи: полный `pytest -q` до и после плюс задачные проверки. Боевые прогоны делает Артур, не агент.
- В конце задачи — отчёт: каждый изменённый файл и что в нём изменилось; какие объекты и поля добавлены или изменены.
- Все правки — кода, конфигов и документов (`CLAUDE.md`, `README.md`) — делает агент по промту. Артур ставит задачи, контролирует и проверяет; руками ничего не правит.
- Оболочка под вкладкой Code на десктопе — Git Bash: пути вида `D:/_projects/...` и POSIX-инструменты; cmdlet'ов PowerShell там нет.

Команды (PowerShell, из корня репо):

```powershell
.\.venv_livecraft\Scripts\python.exe -m pytest app\tests -q
.\.venv_livecraft\Scripts\python.exe -c "import app.main"
.\.venv_livecraft\Scripts\python.exe -c "import tkinter; print(tkinter.TkVersion)"
.\.venv_livecraft\Scripts\python.exe -m app.main --setup
.\.venv_livecraft\Scripts\python.exe -m app.main --dry-run
.\.venv_livecraft\Scripts\python.exe -m app.main --check
.\.venv_livecraft\Scripts\python.exe -m app.main --auth "@Osvald.X"
```

---

## 13. Этапы

| Этап | Содержание | Готово, когда |
|---|---|---|
| 0 | Этот файл; скелет `app\`, `paths.py`, `version.py`, `logging_setup.py`, `messages_ru.py`, `main.py` с кодами выхода | `python -m app.main --version` работает, `pytest` зелёный на пустом наборе |
| 1 | **Сейф**: `app\secretsafe\` целиком, тесты маскирования и приоритета сейфов, `config\loader.py` (два JSON без умолчаний) | секрет не вытекает ни в один вывод (тесты §7.4); поставочный сейф читается, локальный перекрывает |
| 2 | **Настройщик**: `app\setup\`, три вкладки, запись через `VaultStore` и `render_channels_file` | чистая установка настраивается с нуля через окно, без правки файлов руками |
| 3 | **Контур A**: `sheets\`, `sources\`, `slots\`, перенос `llm\` и `texts\` из restreamer в ООП-форме; `--export-slots`, `--no-llm` | на боевой таблице `--export-slots` даёт слоты с корректными `slot_id`, текстами и превью |
| 4 | **Контур B**: перенос `platforms\`, `form\`, `records\`, `pipeline\`, `output\`, `google\auth.py` из planers; `--dry-run`, `--status`, `--check`, `--auth` | на тестовом канале: эфир создан, ключ в `keys.txt` и в тренировочной форме; повторный запуск — «уже стояло»; изменённое описание — «исправлено» |
| 5 | Полный цикл одной командой, отчёт и консоль по шести блокам, чистка старья, замок одного экземпляра | прогон от таблицы до строки в таблице ответов формы без ручных шагов |
| 6 | Сборка: `livecraft.spec` (без `excludes` tkinter), `livecraft.iss`, `build_release.bat` / `build_local.bat`, генерация `program_key.py`, README | получатель без Python проходит сценарий по инструкции и может подставить свои ключи через `--setup` |

---

## 14. Решения

Принято при составлении этого файла (20-09-2026) по постановке Артура:

1. **Одна программа, один процесс.** Пакет `*.bcast` как файл пересылки упраздняется; его **схема** сохраняется как контракт объекта `StreamSlot` (§4). Отладочная выгрузка — `--export-slots`.
2. **Контур A берётся из restreamer, контур B — из planers** (§2). Контур B переносится почти без правок: он отлажен живыми прогонами; переписывать его «покрасивее» нельзя.
3. **Анонсы в Google Docs и Telegram в v1 не делаются.** Постановка Артура: «вместо того, чтобы делать объявления сразу будет идти на ютуб каналы и планировать стримы». Тексты merge уходят прямо в название и описание эфира. Если анонсы понадобятся — это отдельный этап после v1, а не встроенная опция; проектировать «на будущее» под них сейчас не надо.
4. **Конфиги — JSON без умолчаний** (как в planers, задача 5b), не YAML. PyYAML в зависимости не тянем.
5. **Шифруются: ключ OpenAI, id таблицы, диапазон, URL формы.** Токены, `client_secret.json`, названия вопросов формы и каналы — нет (§7.5).
6. **Шифрование поставочных секретов — защита от копирования, а не от специалиста** (§7.2). Формулировка в README и в настройщике обязана это отражать.
7. **Настройщик — Tkinter, не браузер** (§8.1): браузерная форма показывает и сохраняет ровно те значения, которые §7 прячет.
8. **Два субъекта Google-авторизации с раздельными токенами и минимальными скоупами** (§9).
9. **Ключ локального сейфа лежит в самом `vault.local.dat`** — 32 случайных байта, завёрнутые DPAPI, записью `"key"` рядом с `salt` и `fields` (§7.3). Принято Артуром 20-09-2026. Отклонено: отдельный файл `secrets\vault.local.key` — он не описан в §5 и два файла можно рассинхронизировать, а потерянный ключ превращает второй файл в мусор без внятной ошибки. К настройке на машине пользователя решение ничего не добавляет: DPAPI — штатная часть Windows, пароля не спрашивает и настройки не требует.
10. **`tzdata` входит в разрешённый набор инварианта 11.** Принято Артуром 21-09-2026. Причина: на Windows у Python нет своей базы часовых поясов, а контур A обязан переводить дату и время ряда таблицы по Киеву в `start` слота с правильным смещением, в том числе на стыке летнего и зимнего времени. restreamer в боевой работе опирался на тот же пакет (`tzdata==2025.3`). Отклонено: зашить в код правило перехода «последнее воскресенье марта и октября», как planers поступил с квотными сутками, — правило перехода в Украине может поменяться законом, пакет обновляется вслед за ним, а зашитый код — нет, и две недели в году эфиры встали бы на час мимо.

---

## 15. Открытые проверки

Каждая закрывается экспериментом до реализации соответствующего модуля; результат — строкой сюда.

- **Закрыто 20-09-2026:** `tkinter` в `.venv_livecraft` есть, `tkinter.TkVersion` = 8.6. Решение §8.1 (настройщик на Tkinter) выполнимо без переустановки Python.
- **[ПРОВЕРИТЬ]** Вес one-folder сборки с Tcl/Tk против сборки без него (снять `"tkinter"` из `excludes` и сравнить `dist\`). Если рост окажется неприемлемым — вынести настройщик отдельным маленьким exe из того же кода, а не менять решение §8.1 на браузер.
- **Закрыто наполовину 20-09-2026:** DPAPI через `ctypes` на Windows 11 Home (build 26200) работает — `CryptProtectData` без `CRYPTPROTECT_LOCAL_MACHINE` дал блоб 288 байт из 41 байта входа, исходных байтов в блобе нет, `CryptUnprotectData` тем же пользователем вернул те же байты, `szDataDescr` прочиталось как `Livecraft local vault`. **Остаётся открытым:** отказ под другим пользователем Windows — нужна вторая учётная запись, проверяет Артур.
- **Закрыто 20-09-2026 решением Артура:** ключ локального сейфа лежит записью `"key"` в самом `vault.local.dat` (§7.3, §14 решение 9).
- **[ПРОВЕРИТЬ]** Режим доступа к боевой таблице и боевой форме (открыты по ссылке или выданы поимённо) — от этого зависит, что именно даёт шифрование ссылок (§7.2) и какая формулировка пойдёт в README.
- **[ПРОВЕРИТЬ]** Что у слота с несколькими источниками уходит в `sources` и какое превью выбирается, когда источники разного качества, — правило выбора сегодня «одно случайное» (`planers`, решение 8); для эфира, возможно, нужно «первое по времени».
- **[ПРОВЕРИТЬ]** Лимиты YouTube на название (100) и описание (5000) против того, что выдаёт merge на контракте `expanded`: у анонса лимитов не было, обрезка была косметической, у эфира она обязательна.
- **Закрыто 21-09-2026 фактами planers (задача 5p):** стоимости вызовов YouTube Data API — в §9.
- **Закрыто 21-09-2026:** `tzdata` добавлен в инвариант 11 (§14 решение 10) и стоит в `.venv_livecraft` (`tzdata==2026.4`, база IANA `2026d`); `LivecraftSettings` проверяет зону и отдаёт её свойством `zone` (задача 1.6). Проверено: Киев 15-01-2027 12:00 — `+02:00`, 15-07-2027 12:00 — `+03:00`.
- **[ПРОВЕРИТЬ на этапе 6]** База часовых поясов попадает в сборку PyInstaller: собранный `livecraft.exe` на машине без Python читает `ZoneInfo("Europe/Kyiv")`.
- Наследуются от planers и остаются открытыми: свой проект Google Cloud и свой OAuth-клиент; верификация приложения под `auth/youtube`; формат `snippet.customUrl` в живом прогоне; запасной путь «отмена + пересоздание», если для какого-то поля `liveBroadcasts.update` окажется невозможен.

---

## 16. Текущее состояние

- Исходные репозитории на дату разбора: `restreamer` — коммит `3f10426` (17-09-2026), ветка разработки `feature/restreamer`, baseline тестов 700; `broadcaster` — `efcbb3e` (09-09-2026), сборочный чекаут; `planers` — `9af58a6` (19-09-2026), ветка `feature/planers`, этап 5 в работе.
- **Этап 0 — задача 0.1 принята 20-09-2026, коммит `af998b3` в `feature/livecraft`.** Есть: `.gitattributes`, `livecraft.bat`, `requirements.txt` снимком заполненного окружения (все пакеты инварианта 11 плюс `pytest` и `pyinstaller`), `app\version.py` (`APP_VERSION = "0.1.0"`), `app\paths.py` (`LivecraftPaths` — все файлы и папки §5 и §7.3, `resolve_root` по `LIVECRAFT_ROOT`, `write_text_atomically`), `app\core\dates.py`, `app\observability\logging_setup.py` (логгер `livecraft`, файл `{stamp}_livecraft.log`, `mask_stream_key`), `app\ui\messages_ru.py`, `app\main.py` (`ExitCode`, `RunRequest`, разбор всех флагов §10). Baseline тестов livecraft — **101**.
- Пока нет сейфа и загрузчика конфига, любой режим, кроме `--version`, печатает `SETUP_REQUIRED` и возвращает код 2 (`app\main.py::_run`). Настоящую проверку сейфа и конфига эта ветка получает в задаче 1.3.
- `ExitCode` временно живёт в `app\main.py`; переезжает в `app\pipeline\runner.py` вместе с `decide_exit` и `RunExit` на этапе 4.
- **Этап 0 закрыт. Задача 0.2 принята 20-09-2026, коммит `964693e`.** Добавлен пакет `app\runtime\`: `single_instance.py` с объектами `LockOwner` (запись владельца, правила `parse` / `render` / `is_known` / `is_alive`) и `InstanceLock` (поля `path`, `startup_log`, `pid`; `acquire` / `release`), исключение `AnotherInstanceRunning`. Замок берётся в `run_cli` после `ensure_dirs` и до `setup_logging`, снимается в `finally` после `close_logging`; конфликт — `LOCK_REJECTED` в stderr, событие `lock_rejected` в `logs\startup.log`, код 1. Долг §11 из задачи 0.1 закрыт: подписи значений справки переехали в `messages_ru` (`CLI_METAVAR_PATH`, `CLI_METAVAR_HANDLE`). Baseline тестов livecraft — **137**.
- Решения по замку, принятые при реализации и проверенные тестами: время в файле замка и в `startup.log` — в формате инварианта 4, а не ISO донора; однотокенная запись владельца считается нечитаемой и снимается как застарелая; `PermissionError` при `os.open(O_EXCL)` трактуется как «путь занят» и ведёт к отказу запуска (fail-closed), причина уходит в `startup.log` строкой `lock_create_denied`.
- Известные мелкие долги (чинятся, когда задача откроет соответствующий файл): отметка времени в `logs\startup.log` — с точностью до минуты, потому что в `app\core\dates.py` нет формата с секундами; на путях сбоя замка `LOGGER.warning` срабатывает до настройки логов и уходит английской строкой в stderr через `logging.lastResort`; фикстуры `dead_pid` и `live_foreign_process` продублированы в `app\tests\test_main.py` и `app\tests\test_runtime_single_instance.py` вместо `conftest.py`.
- **Задача 1.1 принята 20-09-2026, коммит `f6bd784`.** Пакет `app\secretsafe\`: `value.py` (`SecretField` с ярлыками и стилем маски, `MaskStyle`, `SecretValue` с `reveal` / `masked` / `fingerprint` / `log_label` и переопределёнными `__str__`, `__repr__`, `__format__`), `crypto.py` (`EncryptedField`, `VaultFile`, `VaultCrypto`, `VaultFormatError`, `VaultDecryptError`; AES-256-GCM, HKDF-SHA256, AAD = имя поля плюс версия формата), `dpapi.py` (`Dpapi`, `DpapiUnavailable`; `CRYPTPROTECT_LOCAL_MACHINE` не ставится). Baseline тестов livecraft — **287**.
- Проверено на приёмке: слово `reveal` встречается в `app\secretsafe\` ровно один раз — в объявлении метода; секрет даёт маску в `f"{...}"`, `f"{secret:>40}"`, `logging`, `repr` объекта-владельца, трассировке и в записи чужого логгера (`googleapiclient.discovery`); блоб чужого поля не расшифровывается; `"version": 1.0` и `"version": true` отвергаются как чужой формат.
- **Задача 1.2 принята 20-09-2026, коммит `ca614f5`.** `app\secretsafe\vault.py`: `VaultOrigin` (`supplied` / `own`), `VaultEntry`, `Vault` (`missing`, `is_ready`, `admission_reason`, `secrets`, `with_field`, `without_field`, `log_line`) — объект неизменяемый, файлов не знает. `SecretValue.scrub(text)` вычёркивает своё значение из готовой строки, подставляя `log_label` — значение при этом не покидает объект. В `app\observability\logging_setup.py` появились `SecretScrubber(logging.Filter)` и `install_secret_filter(vault)`. Baseline тестов livecraft — **342**.
- Фильтр чистит готовый текст записи и обнуляет `record.args`; при несошедшихся шаблоне и аргументах чистит `msg` и `args` по отдельности, потому что их печатает в stderr `logging.Handler.handleError`. Обработчики собираются со сверкой по идентичности: `_attach_third_party` вешает на корневой логгер те же объекты, и без сверки фильтр висел бы дважды.
- `install_secret_filter` пока никто не зовёт: сейф открывается в `main.run_cli` вместе с загрузчиком конфига (задача 1.4).
- Известный долг слоёв: `app\observability\logging_setup.py` импортирует `SecretValue` и `Vault` в рантайме, а `app\secretsafe\dpapi.py` импортирует оттуда `get_logger`. Кольца сегодня нет (`value.py` и `vault.py` логгер не трогают), но оно появится, как только тронут. Чинится в задаче 1.3 переносом обоих импортов под `TYPE_CHECKING`.
- **Задача 1.3 принята 20-09-2026, коммит `05b352e`.** `app\secretsafe\store.py`: `VaultSource`, `ProgramKey` (поле `material`; сначала сгенерированный сборкой модуль `app.secretsafe.program_key` с `PARTS: tuple[bytes, ...]`, затем `secrets\program.key` сырыми байтами или base64), `VaultStore` (поля `supplied_path`, `local_path`, `program_key`, `dpapi`; `load`, `save_local`). `VaultFile` получил поле `wrapped_key` и запись `"key"`. Импорты сейфа в `logging_setup.py` уехали под `TYPE_CHECKING` — долг слоёв закрыт; дублированные тестовые фикстуры сведены в `conftest.py` — долг закрыт. Baseline тестов livecraft — **406**.
- Правила чтения, принятые при реализации: любое поле, которое не расшифровалось, делает файл нечитаемым целиком (половина полей из явно испорченного файла — это работа на непонятно чьих значениях); чужой формат файла (версия, битый base64) уходит `VaultFormatError` наружу и по §7.3 станет кодом 2; отсутствие ключа, отказ DPAPI и неизвестные имена полей запуск не валят; битый сгенерированный модуль ключа равнозначен его отсутствию и откатывается к `secrets\program.key`.
- **Задача 1.3a принята 20-09-2026, коммит `82d62e8`.** Долг §7.4 закрыт: шифрование поля стало правилом самого секрета (`SecretValue.encrypt(crypto)`, типы `VaultCrypto` и `EncryptedField` под `TYPE_CHECKING`), значение объекта не покидает, `Select-String reveal` по `app\secretsafe\` снова даёт **одно** совпадение — так и должно оставаться до появления трёх точек применения §7.4. Заодно привязка блоба к месту (AAD) теперь берётся из самого секрета, а не из аргумента вызывающего. Baseline тестов livecraft — **427**.
- **Требование к задаче связки с `main`:** когда локальный сейф не читается, оператор должен получить явную русскую строку о том, что его личные настройки не прочитаны и программа работает на поставочных значениях. Молчаливый откат недопустим: иначе получатель незаметно работает на ключе OpenAI Артура и на его таблице.
- **Задача 1.4 принята 21-09-2026, коммит `bf8150d`.** `app\config\loader.py`: `Platform` (только `youtube`), `Privacy`, `ReasoningEffort`, `ServiceTier`, `ConfigProblem`, `ConfigError`, `SettingProblem`, `ChannelConfig`, `LlmSettings`, `FormSettings` (девять ключей вопросов, варианты, формат даты; своё правило `problem`), `LivecraftSettings` (десять полей, `youtube_pause_seconds: float`), `LivecraftConfig` (`served_languages`, `channel_by_handle`, `auth_targets`); `load_livecraft_config`, `render_channels_file`, `save_channels_file`. В git — `secrets\livecraft.json` (без секретов) и `app\examples\channels.example.json`. Загрузчик сейф не импортирует. Baseline тестов livecraft — **600**.
- Решения задачи 1.4: `youtube_pause_seconds` в поставке **0.5**, а не 2 — так в текущем `planers\secrets\planer.json` (planers 5p, решение 21-09-2026 по замерам); `save_channels_file` копирует прежний файл в `channels.previous.json`, только если он есть (донор упал бы на чистой установке); шаблон превью — относительный, только с `{date}` и `{language}`; шаблон настроек в `messages_ru` совпадает с поставочным файлом — это держит тест.
- Долги после 1.4: `normalize_handle` временно живёт в `app\config\loader.py` (её дом по §5 — `core\text.py`, переедет с `token_file_stem` в контуре B); `AUTH_ALL` объявлен дважды — в `main.py` и в `loader.py` (§11: один источник; чинится задачей 1.5); часовой пояс не проверяется на читаемость — ждёт решения по `tzdata` (§15).
- **planers ушёл вперёд после разбора:** HEAD `9903b2e` (21-09-2026) против `9af58a6` на дату составления этого файла — шесть коммитов (5o-A, 5o-B, 5p и две сборки). Появились `app\observability\run_stats.py`, `app\records\run_record.py` (таблица `runs`, `schema_version` 2), дробная пауза, цены квоты по факту, `core\dates.py::format_datetime_seconds`. planers развивается параллельно: перед каждой задачей этапа 4 его исходники перечитываются по текущему HEAD, а не по этому файлу. `format_datetime_seconds` закрывает долг «`startup.log` с точностью до минуты» — переносится с `core\dates.py` в задаче 3.1.
- **Задача 1.5 принята 21-09-2026, коммит `5480afd`.** `app\setup\readiness.py::Readiness` (поля `paths`, `config`, `config_error`, `vault_load`, `vault_error`; `check`, `is_ready`, `problems`, `template_lines`, `warnings`, `summary_lines`, `log_line`) — объект готовности: читает сейф и оба конфига, сам отвечает, готово ли и что сказать, ничего не печатает. В `store.py`: `LocalVaultState` (`absent` / `read` / `unreadable`), `VaultLoad`, `VaultStore.open(paths)`, имя файла в `VaultFormatError`. `main._run` стал настоящим: не готово — проблемы, шаблон сломанного конфига и `SETUP_REQUIRED`, код 2; готово — сводка без значений, код 0; `--setup` не останавливается проверкой и показывает сводку. `install_secret_filter` ставится сразу после чтения сейфа — проверено тестом, который падает без этой строки. Нечитаемый личный сейф — строка «ВНИМАНИЕ» в консоли и `vault_local_unreadable` в логе. `AUTH_ALL` — один источник, `app\config\loader.py`. Baseline тестов livecraft — **639**.
- `load_livecraft_config` читает `channels.json` раньше `livecraft.json` (записанное решение загрузчика: ошибка вероятнее в том, что заполняет человек), поэтому на пустом корне первым называется `channels.json`. На настоящей установке `livecraft.json` приходит со сборкой.
- **Дефект, найденный на приёмке 1.5:** `VaultStore._read_file` отвечает «файла нет» не только на `FileNotFoundError`, но и на любую другую `OSError` и на `UnicodeDecodeError`; `_read_local` превращает это в `LocalVaultState.ABSENT`. Существующий, но нечитаемый `vault.local.dat` (блокировка антивирусом или синхронизацией, чужие байты) даёт молчаливый откат на поставочные значения — ровно то, что запрещает требование выше. Чинится задачей 1.5a: повреждённый или не открывающийся файл сейфа — `VaultFormatError` с именем файла и код 2, как любой другой повреждённый файл сейфа; «целый, но не наш» (DPAPI не развернул) остаётся предупреждением.
- Мелкий долг для этапа 2: на консоли шаблон сломанного конфига печатается после всех проблем, и между ошибкой `channels.json` и её шаблоном встаёт причина от сейфа; читалось бы лучше, если бы шаблон шёл сразу за своей ошибкой.
- **Задача 1.5a принята 21-09-2026, коммит `8f97fc4`.** Дефект закрыт: `VaultStore._read_file` возвращает `None` только при `FileNotFoundError`; любая другая `OSError` и `UnicodeDecodeError` — `VaultFormatError` с именем файла и короткой причиной без пути, исходная ошибка через `from`, дальше `Readiness.vault_error` и код 2. «Целый, но чужой» личный файл (нет `"key"`, DPAPI отказал, блоб не расшифровался) по-прежнему `LocalVaultState.UNREADABLE`, предупреждение и работа на поставке. `VAULT_NOT_READY` больше не советует `--setup` — совет один, `SETUP_REQUIRED` из `main`. Новые тесты проверены на старом коде: все 7 падают. Baseline тестов livecraft — **647**.
- Мелкий долг dev-режима: `ProgramKey._from_dev_file` на любую `OSError`, кроме «нет файла», тоже отвечает «ключа нет» — поставочный сейф не читается, а причина названа как «не хватает полей». В сборке ключ приходит из сгенерированного модуля, поэтому на получателя не влияет; чинится, когда задача откроет `store.py`.
- **Задача 1.6 принята 21-09-2026, коммит `fb47dca`. Этап 1 закрыт.** `requirements.txt` — одна новая строка `tzdata==2026.4`, LF. `LivecraftSettings.zone -> ZoneInfo` — зону программы контур A берёт у настроек, а не строит сам; `LivecraftSettings.problem` отдаёт первую из двух проблем (`_image_template_problem`, `_timezone_problem`). Проверка зоны двойная: `ZoneInfo` перехватывается по `ZoneInfoNotFoundError` и `ValueError`, а затем имя сверяется с `available_timezones()` — на Windows `ZoneInfo` открывает и `Europe\Kyiv`, и `Europe/Kyiv ` с пробелом, потому что файловая система прощает неканонические имена. Baseline тестов livecraft — **661**.
- **Итог этапа 1:** программа при каждом запуске читает оба файла сейфа и оба конфига, называет точно, чего не хватает (с шаблоном сломанного конфига), громко предупреждает о нечитаемом личном сейфе, останавливается с кодом 2 на повреждённом файле сейфа и ставит фильтр секретов в логах сразу после чтения сейфа. Значение секрета не покидает `SecretValue`: `reveal` в `app\secretsafe\` объявлен и ни разу не вызван.
- **Нарезка этапа 2 (настройщик, §8):** 2.1 — модель вкладки «Ключи и ссылки» без окна (поля сейфа: происхождение, маска, «заменить своим», «сбросить к поставке», проверка формата, запись через `VaultStore.save_local`); 2.2 — модели вкладок «Каналы» и «Настройки запуска» без окна плюс запись `livecraft.json` тем же загрузчиком (`render_settings_file`); 2.3 — окно Tk поверх трёх моделей и запуск по `--setup`; 2.4 — живые проверки по кнопкам («проверить ключ / таблицу / форму», «войти в канал», «проверить все») — после этапа 4, когда появятся клиенты. Модели не импортируют `tkinter` и тестируются без окна (§8.2, последнее правило).
- Этап 2 — задача 2.1 не начата.
