# CLAUDE.md — livecraft

Инструкции для агента (Claude Code / Cowork / Codex), работающего в `D:\_projects\livecraft`.
Этот файл — единственный документ требований и правил проекта. Расхождение кода с этим файлом — доложить, не решать молча.

Составлен 20-09-2026 по разбору трёх рабочих репозиториев: `D:\_projects\restreamer` (коммит `3f10426`, 17-09-2026), `D:\_projects\broadcaster` (коммит `efcbb3e`, 09-09-2026), `D:\_projects\planers` (коммит `9af58a6`, 19-09-2026). Всё, что ниже названо именем файла или символа, проверено по исходникам этих репо на дату разбора.

---

## Предназначение: работа вместо человека

**Смысл существования любой нейросети, программы или скрипта — облегчать жизнь человеку.** Они созданы, чтобы работать. Внимание человека бесценно. Поэтому им категорически запрещено своими действиями добавлять человеку работу или отвлекать его.

Это правило стоит выше любого другого удобства. Оно относится ко всем, кто участвует в livecraft: к самой программе, к Codex и к Коворку.

**Что это значит на деле**

1. **Делать самому до конца.** Получив задачу, исполнитель прикладывает все усилия, чтобы выполнить её самостоятельно, не отвлекая человека. Вопрос человеку — последнее средство, когда исчерпаны все собственные возможности, а не первый шаг.
2. **Улучшать без спроса.** Если исполнитель своими средствами может сделать результат лучше, он обязан это сделать без вопросов и уточнений. Условие одно: не нарушать инварианты и границы задачи. Улучшение за пределами своей задачи не делается молча и не превращается в вопрос. Оно становится готовым следующим шагом: исполнитель приносит его сделанным или готовым к исполнению.
3. **Не перекладывать ответственность.** Запрещено перекладывать на человека ответственность за свою работу: проверку, контроль, исправление, слежение за тем, что исполнитель должен обеспечить сам. Если программа может что-то определить, проверить или исправить сама, она это делает, а не просит человека.
4. **Не создавать работы.** Запрещено любое действие, после которого у человека работы становится больше или его внимание отвлекается без необходимости. Это лишние вопросы, выбор из вариантов вместо готового решения, ручные шаги, которые мог сделать исполнитель, и данные, за которыми человеку придётся следить.
5. **Обязательная самопроверка.** Исполнитель обязан самостоятельно проверять результат своей работы и принятые в процессе решения до передачи результата человеку. Проверка должна быть направлена не только на формальное завершение задачи, но и на выявление ошибок, пропусков, противоречий, нарушений инвариантов, побочных эффектов и несоответствия исходной задаче. При выявлении проблемы исполнитель обязан самостоятельно исправить её всеми доступными способами, не перекладывая исправление на человека. После каждого исправления самопроверка выполняется повторно. Цикл «сделал → проверил → обнаружил ошибку → исправил → проверил повторно» продолжается до получения устойчивого результата либо до объективного исчерпания доступных средств. Человеку передаётся уже проверенный результат, а не результат, требующий от него поиска и исправления ошибок.
6. **Человеку остаётся только то, что может сделать лишь человек:** решения, отнесённые к нему этим файлом, вход в свои аккаунты, боевые прогоны на живых каналах, приёмка итога. Всё остальное делают программа и агенты.

**Как это видно в результате**

Каждый результат — отчёт задачи, итог приёмки, отчёт запуска программы — кратко показывает:

- **что исполнитель улучшил сам**, сверх буквы задачи, и зачем;
- **что не получилось**, почему так сложилось и в чём причина. Исполнитель берёт ответственность на себя и не ссылается на обстоятельства или на человека.

Человек должен видеть подтверждение пользы: его работы становится меньше, а работы, которую взяли на себя программа и агенты, — больше. Если этого не видно, работа сделана не до конца.

**Для программы livecraft** это значит следующее:
- программа сама определяет, проверяет и исправляет всё, что может: язык источника, расхождения с YouTube, сменившиеся ники каналов, устаревшие бинарники;
- оператор получает готовый итог, а не список дел;
- всё, что программа исправила сама, она называет в отчёте запуска;
- всё, что человек обязан сделать, программа называет одной строкой с точным действием.

Принято 24-09-2026.

---

## 0. Главное правило: код пишется в стиле ООП

**Весь код livecraft пишется объектно-ориентированно. Это не рекомендация, а условие приёмки любой задачи.**

- Данные живут в объектах предметной области: Таблица, Ряд, Видео, Слот, Канал, Форма, Сейф, Эфир. Функции заполняют, читают и сравнивают поля объекта; собственных данных у функций нет.
- Правило, которое переводит значение одного объекта в значение другого (ряд таблицы → слот, слот → ответ формы, ответ YouTube → канал, поле настройки → запись сейфа), — **метод объекта-владельца правила**, а не функция по месту использования.
- Новая проверка = новое поле объекта и правило внутри самого объекта. Не новая функция рядом с вызовом.
- Изменилась форма, таблица, площадка или набор настроек — правится структура **одного** объекта, а не цепочка функций.
- Объект с незаполненными полями не выбрасывается и не «чинится» молча: причина — подробно в лог, кратко в терминал; дальше по конвейеру объект не допускается (см. §6 «Допуск»).
- Классы вместо функций с длинными списками аргументов: запрос/конфиг-объект вместо семи именованных параметров.
- Модуль-функция вне объекта допустима **только** как чистое преобразование без знания о предметных объектах (`normalize_handle`, `safe_trim`, `parse_sheet_datetime`). Любое другое исключение — строкой-обоснованием в отчёте задачи.
- В отчёте каждой задачи: какие объекты и какие их поля добавлены или изменены.

Образец, на который надо равняться, — `D:\_projects\planers\app\pipeline\plan.py::PlannedBroadcast`: один объект несёт слой из пакета, слой с площадки и слой результата, сам решает допуск (`admit`), сам решает отправку ключа (`decide_key_delivery`), сам строит свою запись памяти (`to_record`). Именно так, а не «функция, принимающая девять аргументов».

Антиобразец, который переносить в livecraft **нельзя**, — процедурный слой restreamer: `app\planning\batch_planner.py` (`build_prepared_videos`, `materialize_prepared_previews`, `derive_planned_videos` — три функции на 474 строки, состояние в кортежах), `app\llm\merges\merge_service.py` (свободные функции `attempt_openai_merge_with_audit`, `enforce_openai_merged_paragraphs`). Переносим оттуда **поведение и правила**, а не форму кода: каждая такая функция в livecraft становится методом объекта (`SheetPlan`, `VideoPreparation`, `MergeJob`).

---

## 1. Что это

**Livecraft** — от *live* (прямой эфир) и *craft* (создавать, мастерить): подготовка эфиров «под ключ» на одной машине, одной программой.

Сейчас работа разорвана на две программы и ручную пересылку:

| Сегодня | Программа | Делает |
|---|---|---|
| машина 1 | `restreamer` / `broadcaster` | Google Sheets → yt-dlp → LLM-merge → анонсы в Google Docs и Telegram → пакет `plan_*.bcast` документом в Telegram |
| руками | оператор | пересылает пакет владельцу канала, тот кладёт его в `bcast\` |
| машина 2 | `planers` | пакет → сверка с YouTube → эфиры → ключи → `keys.txt` → Google-форма → отчёт |

Livecraft заменяет все три строки одной программой (§14 решения 13, 14, 17, 18). Режимов два.

**Режим А — «из таблицы».** Слоты собираются в памяти, пакет сохраняется на всякий случай, дальше — выбранный вариант, всё из памяти:

```
Google Sheets (план стримов)
   → ряды: ссылка, дата, время
   → язык источника, метаданные и превью (yt-dlp)
   → LLM-merge: одно название + одно описание на слот (функция merge)
   → объекты-слоты StreamSlot в памяти
   → пакет plan_*.bcast в bcast\ (всегда; в режиме А обратно не читается)
   → вариант «объявления»: Google Doc + анонсы и пакет в Telegram (из памяти)
   → вариант «эфиры»: YouTube — сверка, эфир, поток, ключ, обложка → Google-форма ключей → keys.txt
   → вариант «всё»: объявления, затем эфиры
   → консоль, отчёт, лог
```

**Режим Б — «из пакетов».** Таблица и нейросеть не нужны: пакеты `*.bcast` из `bcast\` → карта слотов (правила `planers`) → те же `StreamSlot` → эфиры, как в варианте «эфиры». Так работает владелец канала, которому прислали пакет, и так же Артур восстанавливает удалённые руками эфиры без нового merge — сколько угодно раз и почти бесплатно.

Livecraft **не**: стримит; не ведёт расписание (источник — Google Sheets, её ведёт оператор); не обходит Google-форму; не трогает эфиры, не соответствующие ни одному слоту; не принимает команд в Telegram — только отправляет; не хранит логины и пароли площадок.

Площадки v1 — только YouTube. Facebook и Rumble — за пределами v1.

---

## 2. Что откуда берётся

Два контура, шов между ними — объект-слот. Ничего не переписывается «с нуля», где есть проверенный боем код, но всё переносимое приводится к ООП (§0).

### Контур A — «Замысел»: из `restreamer` (`D:\_projects\restreamer\app\`)

| Откуда | Что даёт | Как переносить |
|---|---|---|
| `google\sheets_client.py` (`read_rows`), `planning\sheet_loader.py::load_sheet_state`, `BatchSheetState` | чтение диапазона таблицы в `SheetRow` | в объект `SheetPlan`: читает, отдаёт ряды, знает свой диапазон и колонку ссылок |
| `planning\sheet_parser.py::parse_sheet_datetime`, `column_letters_to_index`, `extract_end_column_letters_from_sheet_range` | разбор даты и времени (7 форматов даты, 6 форматов времени), буквы колонок | чистые преобразования — переносятся как есть, в `core\sheet_text.py` |
| `core\models.py::SheetRow`, `RowVideoCharacteristics`, `VideoMetadata`, `NormalizedImage`, `PreparedVideo`, `PlannedVideo` | модель ряда и подготовленного видео | сливаются в объекты `SheetRow` → `SourceVideo` → `SlotVideo` |
| `planning\link_normalization.py`, `dedup.py`, `duplicate_rows.py` | нормализация ссылок, дедупликация, подсветка дублей рядов | методы `SheetPlan` / `SourceVideo` |
| `core\language.py`, `language_profile.py`, `language_resolver.py`, `language_display.py`, `planning\merge_rules.py::planned_video_block_language` | определение языка ряда и языка блока (`langdetect`, `pycountry`) | объект `LanguageResolver`; правило языка блока — метод `SlotVideo` |
| `ingest\youtube_metadata.py` (yt-dlp через `subprocess` + JSON, `extract_youtube_video_id`) | название, описание, превью, языки аудио и субтитров источника | объект `MetadataFetcher` (Protocol) + `YtDlpFetcher` |
| `media\image_normalizer.py` | нормализация превью (Pillow) | метод объекта `Preview` |
| `planning\slots.py::planned_video_slot_key`, `planned_video_time_key`, `language_sort_key` | ключ слота `{ddmmyy}_{HHMM}_{lang}`, порядок языков | методы `StreamSlot` |
| `llm\` целиком: `llm_client.py`, `llm_factory.py`, `providers\provider_openai.py`, `llm_rate_limits.py`, `llm_usage_tracker.py`, `model_selection.py`, `models\model_identity.py`, `model_pricing.py` | транспорт OpenAI Responses API, выбор модели, тарифы, учёт расхода | переносится, ключ API — только через сейф (§7); merge и прогон видят нейросеть только через разъём `app\llm\backend.py::LlmBackend`, всё про OpenAI — в `app\llm\backends\` (§14 решение 22) |
| `llm\merges\` целиком (20 модулей: `merge_orchestrator.py::MergeOrchestrator.run`, `merge_prompt.py`, `merge_parser.py`, `merge_validation*.py`, `merge_quality.py`, `merge_retry.py`, `merge_formatting.py`, `script_mix_repair.py`, `quality_*.py`) | **функция merge**: контракты compact (2 источника) / expanded (3+) / narrative (одно событие), валидация, нормализация, повторы | поведение переносится как есть, форма — объекты (§14 решение 23): свободные функции и строки-причины `RuntimeError` становятся методами и полями объектов (`MergeAnswer`, `MergedDescription`, `MergeJob`); каждая задача переноса доказывает совпадение с донором — тесты донора в livecraft и сравнение на одних входах |
| `resources\text\prompt_merge_*.txt`, `prompt_retry_*.txt`, `llm\prompts\templates.yaml`, `resources\text_catalog.py`, `resource_loader.py` | промпты и лексиконы | переносятся файлами, без правок текстов. Источник текстов промта — боевой `llm\prompts\templates.yaml` (секция `llm`: `merge_title_description_prompt`, `merge_structural_rules`, `merge_contracts`, `merge_retry_reinforcements`; `common.no_description_text`) в том виде, в каком его отдаёт загрузчик донора; файлы `resources\text\prompt_merge_*.txt` и `prompt_retry_*.txt` донора — устаревшие запасные значения, с боевыми расходятся (проверено 25-09-2026) и не переносятся |
| `publish\post_llm_sanitation.py`, `sanitizers\description_composer.py`, `quality_gate.py`, `tail_parser.py`, `url_selector.py`, `core\safe_trim.py`, `core\video_title_cleanup.py`, `core\description_cleaner.py` | санация текста после LLM, безопасная обрезка ≤100 / ≤5000 | переносятся; **обрезка обязательна** — лимиты YouTube жёстче лимитов анонса |
| `publish\slot_publish_texts.py::resolve_slot_publish_texts`, `SlotPublishTexts` | «какие именно тексты ушли в анонс» | становится «какие тексты уходят в эфир» — метод `StreamSlot.texts` |
| `publish\planer_package_exporter.py::PlanerPackageFormat`, `PlanerPackageExporter` | **схема слота** (`slot_id`, `date`, `time`, `start`, `language`, `title`, `description`, `previews`, `sources`) и ZIP-пакет `plan_*.bcast` | схема — контракт объекта `StreamSlot` (`to_record`); ZIP-пакет — объект `app\packages\` (§14 решение 13), формат ровно тот, что читает `planers\app\package\reader.py` |
| `runtime\ytdlp_updater.py`, `cookies_updater.py`, `deno_updater.py`, `single_instance.py`, `startup_banner.py` | автообновление `tools\yt-dlp.exe` и `tools\deno.exe`, срок cookies, файл-замок одного экземпляра | переносятся; замок — `state\livecraft.lock`, захват **до** настройки логов |
| `paths\_root.py`, `project_paths.py`, `core\safe_trim.py`, `bootstrap\logging_config.py` | корень проекта, пути, логирование | заменяются образцом из `planers` (§5) |

**Переносится на этапе «Публикация» (§14 решение 14), в ООП-форме:** Google Docs и Drive (`publish\google_docs_writer.py`, `docs_*`, `doc_*`, `google\docs_client.py`, `drive_client.py`, `pipeline\daily_doc_publish.py`), отправка анонсов и пакета в Telegram (`pipeline\daily_telegram_publish.py` и отправляющая часть `telegram\` — транспортом `requests` к Bot API вместо aiogram), `core\cta_detection.py`, `official_links.py` и лексиконы CTA. Точный перечень модулей — при нарезке этапа, по коду донора.

**Не переносится из restreamer:** `telegram_bot\`, `bot_main.py`, `promo.py` и aiogram (приём команд не нужен, только отправка); `config\app_config_loader.py` вместе с YAML (§5: конфиги — JSON, как в planers).

### Контур B — «Эфир»: из `planers` (`D:\_projects\planers\app\`)

Переносится **целиком и почти без правок** — это самая отлаженная часть пайплайна (задачи 5a–5n-D, живые прогоны с 13-09-2026 по 19-09-2026).

| Пакет | Что даёт |
|---|---|
| `pipeline\plan.py` | `PlannedBroadcast` (объект запланированного эфира), `BroadcastSpec` + `diff` (единственное определение «одинаковый»), `ChangedField`, `FixCall`, `FIX_CALLS`, `FIXABLE_FIELDS`, `REPORTED_FIELDS`, `Decision`, `AdmissionKind`, `AdmissionReason`, `admit`, `decide_key_delivery`, `confirm_key`, `to_record` |
| `pipeline\selection.py::build_planned`, `SkipReason`, `Selection` | размножение слота по каналам своего языка, признак `too_late` по `min_lead_minutes` |
| `pipeline\reconciler.py::Reconciler` | сверка с площадкой: опознание по минуте старта UTC + маркер `slot_id` в названии потока, заглушки обложек, решения MATCH / UPDATE / CREATE / AMBIGUOUS |
| `pipeline\runner.py` | оркестрация, `_admit_all`, `_check_form_dates`, `_Executor` (создать → привязать → исправить → настройки видео → факты → ключ в форму), `decide_exit` → `RunExit` |
| `platforms\youtube.py::YouTubePlatform` | все вызовы YouTube Data API v3 через единственную точку `_execute`: пауза `youtube_pause_seconds`, повторы, память отказов; `REASON_BEHAVIORS`, `OPERATION_REASON_BEHAVIORS`, `CREATING_OPERATIONS` (создающие вызовы не повторяются вслепую), `YOUTUBE_STREAM_KEY_PATTERN` |
| `platforms\channel.py`, `channel_sync.py`, `passport.py`, `verified.py`, `base.py`, `fake.py` | объект-канал `Channel` со статусом, `ChannelBook` (проверка без браузера при старте + фаза входов), паспорт «ник ↔ id ↔ файл токена», шлюз `VerifiedPlatform` (к площадке — только каналы `READY`), `FakePlatform` для тестов |
| `form\` (`base.py`, `discovery.py`, `key_form.py`, `submitter.py`) | объект-форма `KeyForm`, разбор `FB_PUBLIC_LOAD_DATA_`, `FormAnswers` / `MissingAnswer`, `DateCoverage` (предстартовая проверка дат), POST `formResponse`, структурное подтверждение `read_confirmation` |
| `records\slot_record.py`, `record_store.py` | память: `SlotRecord`, `SlotStage`, `RecordResults`, `ConfirmedAnswer`, `RecordStore` на stdlib `sqlite3` |
| `google\auth.py` | OAuth с одним токеном на канал, скоуп только `…/auth/youtube`, `save_token` / `drop_token` только после подтверждения канала, `LOGIN_TIMEOUT_SEC` = 600 |
| `output\` (`console.py`, `report.py`, `keys_file.py`, `progress.py`), `ui\messages_ru.py` | шесть блоков консоли, отчёт `.md`, `keystreams\keys.txt`, строки прогресса, все русские тексты в одном месте |
| `core\dates.py`, `retry.py::RetryPolicy`, `retention.py`, `text.py` | форматы дат, единственная политика повторов, чистка старья, `normalize_handle` / `token_file_stem` |
| `paths.py`, `observability\logging_setup.py`, `config\loader.py`, `version.py` | корень рядом с exe (frozen) или репо (dev), атомарная запись, логи, JSON-конфиги без умолчаний, единственный номер версии |
| `planer.spec`, `planer.iss`, `build_release.bat`, `planer.bat` | PyInstaller (одна папка), Inno Setup, пусковой bat | одна сборка без чьих-либо данных (§14 решение 16); `build_local.bat` не переносится |

**Переносится из planers для режима Б (§14 решение 18):** `package\reader.py` (разбор и проверка манифеста), `package\bcast.py` (все пакеты `bcast\` — одна карта слотов, новее побеждает, прошедшие отдельно), `package\model.py` — сливается с `StreamSlot` (слот из записи пакета). `tools\` (пробники) переносятся, но в поставку не идут.

---

## 3. Сквозной сценарий

1. Оператор один раз настраивает программу через графический настройщик (ярлык «Livecraft — настройка», §8): ключ OpenAI, таблица плана, форма ключей, Telegram (бот, куда слать объявления), папка Google Docs, каналы YouTube, технические настройки — вручную или загрузкой пары «токен + ключ» (§14 решение 16). Всё секретное ложится в личный сейф зашифрованным (§7).
2. Оператор запускает ярлык режима (§10). Программа (режим А, вариант «всё»):
   1. берёт файл-замок `state\livecraft.lock` (второй экземпляр — сообщение и код 1); настраивает логи; печатает шапку `Livecraft <версия> — <дата время>`;
   2. открывает сейф, читает настройки и `secrets\channels.json`; сверяет каналы по токенам **без браузера** (ник → id по паспорту → название; сменённые ник и название выравнивает сама);
   3. читает Google Sheets (диапазон из настроек), разбирает ряды: ссылка, дата, время; нормализует ссылки, отбрасывает дубли и прошедшее; определяет язык каждого ряда; группирует ряды в **слоты** по `{дата}_{HHMM}_{язык}`;
   4. по каждому источнику слота берёт метаданные и превью (`tools\yt-dlp.exe`), нормализует картинку;
   5. **merge**: на каждый слот — один запрос к LLM, контракт по числу источников (compact / expanded / narrative), разбор, валидация, нормализация, повторы; на выходе — название ≤100 символов и описание ≤5000 байт после санации, замены `<` `>` и безопасной обрезки;
   6. собирает объекты `StreamSlot` — это шов между контурами; дальше про таблицу, yt-dlp и LLM никто не знает; пишет пакет `bcast\plan_*.bcast` (§14 решение 13);
   6a. объявления: Google Doc, анонсы и пакет в Telegram — в группу или в личный чат с ботом, как задано в настройке (§14 решения 14, 19); всё из памяти;
   7. читает форму ключей (один GET на уникальный URL), делает предстартовую проверку покрытия дат (`KeyForm.date_coverage`);
   8. отбирает слоты под языки каналов → объекты `PlannedBroadcast`; фаза входов в браузере для каналов, которым нужен вход; после неё браузер не открывается;
   9. допуск: канал подтверждён + форма прочиталась + для даты, языка и прочих полей в форме есть варианты. Не допущенный объект на YouTube ничего не создаёт и не исправляет, в форму ничего не шлёт, но остаётся в отчёте, консоли и `keys.txt`; код выхода 1;
   10. сверка с YouTube и действия по каждому объекту: нет эфира → создать (эфир → поток с маркером `slot_id` → bind → обложка → настройки видео); есть и что-то разошлось → исправить **только нужными вызовами** (`FIX_CALLS`); совпало → копия;
   11. ключ и адрес потока читаются **только с площадки**; объект сам решает, должен ли ключ уйти (`decide_key_delivery`); ключ → форма → подтверждение → запись в память; затем следующий объект;
   12. перегенерирует `keystreams\keys.txt`, печатает итог и блоки в консоль, пишет `logs\{DD-MM-YYYY}_{HHMMSS}_report.md` и `…_livecraft.log`, чистит старьё по `keep_days`.
   Вариант «объявления» — шаги 1–6a; вариант «эфиры» — шаги 1–6 и 7–12. Режим Б вместо шагов 3–6 читает пакеты из `bcast\` и собирает из них слоты; дальше шаги 7–12.
3. Оператор смотрит отчёт. Что-то не так с эфиром — правит или удаляет его на YouTube руками; следующий запуск это увидит.

---

## 4. Шов между контурами: `StreamSlot`

Контуры A и B работают в одном процессе; шов между ними — объект `StreamSlot` **в памяти**. Схема пакета `*.bcast` — запись того же слота: режим А пишет пакет после сборки слотов (§14 решение 13) и обратно его не читает; режим Б собирает слоты из пакетов (решение 18). Контур B не знает, откуда пришли слоты.

`StreamSlot` (пакет `app\slots\`) — единственный объект, который пересекает границу:

```
slot_id           "16-09-2026_1900_uk"     — {DD-MM-YYYY}_{HHMM}_{lang}
date              "16-09-2026"             — для людей и для формы
time              "19:00"                  — для людей, в форму не уходит
start             ISO-8601 со смещением    — момент для сравнения с YouTube
language          "uk"
title             ≤100 символов после safe_trim, без < и > (SlotTexts.for_youtube)
description       ≤5000 байт UTF-8 после safe_trim, без < и >
previews          кортеж Preview           — 1..N, обложкой ставится одно случайное
sources           кортеж ссылок источников
```

Правила шва:

- Контур A ничего не знает про YouTube, форму, каналы и ключи. Контур B ничего не знает про таблицу, yt-dlp, LLM и Telegram. Единственный общий импорт — `app\slots\`.
- Тексты слота — **окончательные**: контур B их не редактирует, только обрезает по лимитам площадки при сравнении спек (обрезка с обеих сторон, иначе длинное название правилось бы бесконечно).
- Прошлое фильтрует контур B сам, даже если контур A уже отфильтровал.
- Пакет: запись слота — `StreamSlot.to_record` и обратный разбор; ZIP, манифест и карта слотов из `bcast\` — `app\packages\` (правила `planers\app\package\`). Формат — ровно тот, что читает `planers\app\package\reader.py` (schema_version 1): пакет livecraft годится и для planers, пока тот работает. Ссылка на форму ключей лежит в манифесте открытым текстом (решение 13).
- Границу держат тесты: контур A тестируется по `StreamSlot` на выходе, контур B — по `StreamSlot` на входе и `FakePlatform` / поддельному `requests`.

---

## 5. Структура, корень и файлы

```
app\
  main.py                 argparse → режимы; коды выхода 0/1/2/3
  version.py              APP_VERSION — единственный номер версии
  paths.py                корень рядом с exe (frozen) или репо (dev); write_text_atomically
  core\                   dates.py, text.py, retry.py (RetryPolicy), retention.py, safe_trim.py, sheet_text.py
  secretsafe\             сейф: value.py (SecretValue), vault.py (Vault), store.py (VaultStore), crypto.py (AES-GCM + HKDF), dpapi.py (ctypes), token.py (пара «токен + ключ», §14 решение 16)
  config\                 loader.py → LivecraftConfig (настройки + каналы), объекты ChannelConfig / LivecraftSettings
  observability\          logging_setup.py — маскирование секретов и ключей потока
  runtime\                single_instance.py (InstanceLock — замок одного экземпляра),
                          ytdlp_updater.py, deno_updater.py, cookies_updater.py — автообновление внешних бинарников
  sheets\                 client.py (Google Sheets API), plan.py (SheetPlan, SheetRow), rows.py (нормализация, дедуп)
  sources\                fetcher.py (Protocol), ytdlp.py (YtDlpFetcher), preview.py (Preview, нормализация Pillow), metadata.py, language.py (LanguageResolver — язык источника, §14 решение 12), video.py (SourceVideo, SourceCatalog)
  resources\              loader.py; text\ — промты и лексиконы restreamer файлами, без правок текстов (§2)
  llm\                    backend.py (LlmBackend, LlmRequest, LlmResponse), errors.py, usage.py, selection.py, json_text.py;
                          backends\ — реализации разъёма (openai.py, openai_model.py, openai_response.py, openai_errors.py, openai_rate_limits.py);
                          merges\ — функция merge (ответ модели, качество, контракты, промт, MergeJob)
  texts\                  санация после LLM, safe-обрезка, композиция описания, quality gate
  slots\                  texts.py (SlotTexts), slot.py (SlotKey, StreamSlot), builder.py (SlotGroup, SlotBuilder, SlotBuild)
  packages\               пакет plan_*.bcast: запись (режим А) и карта слотов из bcast\ (режим Б), §14 решения 13, 18
  publish\                объявления: Telegram (Bot API через requests) и Google Docs, §14 решения 14, 19
  platforms\              base.py, youtube.py, channel.py, channel_sync.py, passport.py, verified.py, fake.py
  google\                 auth.py — OAuth: скоупы и токены (§9); клиенты Docs и Drive — этап «Публикация»
  form\                   base.py, discovery.py, key_form.py, submitter.py
  records\                slot_record.py, record_store.py (SQLite secrets\livecraft.sqlite3)
  pipeline\               plan.py (PlannedBroadcast, BroadcastSpec), selection.py, reconciler.py, runner.py
  output\                 console.py, report.py, keys_file.py, progress.py
  setup\                  графический настройщик (§8): app.py (окно), tabs\ (вкладки Tk поверх моделей), panels\, fields\, validators.py; readiness.py — готовность к запуску (сейф + конфиги), её же показывает настройщик
  ui\messages_ru.py       ВСЕ тексты консоли, отчёта, keys.txt и настройщика
  examples\               channels.example.json — шаблон для репо и README; в сборку не идёт
  tools\                  пробники разработки (форма, обложки, эфиры); в поставку не идут
  tests\                  pytest; data\ — сохранённые страницы формы и ответы yt-dlp
secrets\                  vault.local.dat, livecraft.json, channels.json, channels.previous.json,
                          channels_passport.json, livecraft.sqlite3, client_secret.json, <handle>.token.json,
                          sheets.token.json, cookies.txt
tools\                    yt-dlp.exe, deno.exe (внешние бинарники, обновляются сами)
image\                    локальные превью по шаблону {date}\{language}
bcast\                    пакеты plan_*.bcast: пишет режим А, читает режим Б; сюда же кладётся пакет, открытый двойным щелчком
tokens\                   созданные пары «токен + ключ» (§14 решение 16)
keystreams\keys.txt       ключи потоков для ручной передачи
state\                    livecraft.lock, отметки автообновлений
logs\                     {DD-MM-YYYY}_{HHMMSS}_report.md, …_livecraft.log
livecraft.bat             рядом livecraft.exe — запускает его, иначе python -m app.main из .venv_livecraft
livecraft.spec            PyInstaller, одна папка; livecraft.iss — Inno Setup; livecraft.ico
build_release.bat         exe + инсталлятор; в сборке нет ничьих данных (§14 решение 16)
```

Папка `platforms\` — во множественном числе (`platform` занято stdlib). Папка `secretsafe\` — не `secrets\`, чтобы имя пакета не совпадало с именем папки данных.

Корень — `app\paths.py::resolve_root`: `LIVECRAFT_ROOT` из окружения (только тесты и отладка), иначе папка exe в frozen-режиме, иначе корень репо. Ставить в папку с правом записи (`D:\_exe\Livecraft`), не в Program Files.

**Конфиги — JSON, все поля обязательные, умолчаний в коде нет** (решение planers, задача 5b; YAML и PyYAML не тянем). Два файла: `secrets\livecraft.json` (технические настройки; в git его нет — поставочный вид задаёт шаблон `app\ui\messages_ru.py::CONFIG_SETTINGS_TEMPLATE`, и при первом запуске программа сама создаёт файл из шаблона, задача 3.6a) и `secrets\channels.json` (каналы, заполняет оператор или настройщик). Нет файла или обязательного поля — код 2, ошибка и точный шаблон файла в консоли.

**Секретов в конфигах нет** (§7.5): ключ OpenAI, id и диапазон таблицы, токен бота Telegram — только в сейфе. Ссылка на форму ключей, ссылка на папку Google Docs и чаты Telegram — открытые настройки `livecraft.json` (§14 решения 15, 19); пустое значение — «не настроено», соответствующая часть режима не готова. `entry.*`-ID формы не хранятся нигде — читаются из HTML формы.

---

## 6. Инварианты — не нарушать без явного согласования с Артуром

0. **Рабочая единица контура A — слот** (`app\slots\slot.py::StreamSlot`), контура B — **объект запланированного эфира** (`app\pipeline\plan.py::PlannedBroadcast`): один объект = один эфир одного слота на одном канале. Правило допуска — только `PlannedBroadcast.admit`. Память — `secrets\livecraft.sqlite3`, одна запись на (`slot_id`, id канала на YouTube); обратно читаются только результаты (эфир, поток, ключ, подтверждение формы). Задания из памяти не берутся: что делать, каждый запуск решают свежая таблица (режим А) или пакеты `bcast\` (режим Б), форма и `channels.json`.
1. **Истина об эфирах — на YouTube; ключ потока и ссылка на эфир читаются только с площадки.** Память — истина только о том, что форма подтвердила. Перед любым созданием — `liveBroadcasts.list(broadcastStatus=upcoming)` (без `mine` — параметры взаимоисключающие); совпадение по минуте старта UTC + маркер `slot_id` в `liveStreams.snippet.title`. Опознанный эфир сверяется по всем диктуемым полям (`BroadcastSpec.diff`): исправимое расхождение — правка на месте только нужными вызовами (`FIX_CALLS`), план в приоритете; неисправимое (`REPORTED_FIELDS`) — строка в лог, «внимание:» в консоли и строка в отчёте. Решения «создать заново» нет.
1a. **В форму уходит ключ, который в этом запуске должен дойти до стримера** — одно правило, `PlannedBroadcast.decide_key_delivery`: объект допущен, не `too_late`, ключ и адрес потока взяты с площадки в этом запуске, и в памяти нет подтверждения ровно этой тройки «тот же ключ, тот же адрес формы, те же ответы». Момент отправки — сразу после действий по своему объекту; финального прохода нет; подтверждение сразу пишется в память. Задвоение строки у стримера допустимо: стример берёт последнюю строку по дате, каналу и языку — README обязан это объяснить.
2. **Google-форма — единственный канал передачи ключей стримеру.** Никаких обходных путей. `entry.*`-ID не хардкодятся и не конфигурируются — читаются из HTML формы. Программа никогда не отправляет значение, которого нет среди вариантов вопроса: нет даты в «Время стрима» → объект не допущен. Названия вопросов, тексты вариантов и формат даты — настройки (сегодня `app.planer.form_fields` / `form_values` / `form_date_format` в `restreamer\app\config\runtime\app_config.example.yaml`); **ссылка на форму — открытая настройка `livecraft.json`** (§14 решение 15) и открытым текстом уходит в пакет (решение 13).
3. **Google Sheets — только чтение плана.** Обратной записи в таблицу в v1 нет (в restreamer она была под флагами `STG_SHEETS_LINK_WRITEBACK` / подсветки дублей — в livecraft не переносится). Диапазон — настройка (`sheets_range`, сегодня `"A:F"`); **id таблицы — в сейфе**.
4. **Даты `DD-MM-YYYY`, время `HH:MM` по Киеву** — во всех файлах, именах и отчётах. Исключения: `start` слота и `slot_start_utc` в памяти — ISO-8601, только для сравнения моментов; значения, уходящие в форму, — в формате даты формы.
5. **Никаких паролей.** Авторизация — OAuth. Ключ канала — ник `handle` (без «@», NFC, без регистра): все словари, кеши, группы вывода и память отказов — по нему. Паспорт `secrets\channels_passport.json` связывает ник ↔ id YouTube ↔ файл токена; id канала не меняется никогда. Ник или название сменились, а id подтверждён — программа выравнивает `channels.json`, файл токена и паспорт сама.
6. **Секреты не попадают в вывод.** Ключи потока маскируются в логах (последние 4 символа), полностью — только в `keys.txt`. Ключ OpenAI, id и диапазон таблицы, токен бота Telegram **не печатаются нигде**: ни в консоль, ни в отчёт, ни в лог, ни в тексты исключений. Подробно — §7.
7. **Настройки эфира задаёт программа, а не канал.** Аудитория — всегда «не для детей»; категория, автостарт и «ставить обложку» — из `livecraft.json`; язык — из слота; видимость — из `channels.json`. Всё правится одним проходом по ресурсу `videos` (`part=snippet,status`, read-modify-write целиком). Возрастное ограничение 18+ через API только читается.
8. **Эфиры не удаляются автоматически.** Эфиры без соответствия слоту не трогаются и в отчёт не попадают. Эфир с маркером программы, чьего слота больше нет, — в отчёт «Перенесён или отменён?», не удаляется.
9. **Один сбой не валит запуск.** Ошибка изолируется в объект и уходит в отчёт; остальные обрабатываются. Отказ YouTube обрабатывается по причине (пять поведений: повторить / не повторять этот вызов / не вызывать эту операцию на канале / не обращаться к каналу / не обращаться к YouTube). Создающие вызовы (`liveBroadcasts.insert`, `liveStreams.insert`) с неизвестным исходом **не повторяются вслепую**. Между любыми двумя обращениями к YouTube — не меньше `youtube_pause_seconds`.
10. **Одна папка данных на роль.** `secrets\` — всё секретное и все конфиги; `image\` — превью; `bcast\` — пакеты; `tokens\` — созданные токены; `keystreams\` — ключи; `state\` — замок и служебные отметки; `logs\` — лог и отчёт. Всё остальное — в `app\`. Повреждённая память переименовывается в `livecraft.sqlite3.broken-<DD-MM-YYYY_HHMMSS>` и создаётся новая.
11. **Новые зависимости — только с явного согласования.** Разрешённый набор v1: `openai`, `google-api-python-client`, `google-auth`, `google-auth-oauthlib`, `requests`, `cryptography`, `pillow`, `langdetect`, `pycountry`, `tzdata` (база часовых поясов для `zoneinfo`; добавлен решением 10 §14, 21-09-2026) + stdlib (в т.ч. `sqlite3`, `tkinter`, `ctypes`). Сборка — `pyinstaller`, тесты — `pytest` (в приложение не импортируются). `cryptography` **уже стоит** в обоих окружениях (`planers` 50.0.1, `restreamer` 46.0.5) как транзитивная зависимость Google-стека — это не новая зависимость. Внешние бинарники: `tools\yt-dlp.exe`, `tools\deno.exe`. Убрано против restreamer: `aiogram`, `aiohttp`, `PyYAML`, `python-dotenv`; Telegram — HTTP-запросами к Bot API через `requests` (§14 решение 14).
12. **Один экземпляр.** Файл-замок `state\livecraft.lock` берётся в `main` **до** настройки логов; успешный захват и освобождение пишутся в `logs\startup.log`; конфликт — русское сообщение в stderr, строка `lock_rejected` и код 1. Застарелый замок (PID мёртв) перезаписывается атомарно: `unlink` → ровно одна повторная попытка `O_EXCL`; проигрыш повтора — **fail-closed**, а не перезапись. В существующий файл замка не писать никогда.

---

## 7. Секреты: сейф и шифрование

### 7.1 Задача

Программа раздаётся **без чьих-либо данных** (§14 решение 16). Свои значения человек вводит сам или загружает из пары файлов «токен + ключ», которую ему выдал другой пользователь (например, Артур). Получатель токена должен **уметь работать** на этих значениях, но **не должен уметь прочитать** их глазами — открыть файл в блокноте и скопировать ключ или перейти по ссылке руками. Любое значение он может заменить своим.

### 7.2 Честная граница возможного — читать до проектирования

Если программа пользуется значениями, которые человек не должен видеть, значит расшифрованное значение есть в памяти работающей программы, и достаточно упорный пользователь достанет его: распакует `livecraft.exe` и прочитает байт-код, поставит перехватчик на `requests` или посмотрит трафик. Пара «токен + ключ» до загрузки тоже расшифровывается любым, кто напишет свой код.

**Поэтому шифрование значений из токена — это защита от копирования, а не от специалиста.** Она честно решает ровно ту задачу, которую поставил Артур («тупо перейти по ним руками не сможет»), и не решает никакой другой. Не описывать её в README и в интерфейсе как «защищено шифрованием» без оговорки.

Второе, что нужно понимать: **ссылка на таблицу — не система доступа**. Если таблица открыта «всем, у кого есть ссылка», то скрытая ссылка — единственное, что её защищает, и тогда прятать её осмысленно. Если таблица выдана поимённо, ссылка не защищает ничего: получатель без доступа её всё равно не прочитает. Боевая таблица выдана поимённо (§15, 24-09-2026): получателю токена Артур открывает таблицу на его почту. Артур выбирает режим доступа к таблице и форме сам; livecraft подстраивается.

Третье: **секреты самого пользователя защищаются по-настоящему** — они не обязаны никуда уезжать, поэтому шифруются ключом, привязанным к его Windows-аккаунту, и копирование файла на другую машину не даёт ничего. Это и есть настоящая, а не декоративная часть схемы.

### 7.3 Устройство

Пакет `app\secretsafe\`. Ни одной свободной функции с доступом к секрету.

**`SecretValue`** (`value.py`) — обёртка над секретной строкой:
- `reveal() -> str` — единственный способ получить значение; вызывается только в точке применения (заголовок HTTP, параметр API), никогда — ради вывода;
- `masked -> str` — то, что видят люди: `sk-…dc7f`, `docs.google.com/…/edit` → `таблица плана (…f3a1)`;
- `__str__`, `__repr__`, `__format__` возвращают `masked`. Секрет, случайно попавший в `f"{...}"`, в `logging` или в текст исключения, выходит маской. Тест на это — обязательный.

**`Vault`** (`vault.py`) — объект-сейф в памяти: поля по именам (`openai_api_key`, `sheets_id`, `sheets_range`, `telegram_bot_token`), каждое — `SecretValue` либо `None`, плюс происхождение поля (`своё` / `из токена`). Знает, каких полей не хватает для запуска, и сам формирует причину недопуска — так же, как `PlannedBroadcast.admit` формирует свои `AdmissionReason`.

**`VaultStore`** (`store.py`) — чтение и запись файла сейфа `secrets\vault.local.dat`: ключ файла завёрнут DPAPI (привязка к Windows-аккаунту), пишет только настройщик. Копирование файла на другую машину или под другого пользователя бесполезно. Поле с происхождением «из токена» заменяется своим значением; «удалить своё» возвращает значение из токена, если оно было загружено.

**`VaultCrypto`** (`crypto.py`) — сама криптография, `cryptography` (уже в окружении, инвариант 11):
- шифр — **AES-256-GCM** (`cryptography.hazmat.primitives.ciphers.aead.AESGCM`): аутентифицированный, подмена байта ломает расшифровку, а не даёт мусор;
- ключ поля — `HKDF-SHA256(ikm = ключ сейфа, salt = 16 случайных байт файла, info = b"livecraft.vault.v1")`;
- на каждое поле — свой `nonce` (12 случайных байт) и своя AAD = имя поля + версия формата: перекинуть блоб из поля `sheets_id` в поле `telegram_bot_token` не выйдет;
- формат файла — JSON: `{"version": 1, "salt": base64, "fields": {"<имя>": {"nonce": base64, "ct": base64}}}`; запись — только `paths.write_text_atomically`;
- **ключ сейфа** (решение 9, 20-09-2026) — 32 случайных байта, созданные при первой записи и завёрнутые DPAPI; завёрнутый ключ лежит в самом файле записью `"key": base64`. Нет записи или не разворачивается DPAPI — файл считается нечитаемым, программа говорит об этом прямо. Ключ разворачивает только та учётная запись Windows, под которой он был создан — это и есть настоящая, а не декоративная часть схемы (§7.2, третий абзац). Потеря профиля Windows делает сейф нечитаемым; лечится повторной настройкой или загрузкой токена;
- неизвестная версия формата — ошибка с именем файла и код 2, а не тихое игнорирование.

**`AccessToken`** (`token.py`) — пара файлов «токен + ключ» (§14 решение 16): `*.lctoken` — магия, версия, id пары, срок, nonce, AES-GCM-шифротекст значений; `*.lckey` — магия, версия, тот же id пары, 32 случайных байта ключа. Оба двоичные, в блокноте нечитаемы; пароля человек не вводит. Время создания и «действует до» берутся из заголовка `Date` ответа сервера Google по HTTPS, при загрузке — так же; нет сети — загрузка не выполняется. В токен попадают только значения с происхождением «своё», плюс открытые настройки (ссылка формы, папка Docs, чаты Telegram); каналы и токены входа Google — никогда.

**В коде пока старая схема (задачи 1.3, 2.1):** `ProgramKey` (`program_key.py`, `secrets\program.key`) и поставочный `vault.dat`. Они убираются задачей этапа «Токен доступа» (§13); до неё приоритет «личное перекрывает поставочное» продолжает работать как есть.

**`Dpapi`** (`dpapi.py`) — `CryptProtectData` / `CryptUnprotectData` из `crypt32.dll` через `ctypes` (stdlib, ноль зависимостей), флаг `CRYPTPROTECT_LOCAL_MACHINE` **не** ставится — привязка к пользователю, а не к машине. Описание (`szDataDescr`) — `"Livecraft local vault"`. Не Windows или DPAPI недоступен — сейф не создаётся, настройщик говорит об этом прямо.

### 7.4 Дисциплина обращения — обязательна к соблюдению

- Расшифрованное значение живёт **только в `SecretValue`** и только в памяти. Ни временных файлов, ни переменных окружения, ни `state\`, ни кеша.
- `reveal()` вызывается в четырёх местах и нигде больше: заголовок `Authorization` клиента OpenAI; параметры `spreadsheetId` и `range` клиента Sheets (одна точка: метод чтения диапазона); адрес Bot API клиента Telegram (токен бота стоит в пути запроса — `observability` обязана вычёркивать его из строк чужих библиотек); кнопка «показать своё» окна настройщика — только для поля с происхождением «своё», только по явному нажатию, только на экран (не в лог, не в файл, не в буфер обмена); значение «из токена» не раскрывается никогда и никому (§14 решение 11). Запись в токен — `SecretValue.encrypt`, без `reveal()`. Каждый новый вызов `reveal()` — строкой-обоснованием в отчёте задачи. Ссылка на форму больше не секрет (решение 15): постоянной точки `reveal()` у неё нет. Временная точка — однократный перенос старого поля сейфа `key_form_url` в `livecraft.json` (`app\setup\migration.py::FormUrlMigration`, задача 3.6); удаляется вместе с полем на этапе «Токен доступа».
- **Логи, консоль, отчёт и `keys.txt` не содержат значений сейфа.** Где нужно различить значения, пишется ярлык и короткий отпечаток: `sheet=plan(9c2b)` — первые 4 hex sha256 от значения. Ссылки на форму и папку Docs — открытые настройки; в лог их писать можно.
- В `observability\logging_setup.py` ставится `logging.Filter`, который вычёркивает известные секреты из готовой строки лога — последний рубеж на случай чужой библиотеки (`googleapiclient`, `openai`, `urllib3` пишут URL в DEBUG). Фильтр — страховка, а не разрешение писать секреты.
- Тексты исключений секретов не содержат. Ошибка «таблица не открылась» называет ярлык и код ответа, а не URL.
- Тесты: секрет в `f"{secret}"`, в `logging.info("%s", secret)`, в `repr(obj)` и в трассировке даёт маску; сейф с подменённым байтом не расшифровывается; своё значение перекрывает значение из токена; значение «из токена» не попадает в новый токен; токен с чужим ключом, подменённым байтом или истёкшим сроком не загружается.

### 7.5 Что шифруется

| Значение | Где сейчас | В сейф |
|---|---|---|
| ключ OpenAI | `GPT_API_KEY` в `secrets\.env` | **да** |
| id Google-таблицы | `GOOGLE_SHEETS_ID` в `secrets\.env` | **да** |
| диапазон таблицы | `app.google.sheets_range` | да (дёшево и заодно не подсказывает структуру) |
| токен бота Telegram | `secrets\.env` restreamer | **да** |
| URL формы ключей | `app.google.form_url` в `app_config.yaml` | нет: открытая настройка, уходит в пакет (§14 решения 13, 15) |
| ссылка на папку Google Docs | конфиг restreamer | нет: открытая настройка (решение 15) |
| чаты Telegram (группа, личный) | конфиг restreamer | нет: без токена бота бесполезны (решение 19) |
| `client_secret.json` | файл рядом с exe | нет: формат задан Google, читается его библиотекой. Ограничение — по §7.2 честно называется в README |
| токены каналов, `sheets.token.json` | `secrets\<handle>.token.json` | нет: они и так выдаются на конкретный аккаунт и без него бесполезны |
| названия вопросов и варианты формы | `app_config.yaml` | нет: это не секрет, а контракт формы |
| каналы (`channels.json`) | конфиг | нет: ник канала публичен |

---

## 8. Графический настройщик

### 8.1 Решение: Tkinter, окно в самой программе. Браузерного интерфейса не делаем.

Артур просил варианты — вот они, с разбором, и вывод, а не меню.

**Вариант A — Tkinter (stdlib).** Выбран.
- Ноль новых зависимостей (инвариант 11). Tcl/Tk входит в официальный установщик python.org, и у Артура стоит `Python 3.13.9` из `C:\Users\vikon\AppData\Local\Programs\Python\Python313` (`pyvenv.cfg` всех трёх окружений). Проверить за секунду: `.\.venv_livecraft\Scripts\python.exe -c "import tkinter; print(tkinter.TkVersion)"`. Если компонент «tcl/tk and IDLE» при установке был снят — переустановить Python с галочкой, других последствий нет.
- Один процесс, никакого сетевого порта, никакого браузера. Секреты не покидают память программы.
- PyInstaller: в `planers\planer.spec` сейчас `excludes=[… "tkinter"]` — в `livecraft.spec` эту строку **не** переносить. Цена — Tcl/Tk в сборке (единицы–десятки МБ в one-folder); на фоне `googleapiclient` и `pillow` это не проблема.
- Минусы честно: вид по умолчанию скучный (лечится `ttk` и аккуратной сеткой), таблица каналов на `ttk.Treeview` делается дольше, чем HTML-таблица, и на HiDPI-экране MSI Pulse нужен `ctypes.windll.shcore.SetProcessDpiAwareness(1)` в самом начале, иначе шрифт будет мыльным.

**Вариант B — локальный веб-интерфейс (`http.server` + `webbrowser`).** Отклонён, и причина не во вкусе.
Форма настройки показывает и принимает **ровно те значения, которые мы шифруем**. В браузере они попадают в адресную строку и историю, в DevTools, в кеш форм и под любое установленное расширение. То есть вариант B открывает наружу именно то, что §7 закрывает. Плюс появляется localhost-порт, который надо связывать только с `127.0.0.1`, закрывать одноразовым токеном в URL, проверять `Origin` и гасить по таймауту, — работа, которая нужна только затем, чтобы вернуться к уровню безопасности, который у варианта A есть даром. Веб-интерфейс был бы оправдан, если бы настройка шла с другой машины; она идёт с той же.

**Вариант C — PySide6 / Qt.** Отклонён: 100+ МБ в сборке, новая тяжёлая зависимость против инварианта 11, ради формы на три вкладки.

### 8.2 Что настройщик делает

Запуск — ярлык «Livecraft — настройка» (`livecraft.exe --setup`; в сборке — оконный exe без консоли, §13 этап сборки). Каждая вкладка — панель-объект, каждое поле — объект со своим правилом проверки. Сегодня в коде три вкладки (этап 2); «Публикация» и «Токены» добавляются своими этапами.

1. **Ключи и ссылки** — ключ OpenAI, ссылка (или id) таблицы плана, диапазон таблицы (сейф); ссылка на форму ключей и ссылка на папку Google Docs (открытые поля `livecraft.json`, решение 15).
   - Поле, пришедшее из токена, показывается **маской** и кнопкой «заменить своим»: получатель токена не увидит чужой ключ и чужую ссылку в интерфейсе (иначе весь §7 обнуляется).
   - Поле, которое пользователь ввёл сам, показывается ему открыто с переключателем «показать/скрыть»: это его значение.
   - «Удалить своё значение» удаляет поле из сейфа; было значение из токена — возвращается оно.
   - Проверка на месте: ключ OpenAI — форма и пробный запрос по кнопке «проверить»; ссылка таблицы — вытащить id (`/spreadsheets/d/<id>/`) и попробовать чтение первой строки диапазона; ссылка формы — прочитать структуру (`form\discovery.py`) и показать, какие вопросы и варианты нашлись и покрывает ли «Время стрима» ближайшие даты (`KeyForm.date_coverage`). Всё это — вызовы **того же боевого кода**, а не отдельная реализация для настройщика.
2. **Каналы YouTube** — таблица каналов, шесть полей на канал, как в `planers\app\config\loader.py::ChannelConfig`: `platform` (только `youtube`), `account_name` (название как на YouTube), `handle` (ник, ключ канала), `google_account` (почта — подсказка браузеру при входе), `languages` (список из одного кода — §14 решение 21; выбирается выпадающим полем с поиском), `privacy` (`public` | `unlisted`). Кнопки: «добавить», «удалить», «войти в канал» (вызывает ту же фазу входа `ChannelBook.log_in`, после подтверждения пишет токен и запись паспорта), «проверить все» (эквивалент `--check`).
   Запись — только через `config\loader.py::render_channels_file` и `write_text_atomically`, прежний файл — в `channels.previous.json`. Настройщик **не** пишет свой формат JSON руками.
3. **Публикация** (этап «Публикация», решение 19) — токен бота (сейф); куда слать объявления: переключатель «группа | личный чат с ботом» и id обоих чатов. Кнопка «Найти чаты» сама узнаёт id: бот видит чаты, где ему написали `/start` или куда его добавили (Bot API `getUpdates`), человек выбирает из списка. Кнопка «Проверить» шлёт пробное сообщение в выбранный чат. Личный чат — для отладки и для тех, кому группа не нужна.
4. **Токены** (этап «Токен доступа», решение 16) — «Создать токен» (срок в сутках, по умолчанию 3; в `tokens\` ложатся `*.lctoken` и `*.lckey`) и «Загрузить токен» (пути к этим двум файлам).
5. **Настройки запуска** — `min_lead_minutes`, `keep_days`, `auto_start`, `set_thumbnail`, `category_id`, `youtube_pause_seconds`, модель LLM и её параметры (`model`, `fallback_model`, `reasoning_effort`, `service_tier`, `timeout_sec`, `max_output_tokens`), пути шаблонов превью, часовые пояса. Пишется в `secrets\livecraft.json` тем же загрузчиком, который его читает.

Правила настройщика:

- Настройщик **не знает** ни про файлы, ни про шифрование. Он работает с `Vault`, `LivecraftConfig` и `ChannelBook` — теми же объектами, что и пайплайн. Всё, что он умеет делать с диском, делают `VaultStore` и `config\loader.py`.
- Все тексты настройщика — в `app\ui\messages_ru.py`, рядом с текстами консоли и отчёта. В коде — английские идентификаторы.
- Настройщик не запускает пайплайн и ничего не создаёт на YouTube, кроме входа в канал по явной кнопке.
- Ненастроенная программа, запущенная любым ярлыком, открывает окно настройщика (решение 16); из консоли — код 2 и строка «запустите `livecraft.bat --setup`». Это продолжение правила `planers` «нет конфига → код 2 и точный шаблон в консоли».
- Тесты настройщика — на объекты панелей и правила полей (валидация, приоритет сейфов, маскирование, запись через загрузчик), без поднятия окна Tk.

---

## 9. Google: две личности, минимальные скоупы

В livecraft уживаются два разных субъекта авторизации — это надо развести сразу, иначе один токен расползётся на все скоупы.

| Личность | Что делает | Скоуп | Токен |
|---|---|---|---|
| **оператор** | читает план из Google Sheets; пишет Google Doc объявлений (этап «Публикация») | `https://www.googleapis.com/auth/spreadsheets.readonly`; с этапа «Публикация» — ещё `documents` и `drive`, как у restreamer (`restreamer\app\google\auth.py::_SCOPES`); минимальный набор — проверка §15 | `secrets\sheets.token.json` — один на установку |
| **владелец канала** | планирует эфиры на своём канале | `https://www.googleapis.com/auth/youtube` | `secrets\<handle>.token.json` — один на канал |

- Скоупы задаются в `app\google\auth.py` и **больше нигде** — по образцу `planers\app\google\auth.py::YOUTUBE_SCOPE`.
- Из скоупов restreamer (`documents`, `drive`, `spreadsheets.readonly`, `cloud-platform.read-only`) не переносится `cloud-platform.read-only`; `documents` и `drive` добавляются только на этапе «Публикация» (§14 решение 14). Ветка service account не переносится — только OAuth.
- `client_secret.json` — «паспорт программы», OAuth-клиент типа Desktop, один на всех, лежит рядом с exe. Нет файла — код 2, к Google не обращаемся.
- Механика входа — `InstalledAppFlow.run_local_server(port=0)`, `PROMPT = "select_account consent"`, `login_hint` = `google_account`, ожидание не дольше `LOGIN_TIMEOUT_SEC` (600 с), тексты в браузере — русские из `messages_ru`. Токен пишется **только после** подтверждения канала (`keep_login`), иначе чужой токен запоминается навсегда (боевой случай `planers` 17-09-2026).
- Квота YouTube Data API v3 — 10 000 единиц в сутки на проект GCP. Цены — по факту Google Cloud Console за 20-09-2026 и 21-09-2026 (planers, задача 5p; единственный источник — `planers\app\platforms\youtube.py::QUOTA_UNITS`): все чтения (`channels.list`, `liveBroadcasts.list`, `liveStreams.list`, `videos.list`) — 1; `liveBroadcasts.insert`, `.update`, `.bind`, `liveStreams.insert`, `.update` — 1; `videos.update` и `thumbnails.set` — 50. Документация Google даёт 50 за все записи — считаем по факту. Новый эфир с обложкой и настройками видео обходится примерно в 103 единицы.
- Открыто (наследуется от `planers`): свой проект Google Cloud и свой OAuth-клиент; проходить ли верификацию под sensitive-скоуп `auth/youtube` (без неё «приложение не проверено» и потолок 100 пользователей за жизнь проекта).

---

## 10. Режимы запуска и коды выхода

`livecraft.exe` через `livecraft.bat` (окно не закрывается до Enter; bat один на оба случая — рядом exe запускает его, иначе `python -m app.main` из `.venv_livecraft`). Установщик ставит пять ярлыков (§14 решение 17, 18):

| Ярлык | Запуск | Режим |
|---|---|---|
| Livecraft — объявления | `--announce` | А: таблица → merge → слоты → пакет в `bcast\` → Google Doc, анонсы и пакет в Telegram |
| Livecraft — эфиры | `--broadcast` | А: таблица → merge → слоты → пакет в `bcast\` → эфиры, ключи, форма |
| Livecraft — всё | без аргументов | А: объявления, затем эфиры (сбой YouTube не задерживает объявления) |
| Livecraft — эфиры из пакетов | `--from-package` | Б: пакеты из `bcast\` → слоты → эфиры, ключи, форма; без таблицы и нейросети |
| Livecraft — настройка | `--setup` | графический настройщик (§8) |

- Установщик связывает расширение `.bcast` с livecraft: `livecraft.exe <файл>.bcast` копирует пакет в `bcast\` и запускает режим Б.
- Часть выбранного режима не готова (например, не задан чат Telegram) — остальное делается, по неготовой части одна строка с точным действием, код 1. Не готово ничего — код 2. Ненастроенная программа открывает настройщик (§8.2).
- `--dry-run` — любой режим без изменений снаружи: читает таблицу или пакеты, делает merge и сверяется с YouTube (только чтение); ничего не создаёт, в Telegram и Docs не пишет, в форму не шлёт, `keys.txt` не меняет; пакет в `bcast\` пишет;
- `--no-llm` — режим А без merge: тексты источника как есть (быстрый прогон без расхода токенов);
- `--check` — по каждому каналу: вход при необходимости, проверка ник → id → название, языки, число запланированных эфиров;
- `--auth <handle>|all` — принудительный вход (ник в PowerShell **всегда в кавычках**: `--auth "@Osvald.X"`);
- `--status` — сверка эфиров с маркером программы + `keys.txt` и отчёт, без таблицы и LLM;
- `--debug` — подробный лог в терминал; `--version` — `Livecraft <APP_VERSION>`, код 0, ничего не читает.
- `--export-slots` отменён (решение 13): выгрузка слотов — это пакет. В `app\main.py` ключ ещё разбирается (задача 0.1) — убирается задачей пакета.

Коды выхода (решение и причины — одно место, `runner.decide_exit` → `RunExit`): `0` — сделано всё, что можно; `1` — есть ошибки (в том числе неподтверждённая отправка ключа, который должен был уйти, и недопущенные объекты); `2` — ошибка конфигурации, сейфа или авторизации, ничего не делалось; `3` — нет будущих слотов: в таблице (режим А) или в пакетах `bcast\` (режим Б); к каналам не обращались.

---

## 11. Стандарты кода

- **ООП — §0. Это первый пункт стандартов, а не последний.**
- `from __future__ import annotations` в каждом файле; аннотации `X | None`, lowercase generics; явные типы возвращаемых значений и переменных.
- Имена английские: функции `verb_noun`, булевы `is_` / `has_` / `should_`. Папки во множественном числе, файлы в единственном, первое слово имени файла = домен папки.
- Методы ~30 строк, одна ответственность, guard clauses. Явная обработка ошибок, без «голых» `except`. Единственный перехват `Exception` — в `main.run_cli` (обрыв и падение: трассировка в лог, русская строка в консоль, код 1).
- Никакого хардкода: константы — в конфиг, сейф или enum; единый источник истины для каждой константы. Числа тестов в отчётах не хардкодить — формулировка «новых падений относительно baseline нет».
- Повторы сетевых обращений — только через `app\core\retry.py::RetryPolicy` (первое обращение и до 4 повторов, пауза `2 * 2^(n-1)` с плюс случайная добавка до 1 с, потолок 32 с; `rng` параметром, в тестах фиксированный). Что повторять, решает вызывающий; своих констант повторов в модулях нет.
- Иерархические имена логгеров под `"livecraft"` (`"livecraft.youtube"`, `"livecraft.form"`, `"livecraft.llm"`, `"livecraft.sheets"`, `"livecraft.vault"`); структурированные сообщения `key=value`, имя канала в кавычках (`channel="Osvald.X"`). Имя корневого логгера — константа, не строка по месту.
- Все тексты для людей (консоль, отчёт, `keys.txt`, настройщик) — только в `app\ui\messages_ru.py`. В коде — английские идентификаторы сообщений.
- Перенос модуля: старый модуль остаётся re-export фасадом. Дублированного кода нет.
- Тесты `pytest` в `app\tests\`, файл = `test_<модуль>.py`. Тесты гоняют реальный production-путь (JSON-конфиги и сейф во временном корне), а не переписывают логику inline. Подменяются: платформа (`FakePlatform`), `requests`, yt-dlp (сохранённый JSON), LLM (сохранённые ответы), часы (`run(..., clock=)`).
  Обязательное покрытие: разбор таблицы и группировка в слоты с подменённым «сейчас»; маскирование секретов (§7.4); приоритет своего значения над значением из токена и неэкспорт значений «из токена»; сверка на фейковой платформе (нет / совпадает / отличается / удалён / два на минуту); `keys.txt` и отчёт по фиксированному состоянию; разбор формы на сохранённом HTML; сопоставление значений (дата есть / нет, `stream_url` с другим регистром и слэшем); регулярка ключа потока (5 и 4 группы — ок, 3 группы и заглавные — ошибка); submitter с подменённым `requests`; граница контуров (§4).
- Переводы строк: репо хранит LF (`.gitattributes: * text=auto eol=lf`), `.bat` / `.ps1` — CRLF. В workspace `"files.eol": "\n"`.
- Окружение одно — `.venv_livecraft` (Python 3.13): приложение, тесты и сборка exe. Других окружений не заводить. `requirements.txt` руками не править — это снимок `pip freeze`, который делает `D:\_projects\manage_project.py`; по нему и контролируется инвариант 11.
- Номер версии — только `app\version.py::APP_VERSION`. `.spec`, `.iss` и скрипты своих констант версии не держат; сборка поднимает patch сама.

---

## 12. Как работать с задачами

- Ветка — `feature/livecraft`. Коммиты без trailer `Co-Authored-By`, если задача его не просит. Push и теги — только по задаче.
- Каждая задача приходит со SCOPE LOCK и списком DO NOT TOUCH. Границы — буквально. Файлы вне области не трогать, даже если «заодно». Замеченное вне области — строкой в отчёт, а не правкой.
- **Все технические вопросы агент решает сам, до конца.** Символ не найден — ищет фактический эквивалент `grep`'ом; тест падает — разбирается и чинит в своей области; неясен вызов — читает код и выбирает решение, совместимое с текущим. Наверх выносятся только три вещи: (а) нужно изменить контракт из этого файла; (б) нужно править файл из DO NOT TOUCH; (в) не решается ни одним из испробованных способов — тогда в отчёте перечень попыток. Каждое самостоятельное решение — строкой в отчёте.
- Перед правкой — `grep` по текущему коду на точные имена символов; не писать по памяти.
- Перед правкой назвать объект и поля, которые меняются. Решение «добавить функцию рядом» вместо поля или метода объекта — только с обоснованием в отчёте (§0).
- Порядок: исправления ошибок → переименования → структурные рефакторинги. Не смешивать виды изменений в одной ветке.
- Проверка каждой задачи: полный `pytest -q` до и после плюс задачные проверки. Боевые прогоны делает Артур, не агент.
- В конце задачи — отчёт: каждый изменённый файл и что в нём изменилось; какие объекты и поля добавлены или изменены.
- Все правки — кода, конфигов и документов (`CLAUDE.md`, `README.md`) — делает агент по промту. Артур ставит задачи, контролирует и проверяет; руками ничего не правит.
- Оболочка под вкладкой Code на десктопе — Git Bash: пути вида `D:/_projects/...` и POSIX-инструменты; cmdlet'ов PowerShell там нет.

Команды (PowerShell, из корня репо):

```powershell
.\.venv_livecraft\Scripts\python.exe -m pytest app\tests -q
.\.venv_livecraft\Scripts\python.exe -c "import app.main"
.\.venv_livecraft\Scripts\python.exe -c "import tkinter; print(tkinter.TkVersion)"
.\.venv_livecraft\Scripts\python.exe -m app.main --setup
.\.venv_livecraft\Scripts\python.exe -m app.main --dry-run
.\.venv_livecraft\Scripts\python.exe -m app.main --check
.\.venv_livecraft\Scripts\python.exe -m app.main --auth "@Osvald.X"
```

---

## 13. Этапы

| Этап | Содержание | Готово, когда |
|---|---|---|
| 0 | Этот файл; скелет `app\`, `paths.py`, `version.py`, `logging_setup.py`, `messages_ru.py`, `main.py` с кодами выхода | `python -m app.main --version` работает, `pytest` зелёный на пустом наборе |
| 1 | **Сейф**: `app\secretsafe\` целиком, тесты маскирования и приоритета сейфов, `config\loader.py` (два JSON без умолчаний) | секрет не вытекает ни в один вывод (тесты §7.4); поставочный сейф читается, локальный перекрывает |
| 2 | **Настройщик**: `app\setup\`, три вкладки, запись через `VaultStore` и `render_channels_file` | чистая установка настраивается с нуля через окно, без правки файлов руками |
| 3 | **Контур A**: `sheets\`, `sources\`, `slots\`, пакет `.bcast` (`packages\`), перенос `llm\` и `texts\` из restreamer в ООП-форме; связка с `main` (`--no-llm`) | на боевой таблице режим А кладёт в `bcast\` пакет, который читает `planers\app\package\reader.py`, с корректными `slot_id`, текстами и превью |
| 4 | **Публикация**: Telegram (Bot API через `requests`, группа или личный чат), Google Docs, вкладка «Публикация»; `--announce` | ярлык «объявления» на боевой таблице: Google Doc создан, анонсы и пакет в выбранном чате |
| 5 | **Контур B**: перенос `platforms\`, `form\`, `records\`, `pipeline\`, `output\`, `google\auth.py` из planers; режим Б (`package\reader.py`, `bcast.py`); `--broadcast`, `--from-package`, `--dry-run`, `--status`, `--check`, `--auth` | на тестовом канале: эфир создан, ключ в `keys.txt` и в тренировочной форме; повторный запуск — «уже стояло»; изменённое описание — «исправлено»; удалённый руками эфир восстанавливается режимом Б |
| 6 | Полный цикл одной командой, отчёт и консоль по шести блокам, чистка старья | прогон от таблицы до анонса в Telegram и строки в таблице ответов формы без ручных шагов |
| 7 | **Токен доступа**: `secretsafe\token.py`, вкладка «Токены», уход от `ProgramKey` и `vault.dat` | чистая установка настраивается одной загрузкой пары «токен + ключ»; истёкший или чужой токен не загружается |
| 8 | Сборка: `livecraft.spec` (без `excludes` tkinter; консольный и оконный exe в одной папке), `livecraft.iss` (пять ярлыков, ассоциация `.bcast`), `build_release.bat`, README | получатель без Python ставит программу, загружает токен или вводит своё и проходит сценарий по инструкции |

---

## 14. Решения

Принято при составлении этого файла (20-09-2026) по постановке Артура:

1. **Одна программа, один процесс.** Пакет `*.bcast` как файл пересылки упраздняется; его **схема** сохраняется как контракт объекта `StreamSlot` (§4). Отладочная выгрузка — `--export-slots`. *(Файл пакета возвращён решением 13, `--export-slots` отменён.)*
2. **Контур A берётся из restreamer, контур B — из planers** (§2). Контур B переносится почти без правок: он отлажен живыми прогонами; переписывать его «покрасивее» нельзя.
3. **Анонсы в Google Docs и Telegram в v1 не делаются.** Постановка Артура: «вместо того, чтобы делать объявления сразу будет идти на ютуб каналы и планировать стримы». Тексты merge уходят прямо в название и описание эфира. Если анонсы понадобятся — это отдельный этап после v1, а не встроенная опция; проектировать «на будущее» под них сейчас не надо. *(Отменено решением 14.)*
4. **Конфиги — JSON без умолчаний** (как в planers, задача 5b), не YAML. PyYAML в зависимости не тянем.
5. **Шифруются: ключ OpenAI, id таблицы, диапазон, URL формы** *(URL формы — открытая настройка по решению 15; добавлен токен бота Telegram)*. Токены, `client_secret.json`, названия вопросов формы и каналы — нет (§7.5).
6. **Шифрование поставочных секретов — защита от копирования, а не от специалиста** (§7.2). Формулировка в README и в настройщике обязана это отражать.
7. **Настройщик — Tkinter, не браузер** (§8.1): браузерная форма показывает и сохраняет ровно те значения, которые §7 прячет.
8. **Два субъекта Google-авторизации с раздельными токенами и минимальными скоупами** (§9).
9. **Ключ локального сейфа лежит в самом `vault.local.dat`** — 32 случайных байта, завёрнутые DPAPI, записью `"key"` рядом с `salt` и `fields` (§7.3). Принято Артуром 20-09-2026. Отклонено: отдельный файл `secrets\vault.local.key` — он не описан в §5 и два файла можно рассинхронизировать, а потерянный ключ превращает второй файл в мусор без внятной ошибки. К настройке на машине пользователя решение ничего не добавляет: DPAPI — штатная часть Windows, пароля не спрашивает и настройки не требует.
10. **`tzdata` входит в разрешённый набор инварианта 11.** Принято Артуром 21-09-2026. Причина: на Windows у Python нет своей базы часовых поясов, а контур A обязан переводить дату и время ряда таблицы по Киеву в `start` слота с правильным смещением, в том числе на стыке летнего и зимнего времени. restreamer в боевой работе опирался на тот же пакет (`tzdata==2025.3`). Отклонено: зашить в код правило перехода «последнее воскресенье марта и октября», как planers поступил с квотными сутками, — правило перехода в Украине может поменяться законом, пакет обновляется вслед за ним, а зашитый код — нет, и две недели в году эфиры встали бы на час мимо.
11. **Окно настройщика открывает только значения, которые пользователь ввёл сам.** Принято Артуром 23-09-2026. Кнопка «показать своё» — четвёртая точка `reveal()` (§7.4): только поле с происхождением «своё», только по нажатию, только на экран. Поставочное значение не показывается никому, в том числе на машине, где сделана сборка: Артур задаёт и проверяет поставочные значения сам перед сборкой и на локальной сборке, смотреть их в окне незачем. Правило однозначное и не зависит от машины. Отклонено: признак «машина сборки» (в раздаваемом exe появился бы путь раскрытия поставочных значений, закрытый одной проверкой) и сборка `vault.dat` из личного сейфа Артура (не нужна при этом правиле; откуда `build_release.bat` берёт поставочные значения, решается на этапе 6 — они не должны попадать в git открытым текстом).
12. **Язык ряда определяет программа по источнику; колонку языка таблицы она не читает.** Принято Артуром 24-09-2026. Таблица — не первоисточник языка, а запись для человека: брать язык оттуда значит переложить работу программы на оператора и заставить его следить за колонкой. Язык определяется один раз по данным видео (правила restreamer `app\core\language*.py`), записывается полем в объект источника и дальше переходит в `StreamSlot.language`; нигде заново не вычисляется. Не определился — источник дальше не идёт, причина — в отчёте. Отклонено: колонка `L` как первичный источник или подсказка.
13. **Пакет `*.bcast` возвращается как локальный вывод запуска.** Принято Артуром 24-09-2026 (отменяет в решении 1 упразднение файла пакета). Каждый запуск контура A пишет архив `plan_*.bcast` (ZIP: `manifest.json` + `previews/`) в папку `bcast\` в корне программы; формат — ровно тот, что читает `planers\app\package\reader.py::read_package` (schema_version 1), чтобы пакет можно было отдать planers на другой машине. Ключ `--export-slots` с JSON отменяется: пакет и есть выгрузка слотов. Ссылка на форму ключей лежит в манифесте открытым текстом — без неё planers пакет не читает.
14. **Анонсы в Telegram и Google Docs входят в livecraft.** Принято Артуром 24-09-2026 (отменяет решение 3). Цель — один проект вместо restreamer и planers. Отправка в Telegram — HTTP-запросами Bot API через `requests` (aiogram не нужен: программа только отправляет, команд не принимает; интерактивный бот restreamer не переносится); Google Docs — через `google-api-python-client`. Инвариант 11 не меняется. Порядок: контур A с merge → этап «Публикация» (пакет в Telegram, анонсы в Telegram, Google Doc) → контур B: после публикации livecraft заменяет restreamer, planers работает на его `.bcast`. Каждый вывод (пакет, Telegram, Doc) включается в `livecraft.json` явно.
15. **Ссылка на форму ключей и ссылка на папку Google Docs — открытые настройки, не секреты.** Принято Артуром 24-09-2026 (меняет §7.5 для URL формы). Обе хранятся в `livecraft.json` и правятся в настройщике; документ Google Docs программа создаёт сама по ссылке на папку. В сейфе остаются значения, которые действительно нельзя показывать: ключ OpenAI, id таблицы, диапазон, токен бота Telegram.
16. **Поставочные значения выдаются парой файлов «токен + ключ»; программа не несёт секретов.** Принято Артуром 24-09-2026 (меняет §7.3: `ProgramKey` и вшитый `vault.dat` уходят из сборки). Одна программа, один установщик, ярлыки «Livecraft» (запуск; ненастроенная программа открывает окно настройки) и «Livecraft — настройка». В настройщике — вкладка «Токены».
    - «Создать токен» кладёт в `tokens\` два двоичных файла: токен `*.lctoken` (магия, версия, id пары, nonce, AES-GCM-шифротекст значений) и ключ `*.lckey` (магия, версия, тот же id пары, 32 случайных байта). В блокноте оба — нечитаемые байты; пароля человек не придумывает и не вводит. Передавать разными путями.
    - «Загрузить токен» принимает пути к этим двум файлам; чужой ключ к токену — отказ «ключ не от этого токена». Значения один раз перекладываются в личный сейф (DPAPI) с происхождением «из токена»; дальше файлы не нужны.
    - Срок активации зашит в токен: момент создания и «действует до» (создатель задаёт число суток, по умолчанию 3). Время берётся не с часов машины, а из заголовка `Date` ответа сервера Google по HTTPS (`requests`) — и при создании, и при загрузке; нет сети — загрузка не выполняется, причина названа. Уже загруженные значения работают и после срока. Защита — от пересылки пары дальше, не от специалиста (§7.2): кто владеет обоими файлами, может переписать срок, только написав свой код.
    - В токен попадают только значения, которые пользователь ввёл сам; значения «из токена» не экспортируются никогда (продолжение решения 11). Каналы и токены входа Google в токен не входят. Какие функции доступны, программа решает по тому, что настроено, а не признаком в токене. Отзыв доступа — отдельный ключ OpenAI на получателя, его отключение.
    - Шифрование — `cryptography` (AES-GCM, ключ — случайные байты файла ключа, scrypt не нужен); новых зависимостей нет.
17. **Режим задаётся ярлыком.** Принято Артуром 24-09-2026. Режим А (таблица → merge → слоты в памяти → пакет в `bcast\`) с тремя вариантами из памяти: «объявления» (`--announce`: Google Doc, анонсы и пакет в Telegram), «эфиры» (`--broadcast`), «всё» (без аргументов: объявления, затем эфиры). Пакет в режиме А пишется всегда и обратно не читается. Часть режима не готова — остальное делается, строка с точным действием, код 1; не готово ничего — код 2 (§10). Отклонено: переключатели выводов в настройках — ярлык проще и нагляднее.
18. **Режим Б — эфиры из пакетов.** Принято Артуром 24-09-2026. `--from-package` (ярлык «эфиры из пакетов») читает пакеты из `bcast\` по правилам `planers\app\package\` (все пакеты — одна карта слотов, новее побеждает), собирает `StreamSlot` и планирует эфиры тем же кодом, что вариант «эфиры»; таблица и нейросеть не нужны. Зачем: владелец канала без таблицы и OpenAI получает эфиры из присланного пакета (planers закрывается), а Артур восстанавливает удалённые руками эфиры без нового merge сколько угодно раз. Двойной щелчок по `.bcast` копирует пакет в `bcast\` и запускает режим Б (ассоциация ставится установщиком).
19. **Объявления уходят в группу или в личный чат с ботом — по выбору в настройщике.** Принято Артуром 24-09-2026. Группа — чтобы видели все её участники; личный чат — для отладки или для тех, кому группа не нужна. Id чатов — открытые настройки `livecraft.json`; программа узнаёт их сама кнопкой «Найти чаты» (Bot API `getUpdates`), человек выбирает из списка. Токен бота — в сейфе.
20. **Кнопка «Отправить логи» в настройщике.** Принято 24-09-2026 (просьба Артура, способ выбрал Коворк). Кнопка внизу окна, на всех вкладках: программа сама собирает архив `logs\` (новые файлы первыми, до 45 МБ — предел Telegram 50 МБ) плюс `diagnostics.txt` (версия, сборка Windows, что настроено и что нет — без значений сейфа) и шлёт документом (`sendDocument`) в чат поддержки — открытая настройка `support_chat_id`, бот — тот же токен бота. Токен доступа (решение 16) может нести бота и чат поддержки: получатель жмёт кнопку, логи приходят Артуру. Нет бота или чата — кнопка «Сохранить архив логов» кладёт zip на рабочий стол и открывает папку. В архив никогда не входят `secrets\`, `keystreams\`, `tokens\`. Этап — «Публикация» (там появляется клиент Telegram). Отклонено: почта — нужен SMTP-логин и пароль приложения отправителя на машине каждого пользователя, то есть ещё один секрет и ещё одна настройка.
21. **Один канал — один язык.** Принято Артуром 24-09-2026. В настройщике язык канала выбирается одним выпадающим полем с поиском (задача 3.7b). Формат `channels.json` не меняется: `languages` — список из одного кода (совместимость с planers и запас на будущее); канал с несколькими кодами в старом файле показывается первым кодом с предупреждением и при пересохранении получает один. Если понадобится несколько языков на канал — отдельное решение.
22. **Нейросеть — за разъёмом `LlmBackend`.** Принято Артуром 25-09-2026 (задача 3.10a). Merge, выбор модели и прогон знают только общий запрос, ответ, отказ и расход (`app\llm\backend.py`, `errors.py`, `usage.py`, `selection.py`); тарифы, цены, flex, reasoning, заголовки лимитов и разбор исключений SDK живут в реализации `app\llm\backends\openai*.py`. Вторая нейросеть — новая реализация рядом, без правки merge. Выбор реализации настройкой появится вместе со второй реализацией. Отклонено: абстракция провайдера донора (`providers\provider_base.py`) — она протекала тарифами OpenAI в merge.
23. **Merge переносится «поведение как есть, форма — объекты».** Принято Артуром 25-09-2026 (меняет в §2 «ядро как есть»). Правила, пороги, лексиконы, тексты промтов и порядок проверок — ровно донорские; свободные функции `app\llm\merges\` и причины отказа, закодированные текстом `RuntimeError` и разбираемые обратно по подстрокам (`merge_validation.py::_reason_code_from_error`), становятся методами объектов и перечислением причин. Встроенные в код списки донора (например, `merge_validation_helpers.py::_BAD_HOOK_PATTERNS`) выносятся файлами `app\resources\text\` без правки строк. Доказательство совпадения обязательно в каждой задаче переноса: тесты донора на это поведение переносятся в livecraft, а временный скрипт сверки (в git не входит) прогоняет донорскую функцию из `.venv_restreamer` и объект livecraft на одних входах — входы тестов донора плюс сгенерированные; расхождение допустимо только как исправление ошибки донора, каждое — строкой в отчёте задачи.

---

## 15. Открытые проверки

Каждая закрывается экспериментом до реализации соответствующего модуля; результат — строкой сюда.

- **Закрыто 20-09-2026:** `tkinter` в `.venv_livecraft` есть, `tkinter.TkVersion` = 8.6. Решение §8.1 (настройщик на Tkinter) выполнимо без переустановки Python.
- **[ПРОВЕРИТЬ]** Вес one-folder сборки с Tcl/Tk против сборки без него (снять `"tkinter"` из `excludes` и сравнить `dist\`). Если рост окажется неприемлемым — вынести настройщик отдельным маленьким exe из того же кода, а не менять решение §8.1 на браузер.
- **Закрыто наполовину 20-09-2026:** DPAPI через `ctypes` на Windows 11 Home (build 26200) работает — `CryptProtectData` без `CRYPTPROTECT_LOCAL_MACHINE` дал блоб 288 байт из 41 байта входа, исходных байтов в блобе нет, `CryptUnprotectData` тем же пользователем вернул те же байты, `szDataDescr` прочиталось как `Livecraft local vault`. **Остаётся открытым:** отказ под другим пользователем Windows — нужна вторая учётная запись, проверяет Артур.
- **Закрыто 20-09-2026 решением Артура:** ключ локального сейфа лежит записью `"key"` в самом `vault.local.dat` (§7.3, §14 решение 9).
- **Закрыто наполовину 24-09-2026 (боевая проверка 3.2):** таблица плана выдана поимённо — вход чужим аккаунтом получил 403. Для таблицы скрытая ссылка не защищает ничего, защищает доступ Google (§7.2, второй абзац). **Остаётся открытым:** режим доступа к боевой форме ключей.
- **[ПРОВЕРИТЬ]** Что у слота с несколькими источниками уходит в `sources` и какое превью выбирается, когда источники разного качества, — правило выбора сегодня «одно случайное» (`planers`, решение 8); для эфира, возможно, нужно «первое по времени».
- **Закрыто 24-09-2026 (задача 3.5):** лимиты YouTube Data API v3 — название ≤100 символов, описание ≤5000 **байт**, символы `<` `>` запрещены; `SlotTexts.for_youtube` заменяет `<` `>` на `‹` `›` и режет по границе слова в байтах. В planers предел описания — 5000 символов (`planers\app\platforms\youtube.py::YOUTUBE_DESCRIPTION_MAX_CHARS`): при переносе контура B сравнение спек ведётся по правилу `SlotTexts`, а не по символам.
- **Закрыто 21-09-2026 фактами planers (задача 5p):** стоимости вызовов YouTube Data API — в §9.
- **Закрыто 21-09-2026:** `tzdata` добавлен в инвариант 11 (§14 решение 10) и стоит в `.venv_livecraft` (`tzdata==2026.4`, база IANA `2026d`); `LivecraftSettings` проверяет зону и отдаёт её свойством `zone` (задача 1.6). Проверено: Киев 15-01-2027 12:00 — `+02:00`, 15-07-2027 12:00 — `+03:00`.
- **[ПРОВЕРИТЬ на этапе сборки]** Папка `app\resources\text` попадает в datas PyInstaller: `TextResource` в собранном exe ищет её в `sys._MEIPASS\app\resources\text`; без неё определение языка не стартует.
- **[ПРОВЕРИТЬ на этапе сборки]** База часовых поясов попадает в сборку PyInstaller: собранный `livecraft.exe` на машине без Python читает `ZoneInfo("Europe/Kyiv")`.
- **[ПРОВЕРИТЬ на этапе «Публикация»]** Минимальные скоупы для Google Docs по ссылке на папку: `drive` — restricted-скоуп (при верификации приложения нужна проверка безопасности); хватит ли `drive.file` + `documents`, если документ создаётся в папке, которую программа не создавала, — пробником до реализации.
- **[ПРОВЕРИТЬ на этапе «Публикация»]** `getUpdates` отдаёт чаты, только пока у бота не настроен webhook и обновления не забраны другим процессом: если тот же бот сейчас работает у restreamer, «Найти чаты» может вернуть пусто — проверить пробником и назвать причину в окне.
- **[ПРОВЕРИТЬ на этапе сборки]** Консольный и оконный exe в одной папке one-folder на общих библиотеках (два `EXE` в одном `COLLECT` PyInstaller); ассоциация `.bcast` и пять ярлыков в Inno Setup.
- Наследуются от planers и остаются открытыми: свой проект Google Cloud и свой OAuth-клиент; верификация приложения под `auth/youtube`; формат `snippet.customUrl` в живом прогоне; запасной путь «отмена + пересоздание», если для какого-то поля `liveBroadcasts.update` окажется невозможен.

---

## 16. Текущее состояние

- Исходные репозитории на дату разбора: `restreamer` — коммит `3f10426` (17-09-2026), ветка разработки `feature/restreamer`, baseline тестов 700; `broadcaster` — `efcbb3e` (09-09-2026), сборочный чекаут; `planers` — `9af58a6` (19-09-2026), ветка `feature/planers`, этап 5 в работе.
- **Этап 0 — задача 0.1 принята 20-09-2026, коммит `af998b3` в `feature/livecraft`.** Есть: `.gitattributes`, `livecraft.bat`, `requirements.txt` снимком заполненного окружения (все пакеты инварианта 11 плюс `pytest` и `pyinstaller`), `app\version.py` (`APP_VERSION = "0.1.0"`), `app\paths.py` (`LivecraftPaths` — все файлы и папки §5 и §7.3, `resolve_root` по `LIVECRAFT_ROOT`, `write_text_atomically`), `app\core\dates.py`, `app\observability\logging_setup.py` (логгер `livecraft`, файл `{stamp}_livecraft.log`, `mask_stream_key`), `app\ui\messages_ru.py`, `app\main.py` (`ExitCode`, `RunRequest`, разбор всех флагов §10). Baseline тестов livecraft — **101**.
- Пока нет сейфа и загрузчика конфига, любой режим, кроме `--version`, печатает `SETUP_REQUIRED` и возвращает код 2 (`app\main.py::_run`). Настоящую проверку сейфа и конфига эта ветка получает в задаче 1.3.
- `ExitCode` временно живёт в `app\main.py`; переезжает в `app\pipeline\runner.py` вместе с `decide_exit` и `RunExit` на этапе 4.
- **Этап 0 закрыт. Задача 0.2 принята 20-09-2026, коммит `964693e`.** Добавлен пакет `app\runtime\`: `single_instance.py` с объектами `LockOwner` (запись владельца, правила `parse` / `render` / `is_known` / `is_alive`) и `InstanceLock` (поля `path`, `startup_log`, `pid`; `acquire` / `release`), исключение `AnotherInstanceRunning`. Замок берётся в `run_cli` после `ensure_dirs` и до `setup_logging`, снимается в `finally` после `close_logging`; конфликт — `LOCK_REJECTED` в stderr, событие `lock_rejected` в `logs\startup.log`, код 1. Долг §11 из задачи 0.1 закрыт: подписи значений справки переехали в `messages_ru` (`CLI_METAVAR_PATH`, `CLI_METAVAR_HANDLE`). Baseline тестов livecraft — **137**.
- Решения по замку, принятые при реализации и проверенные тестами: время в файле замка и в `startup.log` — в формате инварианта 4, а не ISO донора; однотокенная запись владельца считается нечитаемой и снимается как застарелая; `PermissionError` при `os.open(O_EXCL)` трактуется как «путь занят» и ведёт к отказу запуска (fail-closed), причина уходит в `startup.log` строкой `lock_create_denied`.
- Известные мелкие долги (чинятся, когда задача откроет соответствующий файл): отметка времени в `logs\startup.log` — с точностью до минуты, потому что в `app\core\dates.py` нет формата с секундами; на путях сбоя замка `LOGGER.warning` срабатывает до настройки логов и уходит английской строкой в stderr через `logging.lastResort`; фикстуры `dead_pid` и `live_foreign_process` продублированы в `app\tests\test_main.py` и `app\tests\test_runtime_single_instance.py` вместо `conftest.py`.
- **Задача 1.1 принята 20-09-2026, коммит `f6bd784`.** Пакет `app\secretsafe\`: `value.py` (`SecretField` с ярлыками и стилем маски, `MaskStyle`, `SecretValue` с `reveal` / `masked` / `fingerprint` / `log_label` и переопределёнными `__str__`, `__repr__`, `__format__`), `crypto.py` (`EncryptedField`, `VaultFile`, `VaultCrypto`, `VaultFormatError`, `VaultDecryptError`; AES-256-GCM, HKDF-SHA256, AAD = имя поля плюс версия формата), `dpapi.py` (`Dpapi`, `DpapiUnavailable`; `CRYPTPROTECT_LOCAL_MACHINE` не ставится). Baseline тестов livecraft — **287**.
- Проверено на приёмке: слово `reveal` встречается в `app\secretsafe\` ровно один раз — в объявлении метода; секрет даёт маску в `f"{...}"`, `f"{secret:>40}"`, `logging`, `repr` объекта-владельца, трассировке и в записи чужого логгера (`googleapiclient.discovery`); блоб чужого поля не расшифровывается; `"version": 1.0` и `"version": true` отвергаются как чужой формат.
- **Задача 1.2 принята 20-09-2026, коммит `ca614f5`.** `app\secretsafe\vault.py`: `VaultOrigin` (`supplied` / `own`), `VaultEntry`, `Vault` (`missing`, `is_ready`, `admission_reason`, `secrets`, `with_field`, `without_field`, `log_line`) — объект неизменяемый, файлов не знает. `SecretValue.scrub(text)` вычёркивает своё значение из готовой строки, подставляя `log_label` — значение при этом не покидает объект. В `app\observability\logging_setup.py` появились `SecretScrubber(logging.Filter)` и `install_secret_filter(vault)`. Baseline тестов livecraft — **342**.
- Фильтр чистит готовый текст записи и обнуляет `record.args`; при несошедшихся шаблоне и аргументах чистит `msg` и `args` по отдельности, потому что их печатает в stderr `logging.Handler.handleError`. Обработчики собираются со сверкой по идентичности: `_attach_third_party` вешает на корневой логгер те же объекты, и без сверки фильтр висел бы дважды.
- `install_secret_filter` пока никто не зовёт: сейф открывается в `main.run_cli` вместе с загрузчиком конфига (задача 1.4).
- Известный долг слоёв: `app\observability\logging_setup.py` импортирует `SecretValue` и `Vault` в рантайме, а `app\secretsafe\dpapi.py` импортирует оттуда `get_logger`. Кольца сегодня нет (`value.py` и `vault.py` логгер не трогают), но оно появится, как только тронут. Чинится в задаче 1.3 переносом обоих импортов под `TYPE_CHECKING`.
- **Задача 1.3 принята 20-09-2026, коммит `05b352e`.** `app\secretsafe\store.py`: `VaultSource`, `ProgramKey` (поле `material`; сначала сгенерированный сборкой модуль `app.secretsafe.program_key` с `PARTS: tuple[bytes, ...]`, затем `secrets\program.key` сырыми байтами или base64), `VaultStore` (поля `supplied_path`, `local_path`, `program_key`, `dpapi`; `load`, `save_local`). `VaultFile` получил поле `wrapped_key` и запись `"key"`. Импорты сейфа в `logging_setup.py` уехали под `TYPE_CHECKING` — долг слоёв закрыт; дублированные тестовые фикстуры сведены в `conftest.py` — долг закрыт. Baseline тестов livecraft — **406**.
- Правила чтения, принятые при реализации: любое поле, которое не расшифровалось, делает файл нечитаемым целиком (половина полей из явно испорченного файла — это работа на непонятно чьих значениях); чужой формат файла (версия, битый base64) уходит `VaultFormatError` наружу и по §7.3 станет кодом 2; отсутствие ключа, отказ DPAPI и неизвестные имена полей запуск не валят; битый сгенерированный модуль ключа равнозначен его отсутствию и откатывается к `secrets\program.key`.
- **Задача 1.3a принята 20-09-2026, коммит `82d62e8`.** Долг §7.4 закрыт: шифрование поля стало правилом самого секрета (`SecretValue.encrypt(crypto)`, типы `VaultCrypto` и `EncryptedField` под `TYPE_CHECKING`), значение объекта не покидает, `Select-String reveal` по `app\secretsafe\` снова даёт **одно** совпадение — так и должно оставаться до появления трёх точек применения §7.4. Заодно привязка блоба к месту (AAD) теперь берётся из самого секрета, а не из аргумента вызывающего. Baseline тестов livecraft — **427**.
- **Требование к задаче связки с `main`:** когда локальный сейф не читается, оператор должен получить явную русскую строку о том, что его личные настройки не прочитаны и программа работает на поставочных значениях. Молчаливый откат недопустим: иначе получатель незаметно работает на ключе OpenAI Артура и на его таблице.
- **Задача 1.4 принята 21-09-2026, коммит `bf8150d`.** `app\config\loader.py`: `Platform` (только `youtube`), `Privacy`, `ReasoningEffort`, `ServiceTier`, `ConfigProblem`, `ConfigError`, `SettingProblem`, `ChannelConfig`, `LlmSettings`, `FormSettings` (девять ключей вопросов, варианты, формат даты; своё правило `problem`), `LivecraftSettings` (десять полей, `youtube_pause_seconds: float`), `LivecraftConfig` (`served_languages`, `channel_by_handle`, `auth_targets`); `load_livecraft_config`, `render_channels_file`, `save_channels_file`. В git — `secrets\livecraft.json` (без секретов) и `app\examples\channels.example.json`. Загрузчик сейф не импортирует. Baseline тестов livecraft — **600**.
- Решения задачи 1.4: `youtube_pause_seconds` в поставке **0.5**, а не 2 — так в текущем `planers\secrets\planer.json` (planers 5p, решение 21-09-2026 по замерам); `save_channels_file` копирует прежний файл в `channels.previous.json`, только если он есть (донор упал бы на чистой установке); шаблон превью — относительный, только с `{date}` и `{language}`; шаблон настроек в `messages_ru` совпадает с поставочным файлом — это держит тест.
- Долги после 1.4: `normalize_handle` временно живёт в `app\config\loader.py` (её дом по §5 — `core\text.py`, переедет с `token_file_stem` в контуре B); `AUTH_ALL` объявлен дважды — в `main.py` и в `loader.py` (§11: один источник; чинится задачей 1.5); часовой пояс не проверяется на читаемость — ждёт решения по `tzdata` (§15).
- **planers ушёл вперёд после разбора:** HEAD `9903b2e` (21-09-2026) против `9af58a6` на дату составления этого файла — шесть коммитов (5o-A, 5o-B, 5p и две сборки). Появились `app\observability\run_stats.py`, `app\records\run_record.py` (таблица `runs`, `schema_version` 2), дробная пауза, цены квоты по факту, `core\dates.py::format_datetime_seconds`. planers развивается параллельно: перед каждой задачей этапа 4 его исходники перечитываются по текущему HEAD, а не по этому файлу. `format_datetime_seconds` закрывает долг «`startup.log` с точностью до минуты» — переносится с `core\dates.py` в задаче 3.1.
- **Задача 1.5 принята 21-09-2026, коммит `5480afd`.** `app\setup\readiness.py::Readiness` (поля `paths`, `config`, `config_error`, `vault_load`, `vault_error`; `check`, `is_ready`, `problems`, `template_lines`, `warnings`, `summary_lines`, `log_line`) — объект готовности: читает сейф и оба конфига, сам отвечает, готово ли и что сказать, ничего не печатает. В `store.py`: `LocalVaultState` (`absent` / `read` / `unreadable`), `VaultLoad`, `VaultStore.open(paths)`, имя файла в `VaultFormatError`. `main._run` стал настоящим: не готово — проблемы, шаблон сломанного конфига и `SETUP_REQUIRED`, код 2; готово — сводка без значений, код 0; `--setup` не останавливается проверкой и показывает сводку. `install_secret_filter` ставится сразу после чтения сейфа — проверено тестом, который падает без этой строки. Нечитаемый личный сейф — строка «ВНИМАНИЕ» в консоли и `vault_local_unreadable` в логе. `AUTH_ALL` — один источник, `app\config\loader.py`. Baseline тестов livecraft — **639**.
- `load_livecraft_config` читает `channels.json` раньше `livecraft.json` (записанное решение загрузчика: ошибка вероятнее в том, что заполняет человек), поэтому на пустом корне первым называется `channels.json`. На настоящей установке `livecraft.json` приходит со сборкой.
- **Дефект, найденный на приёмке 1.5:** `VaultStore._read_file` отвечает «файла нет» не только на `FileNotFoundError`, но и на любую другую `OSError` и на `UnicodeDecodeError`; `_read_local` превращает это в `LocalVaultState.ABSENT`. Существующий, но нечитаемый `vault.local.dat` (блокировка антивирусом или синхронизацией, чужие байты) даёт молчаливый откат на поставочные значения — ровно то, что запрещает требование выше. Чинится задачей 1.5a: повреждённый или не открывающийся файл сейфа — `VaultFormatError` с именем файла и код 2, как любой другой повреждённый файл сейфа; «целый, но не наш» (DPAPI не развернул) остаётся предупреждением.
- Мелкий долг для этапа 2: на консоли шаблон сломанного конфига печатается после всех проблем, и между ошибкой `channels.json` и её шаблоном встаёт причина от сейфа; читалось бы лучше, если бы шаблон шёл сразу за своей ошибкой.
- **Задача 1.5a принята 21-09-2026, коммит `8f97fc4`.** Дефект закрыт: `VaultStore._read_file` возвращает `None` только при `FileNotFoundError`; любая другая `OSError` и `UnicodeDecodeError` — `VaultFormatError` с именем файла и короткой причиной без пути, исходная ошибка через `from`, дальше `Readiness.vault_error` и код 2. «Целый, но чужой» личный файл (нет `"key"`, DPAPI отказал, блоб не расшифровался) по-прежнему `LocalVaultState.UNREADABLE`, предупреждение и работа на поставке. `VAULT_NOT_READY` больше не советует `--setup` — совет один, `SETUP_REQUIRED` из `main`. Новые тесты проверены на старом коде: все 7 падают. Baseline тестов livecraft — **647**.
- Мелкий долг dev-режима: `ProgramKey._from_dev_file` на любую `OSError`, кроме «нет файла», тоже отвечает «ключа нет» — поставочный сейф не читается, а причина названа как «не хватает полей». В сборке ключ приходит из сгенерированного модуля, поэтому на получателя не влияет; чинится, когда задача откроет `store.py`.
- **Задача 1.6 принята 21-09-2026, коммит `fb47dca`. Этап 1 закрыт.** `requirements.txt` — одна новая строка `tzdata==2026.4`, LF. `LivecraftSettings.zone -> ZoneInfo` — зону программы контур A берёт у настроек, а не строит сам; `LivecraftSettings.problem` отдаёт первую из двух проблем (`_image_template_problem`, `_timezone_problem`). Проверка зоны двойная: `ZoneInfo` перехватывается по `ZoneInfoNotFoundError` и `ValueError`, а затем имя сверяется с `available_timezones()` — на Windows `ZoneInfo` открывает и `Europe\Kyiv`, и `Europe/Kyiv ` с пробелом, потому что файловая система прощает неканонические имена. Baseline тестов livecraft — **661**.
- **Итог этапа 1:** программа при каждом запуске читает оба файла сейфа и оба конфига, называет точно, чего не хватает (с шаблоном сломанного конфига), громко предупреждает о нечитаемом личном сейфе, останавливается с кодом 2 на повреждённом файле сейфа и ставит фильтр секретов в логах сразу после чтения сейфа. Значение секрета не покидает `SecretValue`: `reveal` в `app\secretsafe\` объявлен и ни разу не вызван.
- **Нарезка этапа 2 (настройщик, §8):** 2.1 — модель вкладки «Ключи и ссылки» без окна (поля сейфа: происхождение, маска, «заменить своим», «сбросить к поставке», проверка формата, запись через `VaultStore.save_local`); 2.2 — модели вкладок «Каналы» и «Настройки запуска» без окна плюс запись `livecraft.json` тем же загрузчиком (`render_settings_file`); 2.3 — окно Tk поверх трёх моделей и запуск по `--setup`; 2.4 — живые проверки по кнопкам («проверить ключ / таблицу / форму», «войти в канал», «проверить все») — после этапа 4, когда появятся клиенты. Модели не импортируют `tkinter` и тестируются без окна (§8.2, последнее правило).
- **Задача 2.1 принята 23-09-2026, коммит `f5262e6`.** `app\secretsafe\store.py`: `VaultLoad` получил поле `supplied` (поставочный слой как прочитан), свойство `own` и classmethod `from_layers` — единственное место правила «личное перекрывает поставочное»; `VaultStore.can_save_local`. `app\setup\validators.py` (`extract_spreadsheet_id`, `is_a1_range` — чистые разборы строки), `app\setup\fields\secret_input.py::SecretInput` (правила четырёх полей — методы объекта; в сейф таблицы уходит id, а не ссылка; `raw` скрыт из `repr`, введённое в тексты проблем не подставляется), `app\setup\panels\keys_panel.py`: `RowAction`, `KeyRow`, `KeysPanelEdit` (негодный ввод — обычный исход, не исключение: вкладка та же, проблема названа), `KeysPanel` (поле `loaded_own` для `is_dirty`; `replace`, `reset`, `save`; `display` — всегда маска). Baseline тестов livecraft — **787**.
- Проверено на приёмке: `tkinter` в `app\setup\` — ноль; вызовов `reveal()` в `app\setup\` нет (без учёта регистра находится только имя `RowAction.REVEAL`, значение `"show_own"`); `reveal` в `app\secretsafe\` — одно объявление; тесты вкладки проверяют отсутствие значений сейфа во всех строках по `reveal()` как эталону.
- **Не сделано в 2.1 по вине промта:** правка `ProgramKey._from_dev_file` (нечитаемый `secrets\program.key` → `VaultFormatError`) противоречит тесту `app\tests\test_secretsafe_program_key.py::test_a_directory_in_place_of_the_key_file_counts_as_absent`, которого не было в SCOPE LOCK. Codex правильно остановился. Долг dev-режима остаётся открытым и закрывается задачей 2.1a (правка кода и замена этого теста).
- **Задача 2.1a принята 23-09-2026, коммит `01d9d90`.** `ProgramKey._from_dev_file`: файл есть, но не открывается — `VaultFormatError` с именем `program.key`, исходная ошибка через `from`, дальше код 2 через `Readiness`; строка лога `program_key_file_unreadable` убрана. Тест «папка вместо ключа — ключа нет» заменён двумя (прямой и через `VaultStore.open`). Долг dev-режима закрыт. Baseline тестов livecraft — **788**.
- Решения к задаче 2.2 (Коворк, 23-09-2026): вкладки «Каналы» и «Настройки запуска» своих правил проверки не держат — черновик вкладки переводится в данные JSON и разбирается тем же `_ConfigParser`, что читает файлы (публичные `parse_channels` / `parse_settings`); ошибка разбора (`ConfigError.key_path`, `ConfigError.problem`) и есть проблема поля. Контракт формы (`form`) на вкладке не правится и переносится при записи как есть. Нет или не читается `livecraft.json` — вкладка открывается на шаблоне `CONFIG_SETTINGS_TEMPLATE` (тест держит его равным поставочному файлу) с явной строкой об этом и пишет файл только по кнопке «сохранить»; загрузчик по-прежнему умолчаний не подставляет.
- **Задача 2.2 принята 23-09-2026, коммит `c603623`.** `app\config\loader.py`: `to_data()` у `ChannelConfig`, `LlmSettings`, `FormSettings`, `LivecraftSettings`; публичные `parse_settings(raw, path)` и `parse_channels(raw, path)` (разбор уже прочитанных данных тем же `_ConfigParser`); `render_settings_file` (побайтно равен поставочному файлу на поставочных данных) и `save_settings_file` (атомарно, без копии прежнего). `app\setup\fields\channel_draft.py::ChannelDraft`, `settings_draft.py::SettingsDraft` — черновики текстом со своим `to_data` и без своих проверок; `app\setup\panels\channels_panel.py` (`ChannelsPanelEdit`, `ChannelsPanel`: `add` / `update` / `remove` / `save`, поля `loaded`, `load_problem`, `is_file_missing`, `channels_file`) и `settings_panel.py` (`SettingsPanelEdit`, `SettingsPanel`: `apply` / `save`, шаблон программы при нечитаемом файле). Проверку ввода обеих вкладок делает загрузчик. Baseline тестов livecraft — **838**.
- **Дефект, найденный Codex в 2.2:** `_ConfigParser._number` пропускает `NaN` и `Infinity` в `youtube_pause_seconds` (`json.loads` их принимает, `nan < 0` ложно), и тогда `render_settings_file` пишет нестандартный JSON. Вкладка от этого защищена, файл, исправленный руками, — нет. Чинится задачей 2.2a.
- Мелочь, не дефект: `youtube_pause_seconds` хранится как `float`, поэтому целое `2` в файле после записи настройщиком станет `2.0` — значение то же.
- **Задача 2.2a принята 23-09-2026, коммит `200f242`.** `_ConfigParser._number` проверяет по порядку тип, конечность (`math.isfinite`, текст `CONFIG_PROBLEM_NUMBER_FINITE`) и минимум; `render_settings_file` пишет с `allow_nan=False`. Дефект NaN закрыт. Baseline тестов livecraft — **844**.
- Нарезка 2.3 уточнена (Коворк, 23-09-2026): 2.3 — окно Tk на трёх вкладках поверх моделей (`app\setup\app.py`, `app\setup\tabs\`) и запуск по `--setup`, **без** кнопки «показать своё»; 2.3a — кнопка «показать своё» по §14 решение 11. Окно не держит правил: всё решают модели 2.1–2.2 и `Readiness`.
- **Задача 2.3 принята 23-09-2026, коммит `7d0464f`.** `app\setup\app.py`: `SetupApp` (DPI до создания Tk, окно, цикл событий) и `SetupWindow` (три вкладки, строка готовности по `Readiness.check`, вопрос при закрытии с несохранённым). `app\setup\tabs\`: `ProblemLine`, `KeyRowView` и `KeysTab` (ввод своего значения — поле с `show="•"`, в виджеты попадает только маска; файл сейфа чужого формата — причина на вкладке, правка выключена), `ChannelsTab`, `SettingsTab`. `app\main.py::_run_setup`: `--setup` открывает окно; `TclError` — строка в консоль, `setup_window_failed` в лог, код 1; обычный запуск tkinter не импортирует (тест в отдельном процессе). Baseline тестов livecraft — **869**.
- Тесты окна создают настоящее окно Tk внутри `capsys.disabled()`: перехват вывода pytest на уровне дескрипторов на Windows иногда ломал инициализацию Tcl (`init.tcl: couldn't read file`). Боевой код ради этого не менялся.
- Порядок в истории: коммит Коворка с CLAUDE.md (решение 11) был сделан через `commit -a`, пока Codex работал над 2.3, и захватил три его файла. До push история переписана: `f9bc9aa` — только CLAUDE.md, `7d0464f` — вся задача 2.3; дерево совпадает с прежним побайтно. Правило Коворка с этого места: CLAUDE.md коммитится только явным путём (`git commit -- CLAUDE.md`) и только когда Codex не работает.
- **Задача 2.3a принята 23-09-2026, коммит `fe873e6`.** `KeysPanel.own_value(field)` — единственный вызов `reveal()` в `app\setup\` (четвёртая точка §7.4): решение берётся из самой строки (`KeyRow.of(...)` и `RowAction.REVEAL`), значение — только из личного слоя; поставочное и отсутствующее — `None` без раскрытия. `KeyRowView.reveal_button` / `show_value` / `hide`, `KeysTab.toggle_own_value` / `hide_revealed`: значение — только в подписи своей строки, прячется перед любым действием вкладки и при смене вкладки (`<<NotebookTabChanged>>` в `SetupWindow`). Baseline тестов livecraft — **885**.
- Порядок в рабочей копии: `requirements.txt` показывался изменённым только из-за CRLF (снимок `manage_project.py`); содержимое то же, Коворк вернул LF из git.
- **Задача 2.3b принята 23-09-2026, коммит `7d53223`.** Окно: тема `clam` (тема Windows `vista` игнорирует фон вкладки), выбранная вкладка жирная и белая, промежуток между вкладками — прозрачный элемент вокруг вкладки (`SetupWindow._apply_style`); подсказки к настройкам — `SETUP_SETTINGS_FIELD_HINTS`, третья колонка `SettingsTab`. Названия полей сейфа: «Google таблица контент-плана», «колонки Google таблицы (например, B:H)», «Google форма для ключей стрима (эфира)»; оговорка вкладки ключей — без подробностей хранения, «не от специалиста» сохранено (§14 решение 6). Поставочный `llm` — как в боевом restreamer: `gpt-5.6-sol` / `gpt-5.4` / `medium` / `flex` / 900 / 8000. Baseline тестов livecraft — **895**.
- Принятое отступление 2.3b: пример в названии диапазона — `B:H`, а не поставочный `A:F`. Название поля стоит в каждой маске и сводке; настоящий диапазон в нём раскрыл бы значение сейфа (§7.5). Тест `test_no_vault_field_name_contains_the_supplied_values` держит правило. Остатки того же рода (пример `A:F` в `SETUP_INPUT_SHEETS_RANGE`, слово «сейф» и пути в `VAULT_NOT_READY`, `VAULT_FILE_BROKEN`, `VAULT_LOCAL_UNREADABLE`) — задача 2.3c.
- **Задача 2.3c принята 24-09-2026, коммит `11e579e`.** `VAULT_LOCAL_UNREADABLE`, `VAULT_FILE_BROKEN`, `VAULT_NOT_READY`, `SETUP_INPUT_SHEETS_RANGE` — без слова «сейф», без `secrets\` и без поставочного диапазона; тест `app\tests\test_ui_messages.py` обходит все тексты `messages_ru` (строки, словари, кортежи) и держит правило. Baseline тестов livecraft — **898**.
- **Этап 2 закрыт 24-09-2026**, кроме задачи 2.4 (живые проверки по кнопкам — после этапа 4).
- Мелкие долги текстов (чинятся, когда задача откроет файл): в `VAULT_LOCAL_UNREADABLE` в скобках старые названия полей («таблица плана, форма ключей»); подробности `VaultFormatError` в `app\secretsafe\crypto.py` — по-английски со словом vault и через `{error}` доходят до человека.
- **Нарезка этапа 3 (контур A), Коворк 24-09-2026:** 3.1 — план из таблицы без сети: `core\sheet_text.py` (разбор даты и времени донора), `sheets\plan.py` (`SheetRow`, `SheetPlan` из значений диапазона, распознавание шапки), `sheets\rows.py` (`PlanRow`, `RowSkipReason`, нормализация ссылки YouTube, отсев пустого, прошедшего, негодного и повторов «та же ссылка — тот же момент»); 3.2 — чтение таблицы: `google\auth.py` (личность оператора, `spreadsheets.readonly`, `secrets\sheets.token.json`), `sheets\client.py` через `RetryPolicy`, вторая точка `reveal()`; 3.3 — источники: `MetadataFetcher` / `YtDlpFetcher`, `Preview`; 3.4 — язык: `LanguageResolver`; 3.5 — слоты: `StreamSlot`, `SlotBuilder`, повторы «слот — язык — ссылка», `--export-slots`, `--no-llm`; 3.6 и дальше — перенос `llm\`, `llm\merges\`, `texts\`. Повторы в доноре: в бою работает только `deduplicate_planned_videos_within_slot_language` (слот + язык блока + ссылка, остаётся ранний ряд); языковой уровень возможен только после 3.4.
- **Задача 3.1 принята 24-09-2026, коммит `c527aed`.** `app\core\sheet_text.py` (разбор даты и времени донора, `column_letters_to_index`, `normalize_header_name`, `extract_youtube_video_id`, `normalize_youtube_link`, `is_real_local_time`), `app\sheets\plan.py` (`SheetRow.plan`, `SheetColumns.from_header`, `SheetPlan.from_values` / `problem` / `plan_rows`), `app\sheets\rows.py` (`RowSkipReason`, `PlanRow`). Отсев: пусто → нет даты/времени → дата не разбирается → времени не существует (переход на летнее) → прошло → не YouTube; повтор «ссылка + момент» — у более позднего ряда. Baseline тестов livecraft — **1000**.
- Решения 3.1: исправлена ошибка донора — `watch?v=` первым параметром не находился шаблоном ссылки и проигрывал более поздней `youtu.be`; в псевдонимы колонки ссылки добавлены «ссылка» и «видео». Сохранено поведение донора (известно, не чинится): «ё» выпадает из имени колонки при сравнении; голый 11-символьный id находится и в ссылке на чужой сайт.
- §7.4 уточнён 24-09-2026 (следствие §7.5, смысл не меняется): точка `reveal()` клиента Sheets раскрывает и id таблицы, и диапазон — оба лежат в сейфе.
- **Задача 3.2 принята 24-09-2026, коммит `9f6b2be`.** `app\core\retry.py::RetryPolicy` (из planers как есть); `app\google\auth.py`: `SHEETS_SCOPE`, `YOUTUBE_SCOPE` (единственное место скоупов), `AuthErrorReason`, `AuthError`, `GoogleLogin` (`operator(paths)`, `credentials`, `save`, `drop`; поле `saves_new_login` — оператор пишет токен сразу, канал на этапе 4 — после подтверждения); `app\sheets\client.py`: `SheetsTarget.from_vault`, `SheetsReader` (`open`, `read_plan`, `read_values`; повторы 429/5xx/транспорт/DNS через `RetryPolicy`), `SheetsFailure`, `SheetsReadError` (текст без id, диапазона и текста `HttpError`); вторая точка `reveal()` — `SheetsReader._request`. Пробник `python -m app.tools.sheets_probe` печатает только колонки и счётчики. Baseline тестов livecraft — **1067**.
- Для боевой проверки 3.2 Коворк положил в `secrets\client_secret.json` копию `planers\secrets\client_secret.json` (в git не попадает): это тот же OAuth-клиент того же проекта Google Cloud, что у restreamer (`restreamer\secrets\credentials.json`), чей токен уже работает со скоупом `spreadsheets.readonly`.
- Решение для `main` на этапе 3.5/4: пробник на сбой входа отдаёт код 1, а §10 для ошибок авторизации в обычном запуске требует код 2 — при связке с `main` следовать §10.
- Мелкий долг тестов: `app\tests\test_sheets_plan.py::test_skipped_rows_and_summary_go_to_the_log` ставит уровень логгера присваиванием `level`, а не `setLevel` — хрупко к порядку тестов.
- **Боевая проверка 3.2 пройдена 24-09-2026** (`python -m app.tools.sheets_probe` на тестовой таблице Артура): колонки найдены по шапке (Links — 3-я, Date — 4-я, Time — 5-я в диапазоне `A:F`), прочитано 10 рядов, допущено 10. Первый вход был сделан аккаунтом канала — 403, `sheets.token.json` удалён, повторный вход нужным аккаунтом прошёл. Итог — задача 2.3d (`--relogin` пробника, вставка в русской раскладке, понятная кнопка сброса своего значения).
- В таблице плана есть колонка языка `L` (A) и колонка `Chips` (B); программа их не читает (§14 решение 12). Нужны только ссылка, дата и время; достаточно диапазона `C:E`, `A:F` тоже годится.
- **Задача 3.3 принята 24-09-2026, коммит `76147ac`.** `app\texts\source_title.py::sanitize_source_video_title` (сверено с донором на 200 000 случайных строк), `app\sources\metadata.py::SourceMetadata.from_ytdlp`, `fetcher.py` (`SourceFailureReason`, `SourceFetch`, `MetadataFetcher`), `ytdlp.py` (`YtDlpResult`, `YtDlpFetcher` с временной копией cookies), `preview.py` (`Preview` — всегда JPEG ≤ 2 МБ, ужатие 90/80/70; `PreviewResult`, `PreviewProblem`, `PreviewDownloader` через `RetryPolicy`), `video.py` (`SourceVideo`, `SourceTally`, `SourceCatalog` — одно обращение на ссылку). Пробник `python -m app.tools.source_probe <ссылка>`. Отличие от донора: несконвертированная обложка — проблема, а не исходный формат (YouTube принимает только JPEG). Baseline тестов livecraft — **1158**.
- Коворк положил `tools\yt-dlp.exe` и `tools\deno.exe` из restreamer для боевой проверки пробника (в git не попадают, §5). Автообновление бинарников — отдельной задачей (`runtime\ytdlp_updater.py`, `deno_updater.py`, `cookies_updater.py`).
- restreamer ушёл вперёд: `35324e5` (24-09-2026) — исправление шаблона `watch?v=`, то же, что в livecraft 3.1.
- **Задача 2.3d принята 24-09-2026, коммит `26fcf9c`.** `app\setup\tabs\__init__.py`: `EditShortcut`, `EditShortcuts` (Ctrl+V/A/X/C по коду клавиши в нелатинской раскладке через `bind_all`, латинскую отрабатывает штатная привязка Tk — двойной вставки нет); поля ключей гасят `<<Copy>>` и `<<Cut>>`; `KeyRow.reset_label` («Вернуть значение программы» / «Удалить своё значение»), `KeysPanel.row(field)`; пробник таблицы — `--relogin` и подсказка при 403. Baseline тестов livecraft — **1183**.
- Вопрос о кнопке «показать своё» закрыт решением 11 §14 (23-09-2026): только своё значение, поставочное — никогда и никому. §7.4 теперь называет четыре точки `reveal()`.
- **Задача 3.4 принята 24-09-2026, коммит `4463bcd`.** `app\resources\loader.py::TextResource` (+ `text\merge_service_hints.txt` побайтно из restreamer); `app\texts\analysis_text.py::clean_text_for_analysis(text, service_hints)`; `app\sources\language.py`: `normalize_language`, `LanguageSource`, `LanguageSignal`, `TextLanguageDetector`, `LanguageProfile` (`of`, `decide` — голосование `resolve_language` донора без изменения приоритетов), `LanguageDecision` (`language: str | None`), `LanguageResolver`. `SourceVideo.language`, `language_code`, причина `SourceFailureReason.NO_LANGUAGE`; `SourceCatalog` решает язык один раз на ссылку и не качает обложку источнику без языка; пробник печатает язык источника. Сверка с донором прямым сравнением: чистка текста — 200 000 строк, `decide` — 50 000 профилей, расхождений нет. Baseline тестов livecraft — **1270**.
- Известно после 3.4 (поведение донора, перенесено как есть): в чистке текста заголовок «🌐 …:» внутри строки не срабатывает — двоеточие срезается раньше сверки; срабатывает только в проверке служебного хвоста. Мелкий долг: докстрока `app\sources\metadata.py` всё ещё говорит «язык решается в задаче 3.4» — чинится, когда задача откроет файл.
- **Задача 3.5 принята 24-09-2026, коммит `584ba6a`.** `app\core\safe_trim.py` (донор как есть); `app\slots\texts.py` (`SlotTextOrigin`, `SlotTexts`: `from_sources`, `for_youtube` — замена `<` `>`, название ≤100 символов, описание ≤5000 байт с повторной обрезкой по избытку байт, `problem`); `app\slots\slot.py` (`SlotKey` — момент в зоне программы, `slot_id`, `sort_key` uk→en→прочие; `StreamSlot` — поля §4 плюс `text_origin`, `to_record(preview_names)`); `app\slots\builder.py` (`SlotGroup` со страховкой повторной ссылки, `SlotBuild`, `SlotBuilder` — группы по `slot_id`). Baseline тестов livecraft — **1322**.
- Решения 3.5: группа определяется `slot_id`, а не моментом — в осенний час перевода часов две записи одного местного времени становятся одним слотом, момент берётся у первой; описание после подгонки в байтах может быть чуть короче предела (кириллица ≈4880 байт) — плата за обрезку по границе слова; `to_record` требует ровно одно имя на каждую обложку.
- **CLAUDE.md разнесён по решениям 13–19 (24-09-2026).** Код пока расходится с текстом в трёх местах, каждое чинит своя задача: ссылка на форму в сейфе (`Vault` поле `key_form_url`, вкладка ключей) — задача 3.6; `--export-slots` в `app\main.py` — задача пакета; `ProgramKey` и `vault.dat` — этап «Токен доступа».
- **Задача 3.6 принята 24-09-2026, коммит `32f1202`.** Ссылка на форму — открытая настройка `FormSettings.url` (пусто — не настроено; правило ссылки переехало из `SecretInput` в `FormSettings.url_problem`), поле на вкладке «Настройки запуска». `SecretField.KEY_FORM_URL` — устаревшее (`is_legacy`, `SecretField.current()`), для запуска не требуется, фильтр логов его по-прежнему вычёркивает. `app\setup\migration.py::FormUrlMigration` при запуске один раз переносит ссылку из сейфа в `livecraft.json` и убирает поле из личного сейфа (поставочный не трогается); `main._run` после переноса перечитывает готовность. Baseline тестов livecraft — **1361**.
- Дефекты после 3.6, чинятся задачей 3.6a: сводка готовности (`Readiness._field_lines`) перебирает весь `SecretField` и показывает строку формы; `secrets\livecraft.json` в git — перенос ссылки (и любая правка в настройщике) у Артура попала бы в `git diff` и в поставку.
- **Задача 3.6a принята 24-09-2026, коммит `89d7e51`.** `secrets\livecraft.json` вне git (вся папка `secrets\` личная; `.gitignore` получил `/bcast/`, `/tokens/`, потерял `/export/`); `app\config\loader.py::ShippedSettings` (`settings` — разбор шаблона тем же `parse_settings`, `install` — нет файла → запись `render_settings_file`, есть → не трогать); `main._run` кладёт файл до `Readiness.check`. Сводка готовности — только `SecretField.current()` плюс строка «форма ключей: настроена / не настроена» по `FormSettings.is_configured`. Тесты не опираются на личный `livecraft.json`: поставочный файл собирается `ShippedSettings.install` во временной папке. Baseline тестов livecraft — **1373**. Дефекты после 3.6 закрыты.
- Мелкие долги после 3.6a (чинятся, когда задача откроет файл): докстрока `app\setup\panels\settings_panel.py` о шаблоне при отсутствии файла устарела (ветка теперь только для сломанного файла); `REPO_SETTINGS_FILE` в `test_setup_migration.py` и `test_tools_sheets_probe.py` — старое имя `SHIPPED_SETTINGS_FILE`.
- **Задача 3.7 принята 24-09-2026, коммит `0a95b8c`.** `app\packages\package.py`: `PackageProblem` (`NO_SLOTS`, `FORM_NOT_CONFIGURED`), `PackageResult` (`is_written`, `log_line` без ссылки формы, `console_line`), `SlotPackage` (`of` — момент сборки в зоне программы, слоты по `(start, language)`, слот с проблемой и повтор `slot_id` — WARNING, побеждает поздний; `manifest` — схема донора, форма из `FormSettings.to_data()`; `write` — ZIP во временный `.plan_…` рядом и `os.replace`). `LivecraftPaths.bcast_dir`. `--export-slots` удалён из `main` и текстов. Совместимость проверена читателем planers (`read_package`, `scan_bcast` из `.venv_planers`: 3 слота, `problems=()`). Baseline тестов livecraft — **1395**.
- Боевая настройка Артура 24-09-2026: ключ OpenAI и ссылка на форму введены в настройщике, `secrets\channels.json` — копия `planers\secrets\channels.json` (формат совпал, 6 каналов); сводка готовности чистая. Замечания к окну — задача 3.7a (сохранение ключей в один шаг, кнопки по центру, выбор языков канала из списка). Замечания к запуску без настроек (требует всё сразу, велит создать файл руками, перенос ссылки зависит от каналов) — задача 3.8.
- **Задача 3.7a принята 24-09-2026, коммит `d41405e`.** `app\setup\fields\language_choice.py` (`LanguageOption`, `LanguageCatalog` — все 184 языка pycountry с alpha_2, русские названия у 158; языки формы первыми с пометкой; `LanguageSelection`); вкладка каналов — поиск и список с множественным выбором, названия языков в таблице, предупреждение о языке не из формы; вкладка ключей — «Сохранить значение» и сброс пишут сейф сразу, общей кнопки нет; ряды кнопок каналов и настроек по центру. Baseline тестов livecraft — **1427**.
- Мелкий долг после 3.7a: вкладка каналов читает языки формы при открытии окна — после сохранения настроек пометки обновятся только при следующем открытии. Чинится, когда задача откроет `channels_tab.py` (обновление по `on_saved` вкладки настроек).
- Нарезка задачи 3.8 (Коворк, 24-09-2026): 3.8 — готовность по частям режима (`RunPart`, `RunMode`), раздельное чтение настроек и каналов, открытие настройщика вместо простыни шаблона, перенос ссылки без зависимости от каналов, флаги `--announce`, `--broadcast`, `--from-package`; 3.9 — сам режим А в `main`: таблица → источники → слоты → пакет, итог в консоль.
- **Задача 3.8 принята 24-09-2026, коммит `b8f7669`.** `app\setup\run_mode.py` (`RunPart` — PLAN, MERGE, PACKAGE, ANNOUNCE, BROADCAST, PACKAGES_IN; `is_built`, `not_built_line`; `RunMode` — ANNOUNCE, BROADCAST, ALL, FROM_PACKAGE; `parts(no_llm)`); `Readiness` читает сейф, `livecraft.json` и `channels.json` независимо, `part()` и `for_mode()` → `PartReadiness`, `ModeReadiness` (`ready`, `blocked`, `not_built`, `is_nothing_ready`, `is_fixable_in_setup`); шаблоны файлов — только в лог; не готово ничего — открывается настройщик, код 2; повреждённый файл сейфа — строки и код 2 без окна; флаги `--announce`, `--broadcast`, `--from-package`; перенос ссылки формы не зависит от каналов. Baseline тестов livecraft — **1485**.
- **Задача 3.7b принята 24-09-2026, коммит `f563ee1`.** Язык канала — одно выпадающее поле (`ttk.Combobox`) с сужением по вводу, один код в черновик (`LanguageSelection.single`, `first`, `extra_codes`; `LanguageCatalog.label_of`, `code_of`, `labels`, `text_problem`); после сохранения настроек пометки формы на вкладке каналов обновляются сразу (`SetupWindow.settings_saved`) — долг 3.7a закрыт. Baseline тестов livecraft — **1503**.
- Долги после 3.8 и 3.7b: `NOT_BUILT_PARTS` не содержит MERGE и BROADCAST, хотя их ещё нет — «эфиры» и «нейросеть» показываются готовыми (чинит 3.9); отсутствие `channels.json` пишется в лог уровнем ERROR (обычное состояние чистой установки — чинит 3.9); правило «языки каналов» в `Readiness._channels_line` повторяет `LivecraftConfig.served_languages`, `ConfigError.is_template_needed` не используется — чинятся, когда задача откроет `loader.py`; выпадающий список языка не раскрывается сам при вводе.
- **Задача 3.9 принята 24-09-2026, коммит `c062b45`.** Режим А делает работу контура A: `app\slots\intake.py` — `IntakeRequest` (paths, settings, vault, now, package_id), `PlanIntake` (`of`, `run`: таблица → ряды → источники → слоты → пакет, остановка там, где дальше идти не с чем), `IntakeStage`, `RowTally`, `IntakeResult` (`exit_code` по §10, `console_lines`, `log_line`). `ExitCode` переехал в `app\setup\run_mode.py` (`combined`: 2 > 3 > 1 > 0), `main` реэкспортирует; `NOT_BUILT_PARTS` = ANNOUNCE, MERGE, BROADCAST, PACKAGES_IN; отсутствие конфига — `config_missing` INFO. Нейросети пока нет — тексты из видео. Baseline тестов livecraft — **1531**.
- Решения 3.9: все видео отказали — код 1, а не 3 (иначе отсутствие `yt-dlp.exe` выглядело бы как «нет будущих эфиров»); вход в Google открывается в `run()`, строка «откроется браузер» — через `on_login`. Долг: при ненастроенной форме в консоли две строки об одном («Не готово — пакет» и «Пакет не записан») — убрать одну, когда задача откроет `intake.py` или `readiness.py`.
- **Боевой прогон режима А пройден 24-09-2026** (`--announce`, тестовая таблица Артура): рядов 10, допущено 10; видео годных 10 из 10, без обложки 0; слотов 4 (uk 1, en 2, ru 1); пакет `bcast\plan_17-03-2027_18-03-2027_gen24-09-2026-2018.bcast` (4 слота, 10 обложек). Коворк прочитал этот пакет читателем `planers\app\package\reader.py::read_package` без замечаний: `start` со смещением `+02:00`, описания составных слотов 3953–4929 байт — подгонка под 5000 байт работает. Для прогона Коворк положил в `secrets\` копию `restreamer\secrets\cookies.txt` (в git не попадает). Критерий этапа 3 выполнен, кроме merge.
- **Нарезка переноса нейросети (Коворк, 24-09-2026):** 3.10 — транспорт LLM (`app\llm\`: модель и тарифы, учёт расхода, заголовки лимитов, классификация ошибок, клиент Responses API с ключом из сейфа, выбор модели primary/fallback, разбор JSON из ответа, пробник `app\tools\llm_probe.py`); 3.11 — промт и разбор merge (`merge_prompt`, `merge_parser`, `merge_validation*`, `merge_formatting`, `merge_quality`, константы и лексиконы); 3.12 — исполнение merge (`merge_orchestrator`, `merge_executor`, `merge_retry`, `merge_single_source`, `merge_youtube`, `merge_links`, `script_mix_repair`, `quality_*`) → `MergeJob(SlotGroup) -> SlotTexts` с origin `merged`, включение MERGE в `intake`; 3.13 — санация после LLM (`publish\post_llm_sanitation.py`, `sanitizers\`, `quality_gate.py`, `tail_parser.py`, `url_selector.py`). Всего донора ≈ 7000 строк в `llm\` плюс санация.
- **Задача 3.10 принята 24-09-2026, коммит `0729873`.** Пакет `app\llm\`: `model.py` (`ModelFamily`, `ModelPrice` — снимок цен донора 09-2026, `ServiceTierRule`, `LlmModel`), `usage.py` (`RequestUsage`, `RunUsage` — объект запуска вместо глобального состояния), `rate_limits.py` (`RateLimitSnapshot`), `errors.py` (`LlmErrorKind`, `LlmRequestError.classify` — подробность ответа вычищается ключом и обрезается), `json_text.py` (чистые разборы JSON донора), `client.py` (`LlmRequest`, `LlmResponse`, `OpenAiClient` — ключ из сейфа, единственная точка `reveal()` в `_api` при создании SDK-клиента; `LlmExchange` — откаты: flex 429 → ожидания 20/40/80 с (`FLEX_RETRY_DELAYS_SEC`, правило тарифа, согласованное исключение из §11) → default; исчерпан `max_output_tokens` → один повтор с удвоением; снятие температуры; сетевые повторы — `RetryPolicy`, SDK `max_retries=0`), `selection.py` (`ModelChoice.select` — отказ доступа → запасная, иной сбой → основная «не проверена»). Пробник `python -m app.tools.llm_probe`. Провайдер один — абстракции провайдера нет. Baseline тестов livecraft — **1629**.
- **Задача 3.10a принята 25-09-2026, коммит `ba46e35`.** Разъём нейросети (§14 решение 22): `app\llm\backend.py` — `LlmBackend` (Protocol: `name`, `run_usage`, `complete`, `probe`), `LlmRequest` (`prompt`, `model_name`, `max_output_tokens`, `label`, `timeout_sec`, `schema`, `temperature`; `from_settings`, `probe`), `LlmResponse` (`notes` — что реализация делала сверх одного обращения); `errors.py` — `LlmErrorKind` без приставки `openai_`, `LlmRequestError` с полем `backend`, разбор исключений SDK ушёл в `backends\openai_errors.py::OpenAiFailure`; `usage.py` — `RequestUsage` (`thinking_tokens`, `tier`, `label`, `cost_usd`), `RunUsage` считает итоги по списку ответов; `selection.py` — `ModelChoice.select(backend, primary, fallback)`, модели — имена. OpenAI: `backends\openai.py` (`OpenAiRequest`, `OpenAiClient`, `LlmExchange`, `FLEX_RETRY_DELAYS_SEC`), `openai_model.py` (`OpenAiModel`, цены, `ServiceTierRule`), `openai_response.py` (`OpenAiUsage`, `OpenAiReply`), `openai_rate_limits.py`. Старые `client.py`, `model.py`, `rate_limits.py` удалены без фасадов (вне пакета ими пользовался только пробник). Точка `reveal()` ключа OpenAI — `app\llm\backends\openai.py::OpenAiClient._api`. Общие модули не называют OpenAI, flex, service_tier и reasoning — это держит тест `test_neutral_modules_name_no_backend`. Baseline тестов livecraft — **1653**.
- Строки лога нейросети после 3.10a (важно при сравнении логов с restreamer): `backend=openai reason_code=<вид без openai_>`; в строке запуска `thinking_tokens=` и `tiers=` вместо `reasoning_tokens` и `service_tiers`.
- Мелкий долг после 3.10a: `app\llm\backends\openai.py::DEFAULT_SCHEMA_NAME = "merge_v1"` — имя схемы merge живёт в транспорте; схему ответа merge (`MergeExecutor._STRUCTURED_SCHEMA` донора, имя `merge_summary_v2`) задаёт объект merge в задаче исполнения, после чего запасное имя становится нейтральным.
- **Нарезка переноса merge уточнена (Коворк, 25-09-2026), по коду донора `35324e5`:** 3.11 — ответ модели: разбор JSON-ответа, отделение хвоста (хештеги, официальные ссылки, ссылки YouTube), восстановление числа абзацев, отказы разбора перечислением, повтор абзацев и эхо тезиса, починка смешанного алфавита (`merge_parser.py`, `merge_validation_helpers.py`, `script_mix_repair.py`, нужная часть `core\text_utils.py`, `core\cta_detection.py::starts_with_cta_prefix`, `core\official_links.py`, `core\constants.py`); 3.11b — качество описания: `quality_service_lines.py`, `quality_diagnostics.py`, `quality_normalizer.py`, `merge_quality.py`, `merge_text_utils.py`; 3.12 — источники в промте: официальные ссылки и ссылки YouTube (`merge_links.py`, `merge_youtube.py`), контракты compact / expanded / narrative, промт и инструкции повтора (`merge_prompt.py`, `merge_retry.py`, `merge_constants.py`); 3.12b — проверка покрытия и восстановление форматирования (`merge_validation.py`, `merge_formatting.py`); 3.13 — `MergeJob` через `LlmBackend` (`merge_executor.py`, `merge_orchestrator.py`, `merge_single_source.py`, `merge_service.py`, `merge_run_summary.py`) и MERGE в `intake`; 3.14 — санация после LLM.
- **Задача 3.11 принята 25-09-2026, коммит `78c0e53`.** Ответ модели merge (§14 решение 23): `app\llm\merges\reject.py` (`MergeRejectCode` — 11 кодов, `MergeReject` — отказ значением, `reason_codes` как у `_reason_codes_from_error` донора), `layout.py` (`TailBlock`, `LayoutRecovery`, `DescriptionLayout.of` — хвост, восстановление тела, блокировка при повторе и эхе), `description.py` (`MergedDescription` — повтор абзацев, эхо тезиса и его починка, призыв в начале, служебные строки; `HomoglyphMap`, `HomoglyphRepair`; `HookParagraph`), `hook.py` (`BadHookLexicon` из `merge_bad_hook_patterns.txt`), `answer.py` (`MergePayload`, `MergeAnswer.parse` — отказов исключениями нет). `app\texts\paragraphs.py`, `app\texts\description_marks.py` (`CtaLexicon`), ресурс `lexicon_cta_prefixes.txt` побайтно. Сверка с донором: 194 027 входов (60 018 ответов, 14 001 описание, 120 008 слов), 0 расхождений. Коды донора `forbidden_variants` и `meta_header` недостижимы (лишние ключи проверяются раньше вариантов; служебные строки снимаются раньше проверки) — не перенесены; название или описание списком — `unexpected_error`, как у донора, причина в `detail`. `MergeAnswer.model` — модель, которая ответила. Baseline тестов livecraft — **1815**.
- Мелкий долг после 3.11: в `app\llm\merges\answer.py::EMOJI_PATTERN` и в `description.py` инструмент записи Codex превратил экранированные `\u2600-\u27BF` и `\u02bc` в литеральные символы — поведение то же (сверка), но границы диапазона не видны глазом; вернуть экранирование — задача 3.11b.
- **Задача 3.11b принята 25-09-2026, коммит `4fd3f47`.** Качество описания merge: `app\llm\merges\blocks.py` (`DescriptionBlocks` — тезис, вводная, пункты, заголовок и ссылки, призыв; `render`, `script_mix_suspects`), `service_lines.py` (`ServiceLineKey`, `ServiceLineCatalog` из `canonical_service_lines.json`, `ServiceLanguage` поверх `TextLanguageDetector`), `quality.py` (`QualityReasonCode`, `QualityGateStatus`, `QualityRules`, `QualityRequest`, `BulletNormalization`, `CompactTrim`, `ServiceLineFix`, `QualityDiagnostics` — поля донора, `QualityNormalization`), `agenda.py` (`AgendaLexicon`), `rules.py` (пороги донора); `MergedDescription.quality_normalized`, `overloaded_bullet_count`, `contains_agenda_heading`, `looks_like_per_source_dump`; `CtaLexicon.hints`, `looks_like_cta_line`, `looks_like_cta_paragraph`; `TextResource.data` (JSON). Встроенные списки донора — файлы `merge_service_language_hints.json`, `merge_allowed_latin_tokens.txt`. Долг экранирования 3.11 закрыт, тест `test_merge_patterns_have_no_literal_range_characters` держит. Сверка с донором: 42 025 входов, 0 расхождений с донором, в котором исправлена одна ошибка (ниже). Baseline тестов livecraft — **1944**.
- **Исправлена ошибка донора (решение 23 допускает):** `restreamer\app\llm\merges\quality_normalizer.py::_split_bullet_marker` снимал простой маркер пункта шаблоном распознавания `…\s+\S+` и съедал первое слово: «- first point here» → «🔹 point here», «- word» → «🔹 - word». В livecraft снимается только маркер (`quality.py::PLAIN_BULLET_MARKER_PATTERN`). Проверено Коворком по коду донора. Следствие для сверок 3.12b и дальше: донорская сторона сверки запускается с тем же исправленным `_split_bullet_marker`, иначе расхождения на пунктах без эмодзи-маркера ожидаемы и не являются дефектом.
- **Нарезка merge уточнена по коду донора (Коворк, 25-09-2026):** 3.12 — промт: тексты `templates.yaml` ресурсами, подготовка описаний источников, выбор контракта compact / expanded / narrative, профили повтора, сборка текста промта (`merge_prompt.py`, `merge_retry.py`, нужная часть `core\description_cleaner.py`, `core\language_display.py::language_full_name`); 3.12b — официальные ссылки источников, счёт ссылок YouTube, проверка покрытия, восстановление форматирования (`merge_links.py` — боевые только `_extract_official_links_from_sources` и `_count_output_official_links`, `merge_youtube.py` — боевой только `_count_youtube_urls_in_text`, кандидаты YouTube с сетевым yt-dlp в боевом пути донора не используются и не переносятся; `merge_validation.py`, `merge_formatting.py`); 3.13 — `MergeJob`.
- **Задача 3.12 принята 25-09-2026, коммит `e0b6331`.** Промт merge: `app\llm\merges\prompt_texts.py` (`MergeContractMode`, `MergePromptTexts.load` — все тексты промта ресурсами, `fill_placeholders` — подстановка `{ключ}` донора), `source.py` (`PreparedSourceDescription`, `MergeSource.of(video, texts)` — очищенное описание, «нет описания», блок источника и строка лога), `contract.py` (`SourceTextSet` — «одно событие» и имена спикеров, `MergeContract.select` — compact / expanded / narrative, `max_body_paragraphs`, текст блока), `retry.py` (`RetrySignal`, `RetryFacts`, `RetryProfile.targeted` / `expanded` / `standard`, блок `RETRY INSTRUCTION:`), `prompt.py` (`MergePrompt.of` → промт или `MergePromptRefusal`, `with_retry`, `text`, `source_texts_for_quality`, три строки лога донора). `app\texts\analysis_text.py::AnalysisTextReport` — чистка текста со счётчиками, `clean_text_for_analysis` берёт текст из него. Ресурсы: боевые значения `templates.yaml` побайтно (`merge_prompt_title_description.txt`, `merge_prompt_structural_rules.txt`, `merge_prompt_no_description.txt`, `merge_prompt_contracts.json`, `merge_retry_reinforcements.json`) и встроенные тексты донора файлами без правки строк. Сверка с донором: 20 021 слот, ≈4,9 млн значений (текст промта, поля контракта, блоки, тексты для качества, строки лога), 0 расхождений; ветви покрыты (все три контракта, строка спикеров, «нет описания»). Baseline тестов livecraft — **2050**.
- Проверено Коворком на приёмке 3.12: изменены только файлы задачи, рабочая копия чистая; свободные функции — только чистые преобразования (`fill_placeholders`, `capitalized_runs`, `language_full_name` и внутренние `_range_label`, `_expanded_bullet_range`, `_unique_in_order`); `reveal`, перехвата `Exception` и `print` в новых модулях нет. У restreamer в рабочей копии изменено 12 файлов (в т.ч. `templates.yaml`) — только переводы строк (`git diff --ignore-cr-at-eol` пуст), тексты промта те же.
- **Решение для 3.13 (Коворк, 25-09-2026):** проверка ответа берёт предел абзацев тела из того же `MergeContract`, что стоит в промте попытки (`MergePrompt.contract.max_body_paragraphs`). Донор выбирает контракт для промта по очищенным описаниям, а предел абзацев в `merge_orchestrator.py` — по сырым; в редком случае «одно событие» расходится, и модель получает одно правило, а проверяется по другому. Это ошибка донора (решение 23 допускает исправление); в сверке 3.13 донорская сторона запускается с тем же правилом.
- Отмечено на приёмке 3.12: в одном из девяти полных прогонов pytest упал один тест, имя не сохранилось (прогон шёл 83 с против обычных ~31 с). Причина не найдена; задача 3.12b прогоняет набор пять раз с `-rf` и называет упавший тест, если он повторится. Четыре `ValueError` в `app\llm\merges\service_lines.py` — отказ на испорченном файле ресурса (ошибка поставки, а не причина отказа merge); это не долг, промты больше не просят «ноль ValueError» по всей папке.
- Не перенесено из донора как мёртвое в бою (для 3.12b): `merge_links.py::_inject_official_links_block_if_missing`, `_description_has_official_links_block` (исполнитель всегда ставит `fill_applied=False`), `merge_youtube.py::_extract_merge_youtube_candidates`, `_build_youtube_candidates_block`, `merge_validation.py::_is_softened_distinctive_source_coverage_eligible` (зовут только тесты донора). `_build_rejected_merge_attempt` и `_best_effort_rejected_payload_fields` — задача 3.13.
- **Задача 3.12b принята 25-09-2026, коммит `fcacdf6`.** Проверка ответа merge: `app\core\url_text.py` (чистые разборы ссылок донора: `split_url`, `is_youtube_host`, `strip_tracking_params`, `normalize_display_url`, `canonical_link_key`, `normalize_link_candidate`), `app\llm\merges\links.py` (`OfficialLinkHints`, `OfficialLinkCandidate`, `OfficialLinkSelection.of` / `for_sources` — отбор до трёх официальных ссылок), `check.py` (`MergeCheckRules`, `MergeAttemptLabel`, `MergeCheckRequest`, `MergeDiagnostics` — поля и две строки лога донора, `MergeCheck.run` / `run_with_recovery` — проверки в порядке донора, `MergeCheckPassed`, `FormattingRecovery`); `MergedDescription` получил счёт ссылок и эмодзи, снятие неструктурных эмодзи, повтор соседних строк, похожие начала абзацев, призыв в начальных строках и в тезисе; `MergeRejectCode` — коды проверки описания, `MergeReject.validation_codes` с нормализацией кодов гейта донора, `MergeRejectStage` и `CODE_STAGES` (какой шаг какой код выдаёт; полноту каждого шага держит свой тест). Сверка с донором: 24 008 входов, 0 расхождений, каждая причина отказа встретилась. Baseline тестов livecraft — **2247**, пять полных прогонов без падений (34–38 с) — мигание, замеченное в 3.12, не повторилось.
- Исправлена ошибка донора (решение 23): ссылка вида `https://[bad` роняла у донора `urlsplit` (`ValueError`) и весь merge слота; livecraft такую строку ссылкой не считает (`url_text.py::split_url`). В сверке донор упал на ней в 879 входах.
- Остановка Codex по (б) в 3.12b — ошибка промта Коворка: в SCOPE LOCK не было `app\tests\test_llm_merges_answer.py`, тест полноты которого требовал, чтобы разбор ответа выдавал каждый код. Закрыто дополнением: коды знают свой шаг.
- Долг после 3.12b: `EMOJI_PATTERN` объявлен дважды — `app\llm\merges\answer.py` и `description.py`; чинит 3.13.
- **Нарезка merge уточнена (Коворк, 25-09-2026):** 3.13 — исполнение: попытка (`MergeAttempt`: запрос через `LlmBackend` со схемой ответа merge → разбор → починка алфавита → нормализация качества → проверка с восстановлением), цикл попыток слота с выбором профиля повтора (`MergeJob`), состояние запуска (`MergeRun`: модель, остановка по квоте и по ошибке настройки модели, счётчики `MergeRunSummary` донора), итог слота — `SlotTexts` с происхождением `merged` или тексты источников; без связки с `intake`. 3.14 — санация после LLM. 3.15 — MERGE в `intake`, `--no-llm`, боевой прогон режима А с нейросетью.
- **Решения к 3.13 (Коворк, 25-09-2026):** (1) пауза донора `llm.pre_delay_sec` (5 с перед каждым запросом) не переносится: частоту держит транспорт (ожидания flex 429, `RetryPolicy`), а запросов за запуск — десятки, до лимитов уровня 1 далеко; новой настройки нет. (2) Квота исчерпана или модель не настроена — merge останавливается до конца запуска, оставшиеся слоты получают тексты источников, запуск идёт дальше (у донора ошибка модели роняла весь прогон — противоречит инварианту 9); код выхода — 1, это связка 3.15. (3) Ошибка донора: в `merge_orchestrator.py::MergeOrchestrator.run` переменная `diagnostics` никогда не заполняется, и направленные повторы «мало пунктов» и «много пунктов» говорят модели «у тебя 0 пунктов»; в livecraft число пунктов берётся из отвергнутой попытки. (4) Merge идёт, только если непустых описаний у источников слота не меньше двух (`pipeline\slot_processing.py`, `run_classic_merge`), иначе — тексты источников, как у донора. (5) Запасное имя схемы `DEFAULT_SCHEMA_NAME = "merge_v1"` уходит из транспорта: схему ответа merge (`merge_summary_v2`) задаёт merge.
- **Задача 3.13 принята 25-09-2026, коммит `c771c41`.** Исполнение merge (§14 решение 23): `app\llm\merges\attempt.py` — `MergeRules` (правила проверки и тексты промта одним объектом на запуск), `MergeAttempt` (промт, метка, источники, нейросеть, настройки, правила; `request` — схема `merge_summary_v2`, температура 0; `run`), `MergeAttemptResult` (ровно одно из `accepted` / `rejected` / `error`; `is_quota`, `is_model_configuration`, `code`, `codes`, `is_recoverable`, `next_retry`, `invalid_line`), `AcceptedMerge`, `RejectedMerge` (`facts` — числа отвергнутой попытки для подсказки повтора); `job.py` — `MergeJob(group, merge_run)` → `MergeOutcome`, `AttemptHistory` (две попытки, три после повторяемого отказа), `ParagraphEnforcement`, `MergeSkipReason`; `run.py` — `MergeRun` (нейросеть, модель, настройки, правила, `tally`, `stop_reason`; `stop` — первая причина остаётся), `MergeTally` (счётчики `MergeRunSummary` донора, блоки по датам `MergeDayBlocks`), `MergeStopReason`, `MergeArtifactStatus`. `MergeReject.paragraph_count` и `signals`; `RetrySignal.first_of`, `SIGNAL_ORDER`, `RetryProfile.after_reject`; `SlotTextOrigin.MERGED`. `EMOJI_PATTERN` — один, в `description.py` (долг 3.12b закрыт); запасное имя схемы транспорта — `response`. Сверка с донором: 1866 запусков, 4037 слотов (в т.ч. 35 сценариев тестов донора), сравнивались хеш промта каждой попытки, схема, температура, коды попыток, итог и счётчики — 0 расхождений; без каждого из четырёх принятых исправлений донора сверка расходится (505 / 340 / 149 / 103). Baseline тестов livecraft — **2320**, четыре полных прогона без падений.
- Проверено Коворком на приёмке 3.13: изменены только файлы задачи; restreamer не тронут; в `attempt.py`, `job.py`, `run.py` нет `reveal`, `except Exception`, `print`, `time.sleep`; единственный перехват — `LlmRequestError` в `MergeAttempt.run` (сбой запроса — значение итога); тексты ответа и источников в строки лога не попадают.
- **Закрыто на приёмке 3.13 (Коворк, по коду донора):** выравнивание принятого описания к 4 абзацам тела (`merge_executor.py::_enforce_description_structure` зовёт `merge_parser.py::separate_merge_body_and_tail` с пределом по умолчанию 4) — не ошибка донора, переносится как есть. Предел проверки 7 у расширенного контракта — допуск на ответ модели, а промт контракта просит «3 to 4 compact paragraphs» и «bullet section at most 2 visual paragraphs» (`merge_prompt_contracts.json`, `expanded`); выравнивание сливает лишние абзацы переводом строки, а не склейкой текста, и приводит ответ к обещанному промтом виду. Предел из контракта здесь дал бы расширенному слоту до 7 абзацев — против промта.
- Codex изменил одну строку `answer.py` сверх разрешённого `EMOJI_PATTERN` (отказ по числу абзацев несёт это число): без неё требование «число из `MergeReject`, без разбора текста» не выполнялось. Принято.
- Мелкий долг после 3.13: `app\tests\test_llm_backends_openai_errors.py` — в тексте тестового сообщения осталось имя `merge_v1`; на поведение не влияет, чинится, когда задача откроет файл.
- **Нарезка санации после LLM (Коворк, 25-09-2026, по коду донора `35324e5`).** В боевом пути донора тексты пакета слота — `publish\slot_publish_texts.py`, ветка `SANITIZED_BLOCK`: `pipeline\slot_processing.py` после успешного merge зовёт `publish\post_llm_sanitation.py::build_sanitized_merged_publication_payload` (метка источника `primary_success`); блок с повтором абзацев или призывом в начале (`sanitizers\quality_gate.py::PublishQualityGate`) не публикуется — пакет получает тексты источников. 3.14 — санация без сети: хвост ответа (`sanitizers\tail_parser.py`), ссылки в тексте, блок официальных ссылок (`sanitizers\description_composer.py::extract_official_links`), официальные ссылки из источников и хвоста (`url_selector.py::select_authoritative_non_youtube`), повторная нормализация качества, сборка описания, проверка перед публикацией; блок «рекомендуемые материалы» — пустой. 3.14b — «рекомендуемые материалы»: ссылки YouTube из описаний источников, язык и название кандидата через `MetadataFetcher` и `LanguageResolver` (у донора два обращения yt-dlp на кандидата), заголовки блоков на языках вне uk/en/ru (у донора — перевод нейросетью с кешем на диске). 3.15 — MERGE в `intake`, `--no-llm`, боевой прогон.
- Не переносится из санации донора как мёртвое в боевом пути пакета: `sanitize_post_llm_text_for_merged_publish` (не вызывается), `should_suppress_raw_merge_attempt_publish` и `log_safe_merge_attempt_fallback` (Docs и Telegram — этап «Публикация»), `resolve_block_generation_mode`, счётчик `runtime_analytics.record_malformed_tail_url_cleanup` (строка лога `merged_source_urls_cleaned` остаётся).
- **Задача 3.14 — коммит `e05ef84` (25-09-2026), поведение принято; структура — правка 3.14c.** Санация принятого ответа merge (§14 решение 23): `app\llm\merges\publication.py` — `SanitizedDescription` (хвост, чистка ссылок, сдвоенные маркеры, абзац-призыв в конце тела; строки `tail_parse`, `tail_layout`, `post_llm_sanitation`), `PublishGate` (повтор абзацев; призыв или негодный тезис в начале), `MergePublication` (`of`, `is_blocked`, `slot_texts`, строка `publish_sanitation_applied=yes`); `app\texts\tail.py` (`TailReader`, `TailScan`, `TrailingTail`, `EmbeddedTail`), `app\texts\official_links.py` (`OfficialLinksBlocks`), `app\texts\composer.py` (`PublishHeadings` из ресурса `publish_headings.json`, `DescriptionParts`), `app\llm\merges\links.py::AuthoritativeLinks`, `app\core\url_text.py::SourceUrl`; `MergeRules.headings`; `MergeJob._merged` санирует принятое описание, блок — тексты источников и строка `merge_publish_gate_blocked target=package`; `MergeOutcome.answer_accepted` / `publish_blocked`: успех merge и настоящие блоки в `MergeTally` считаются по принятому ответу, как у донора (он считал до санации). Baseline тестов — **2469** (по отчёту Codex: пять полных прогонов). Сверка с донором (по отчёту Codex): 8955 входов, 0 расхождений; чувствительность — порча правил даёт 105 и 632 расхождения.
- Проверено Коворком на приёмке 3.14: изменены только файлы задачи; restreamer не тронут; в новых и изменённых модулях нет `reveal`, `except Exception`, `print`, `time.sleep`; импорты — stdlib и `app`; вход санации — описание после выравнивания абзацев, как `description_audit` донора (`merge_service.py:149`); тест блока идёт через `MergeJob.run` на реальном принятом ответе сверки 3.13. Решения Codex приняты: описание без официальных ссылок (`full_text` донора) не собирается — в боевом пути не используется; рекомендуемые видео до 3.14b рисуются строками «👉 ссылка»; счётчики ссылок YouTube в описаниях источников считаются уже сейчас.
- **Правка 3.14c (Коворк, 25-09-2026):** (1) `app\core\url_text.py` импортировал `URL_PATTERN` и `URL_LINE_PATTERN` из `app\texts\description_marks.py` — первая зависимость `app\core` от предметного слоя, а тот же `URL_PATTERN` объявлен ещё и в `app\texts\analysis_text.py` (двойной источник, §11). Хозяин шаблонов ссылок — `app\core\url_text.py`; `description_marks.py` и `analysis_text.py` берут их оттуда; тест держит `app\core` без импортов из `app` вне `app.core`. (2) `SanitizedDescription.of` — 44 строки против ~30 (§11): сбор фрагментов двух хвостов уходит в `TailFragments`.
- К 3.15: (1) блок перед публикацией должен быть виден оператору — у донора `run_final_summary` несёт `publish_gate_blocked_count` и `publish_gate_blocked_languages`; в livecraft — счётчик в `MergeTally` по `MergeOutcome.publish_blocked` и строка отчёта запуска. (2) Сверка уровня слота с донором после 3.14 требует санации на донорской стороне: `MergeJob` отдаёт санированное описание.
- **Задача 3.14c принята 25-09-2026, коммит `50b4217`.** Шаблоны ссылок `URL_PATTERN`, `URL_LINE_PATTERN` объявлены только в `app\core\url_text.py` (`description_marks.py` — реэкспорт, `analysis_text.py` берёт оттуда); тест `test_core_url_text.py::test_core_modules_import_nothing_from_app_outside_core` держит `app\core` без импортов из `app` вне `app.core` (разбор ast на всех уровнях; проверен на старом коде — падал); `TailFragments.followed_by`, `hashtags_line`; `SanitizedDescription.of` — 21 строка. Baseline тестов — **2473**. Сверка с донором (по отчёту Codex) — 8955 входов, 0 расхождений.
- **Этап R — доводка рефакторинга (Коворк, 25-09-2026, по просьбе Артура), до 3.14b.** Разбор всего кода на `50b4217` и план из 25 задач — `REFACTORING.md` в корне репозитория. Итог разбора: общие вещи (имена логгеров, словарь значений лога, текстовые примитивы, разбор ссылки, цикл повторов, контракт ошибок, лексиконы) объявлены копиями по модулям; слои и шов §4 нарушены (`app\slots\intake.py` — оркестратор контура A в пакете шва, `StreamSlot` зависит от `app\sources`, `ExitCode` — в `app\setup`); допуск ряда и источника выражен `Optional`-полями вместо типа; процедурные остатки — `config\loader.py`, `main.py`, `core\url_text.py`, `MergedDescription`; логика выбора языка и сейфа — во вкладках Tk. Найдено 7 дефектов поведения (D1–D7, задача R1.1), в том числе падение запуска и окна на ссылке формы с «]». Порядок: R1 исправления → R2 страховочная сетка тестов и сверка с донором в репозитории → R3 общие объекты → R4 слои и запуск → R5 контур A и конфиг → R6 merge → R7 настройщик → R8 завершение (карта слоёв и новые пакеты `app\run\`, `app\intake\` — в §5/§11). Документ — план, требования остаются здесь; после R8.2 его выводы переносятся сюда, а сам он удаляется.
- **Этап R ведётся по `REFACTORING.md` с поправками и порядком `REFACTORING_STANDARD.md`** (файл положил Артур 25-09-2026; факты Коворк сверил с кодом `50b4217`, исправления — раздел 12 файла). Эталон — 21 измеримое правило E1–E21; его держит замок `app\tools\code_standard` с реестром долгов `app\tests\data\code_standard\debt.json`, который только сокращается (задачи R2.1a, R2.1b). Порядок — 43 задачи раздела 8 (R1.1 разрезан на R1.1a и R1.1b), каждая не больше M. С R1.2 каждый промт задачи R несёт раздел ДОЛГИ К СНЯТИЮ (вывод `python -m app.tools.code_standard --files`) и блок ПРАВИЛА ЭТАПА R; приёмка — `--compare` с предыдущим коммитом.
- **Задача R1.1a принята 25-09-2026, коммит `5e215d6`.** D1: `FormSettings.url_problem` разбирает ссылку через `split_url` — негодный адрес (`https://[bad`, `…]/forms/x`) даёт проблему поля, а не падение запуска, окна настройки и переноса ссылки из сейфа. D2: `SecretInput.is_empty`, `KeyRow.empty_input_problem`, `KeysPanel._input_problem` — пустой ввод называет настоящую кнопку сброса. D3: `GoogleLogin.save` пишет токен через `write_text_atomically`; сбой записи и удаления — `AuthErrorReason.TOKEN_UNWRITABLE`. D5: `_is_language_code` — двухбуквенный код ISO 639-1 из справочника pycountry, негодный — `CONFIG_PROBLEM_LANGUAGE_UNKNOWN` с самим значением; живой `secrets\channels.json` проходит. D6: пробник качает обложку только источнику с решённым языком. D7: конечность паузы проверяет только загрузчик. Baseline тестов livecraft — **2517**. Пример каналов с одним языком не вошёл: он тянет строку `app\tests\test_main.py:508`, которой не было в SCOPE LOCK (промах промта Коворка); Codex остановился по (б) и приготовил проверенный патч — задача R1.1a-2. Долг: `LANGUAGE_CODE_LENGTH = 2` объявлен в `app\config\loader.py` и `app\texts\composer.py` — правило «код ISO 639-1» станет одним объектом (R3.5b, N18).
- **Задача R1.1a-2 принята 25-09-2026, коммит `6bad14d`.** Пример `app\examples\channels.example.json` — один язык на канал (решение 21); тесты ждут данные примера; совместимость со старым файлом с двумя языками держат тесты на фикстуре `two_languages_window`, новый — `test_the_table_shows_every_language_of_an_old_file`. Baseline тестов livecraft — **2518**. Следующая — R1.1b (D4, D9).
- **Задача R1.1b принята 25-09-2026, коммит `0f2d47c`.** D4: `app\secretsafe\crypto.py` — `VaultSource` (переехал из store.py), `VaultFormatReason` (file_unreadable, not_text, damaged, unsupported_version, key_invalid), `VaultFormatError` (поля `reason`, `detail`, `file_name`, `source`; `located`, `problem`, `is_replaceable`, `advice`, `human`, `log_line`; `str` = `human`), `Base64Record` — один разбор base64 вместо трёх копий; человек видит русскую причину, имя файла и одно действие, английская подробность — только в лог (`vault_error` в main, `vault_local_broken` в store). D9: `LocalVaultState.BROKEN`, `VaultStore.load_for_setup` и `load` — один путь `_load(read_local)`; `KeysPanel.from_store` читает `load_for_setup`, вкладка открывается с `SETUP_KEYS_NOTICE_LOCAL_BROKEN`; `Readiness.for_mode` — повреждённый свой файл чинится в окне (запуск открывает настройщик, код 2), повреждённый файл программы — совет переустановить. Baseline тестов livecraft — **2545**. Этап исправлений R1 (кроме R1.2) закрыт. Замечено: строка готовности в окне при повреждённом своём файле советует открыть окно (N19, R5.3).
- **Задача R2.1a принята 25-09-2026, коммит `34442a2`.** Замок эталона: пакет `app\tools\code_standard\` (`Rule`, `Standard` из `standard.json`, `SourceTree`/`ModuleSource`/`DefinitionSite`, `FunctionShape` — E1, E2, E3, E4, E12, E13, `ModuleShape` — E18, E19, E21, `Measurement`, `Ledger`/`LedgerDiff`, `Exceptions`, `StandardReport`, `StandardCommand`), команда `python -m app.tools.code_standard` (`--init`, `--write-debt`, `--compare`, `--files`), тест `app\tests\test_code_standard.py`; `test_module_definitions.py` стал правилом E21. Реестр `app\tests\data\code_standard\debt.json` — **170** записей (E1 63, E2 57, E3 9, E4 7, E12 7, E13 14, E18 12, E19 1, E21 0), исключений 5. Замок работает под Python 3.10 без пакетов проекта — Коворк запускает его прямо на репозитории. Baseline тестов livecraft — **2582**.
- **Задача R2.1b принята 25-09-2026, коммит `89d4afb`.** Замок проверяет все 21 правило: `CodeValues` (E5–E10, E14, E15, E17), `CloneFinder` (E11), `ImportGraph`/`LayerMap` (E16, карта слоёв в `standard.json`, пакет `examples` — уровень 0), `SuiteShape` (E20); пакет замка — изолированная область для E8/E9. Шесть тестов с обходом исходников — через `SourceTree`. Отчёт замка: E1 63 (+3 исключения), E2 57, E3 9, E4 7, E5 284, E6 8, E7 30, E8 37, E9 9, E10 35, E11 6, E12 7, E13 14 (+2), E14 20, E15 75, E16 13, E17 11, E18 12, E19 1, E20 46, E21 0 — реестр **744** записи. Baseline тестов livecraft — **2608**. С R1.2a каждый промт этапа R несёт ДОЛГИ К СНЯТИЮ и ПРАВИЛА ЭТАПА R.
